// imports
import scala.concurrent.duration._
import org.scalatest._
import akka.actor._
import akka.testkit._
import com.typesafe.config._
import org.ru.throstur.bus._

class Longspec extends TestKit(ActorSystem("longspec-spec-test")) with ImplicitSender with FlatSpecLike with Matchers with BeforeAndAfterAll {

	override def afterAll() = system.shutdown()


	"The System-under-test (0)" should "AUTO-GEN-TEST (0)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(0))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(0))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(0))
		expectMsg(Bus.Success)

	}

	"The System-under-test (1)" should "AUTO-GEN-TEST (1)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(1))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(1, "foo"))
		actorRef ! (Bus.Publish(1, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(1))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(1))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(1))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(1, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(1, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(1))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(1))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(1))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(1, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (2)" should "AUTO-GEN-TEST (2)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(2))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(2))
		actorRef ! (Bus.CreateConnection(2))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(2))
		actorRef ! (Bus.SubscribeCallback(2))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(2))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(2))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(2, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(2))
		expectMsg(Bus.Error)

	}

	"The System-under-test (3)" should "AUTO-GEN-TEST (3)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(3))
		actorRef ! (Bus.Publish(3, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(3))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(3))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(3))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(3))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(3))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(3))
		expectMsg(Bus.Success)

	}

	"The System-under-test (4)" should "AUTO-GEN-TEST (4)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(4))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(4))
		actorRef ! (Bus.Disconnect(4))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(4))
		actorRef ! (Bus.UnsubscribeCallback(4))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(4))
		actorRef ! (Bus.SubscribeCallback(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(4))
		actorRef ! (Bus.Unsubscribe(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(4, "foo"))
		actorRef ! (Bus.DestroyConnection(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(4))
		actorRef ! (Bus.UnsubscribeCallback(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(4))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(4))
		actorRef ! (Bus.Unsubscribe(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(4))
		actorRef ! (Bus.Unsubscribe(4))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(4))
		actorRef ! (Bus.DestroyConnection(4))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (5)" should "AUTO-GEN-TEST (5)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(5))
		actorRef ! (Bus.Unsubscribe(5))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(5))
		actorRef ! (Bus.UnsubscribeCallback(5))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(5))
		actorRef ! (Bus.Unsubscribe(5))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(5, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(5))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(5, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(5))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(5))
		expectMsg(Bus.Error)

	}

	"The System-under-test (6)" should "AUTO-GEN-TEST (6)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(6))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(6))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(6))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(6))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(6))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(6))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(6))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(6))
		expectMsg(Bus.Error)

	}

	"The System-under-test (7)" should "AUTO-GEN-TEST (7)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(7))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(7, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(7))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(7, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(7))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(7))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(7, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(7))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(7))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(7))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(7))
		expectMsg(Bus.Error)

	}

	"The System-under-test (8)" should "AUTO-GEN-TEST (8)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(8))
		actorRef ! (Bus.DestroyConnection(8))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(8))
		actorRef ! (Bus.Disconnect(8))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(8))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(8, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(8, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (9)" should "AUTO-GEN-TEST (9)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(9))
		actorRef ! (Bus.SubscribeCallback(9))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(9))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(9))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(9))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(9))
		actorRef ! (Bus.CreateConnection(9))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(9))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(9))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(9))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(9))
		expectMsg(Bus.Error)

	}

	"The System-under-test (10)" should "AUTO-GEN-TEST (10)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(10))
		actorRef ! (Bus.CreateConnection(10))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(10))
		actorRef ! (Bus.CreateConnection(10))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(10))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(10, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(10))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(10))
		expectMsg(Bus.Error)

	}

	"The System-under-test (11)" should "AUTO-GEN-TEST (11)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(11))
		actorRef ! (Bus.CreateConnection(11))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(11, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(11, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(11))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(11))
		actorRef ! (Bus.DestroyConnection(11))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(11))
		actorRef ! (Bus.Publish(11, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(11))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(11))
		expectMsg(Bus.Error)

	}

	"The System-under-test (12)" should "AUTO-GEN-TEST (12)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(12))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(12, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(12))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(12))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(12))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(12))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(12))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(12))
		expectMsg(Bus.Error)

	}

	"The System-under-test (13)" should "AUTO-GEN-TEST (13)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(13))
		actorRef ! (Bus.SubscribeCallback(13))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(13))
		actorRef ! (Bus.SubscribeCallback(13))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(13))
		actorRef ! (Bus.DestroyConnection(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(13))
		actorRef ! (Bus.Connect(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(13))
		actorRef ! (Bus.UnsubscribeCallback(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(13))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(13))
		actorRef ! (Bus.Subscribe(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(13))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(13))
		actorRef ! (Bus.SubscribeCallback(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(13))
		actorRef ! (Bus.Disconnect(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(13))
		actorRef ! (Bus.DestroyConnection(13))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (14)" should "AUTO-GEN-TEST (14)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(14))
		actorRef ! (Bus.CreateConnection(14))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(14))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(14))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(14))
		actorRef ! (Bus.Disconnect(14))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(14))
		actorRef ! (Bus.SubscribeCallback(14))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(14))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(14, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(14, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (15)" should "AUTO-GEN-TEST (15)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(15))
		actorRef ! (Bus.Unsubscribe(15))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(15))
		actorRef ! (Bus.CreateConnection(15))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(15))
		actorRef ! (Bus.Disconnect(15))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(15))
		actorRef ! (Bus.Publish(15, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(15))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(15))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(15, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(15))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(15))
		actorRef ! (Bus.UnsubscribeCallback(15))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(15))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(15))
		actorRef ! (Bus.DestroyConnection(15))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(15, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(15))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(15))
		expectMsg(Bus.Error)

	}

	"The System-under-test (16)" should "AUTO-GEN-TEST (16)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(16))
		actorRef ! (Bus.SubscribeCallback(16))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(16))
		actorRef ! (Bus.Subscribe(16))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(16))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(16))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(16))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(16))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(16, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(16))
		expectMsg(Bus.Error)

	}

	"The System-under-test (17)" should "AUTO-GEN-TEST (17)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(17))
		actorRef ! (Bus.UnsubscribeCallback(17))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(17))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(17))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(17, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (18)" should "AUTO-GEN-TEST (18)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(18))
		actorRef ! (Bus.SubscribeCallback(18))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(18))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(18))
		actorRef ! (Bus.CreateConnection(18))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(18))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(18))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(18))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(18))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(18))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(18))
		expectMsg(Bus.Success)

	}

	"The System-under-test (19)" should "AUTO-GEN-TEST (19)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(19))
		actorRef ! (Bus.Disconnect(19))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(19))
		expectMsg(Bus.Error)

	}

	"The System-under-test (20)" should "AUTO-GEN-TEST (20)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(20))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(20))
		actorRef ! (Bus.DestroyConnection(20))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(20, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(20))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(20))
		actorRef ! (Bus.SubscribeCallback(20))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(20))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(20))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(20))
		actorRef ! (Bus.UnsubscribeCallback(20))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(20))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(20))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(20))
		actorRef ! (Bus.CreateConnection(20))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(20))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(20))
		expectMsg(Bus.Error)

	}

	"The System-under-test (21)" should "AUTO-GEN-TEST (21)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(21))
		actorRef ! (Bus.Subscribe(21))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(21))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(21))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(21))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(21))
		expectMsg(Bus.Success)

	}

	"The System-under-test (22)" should "AUTO-GEN-TEST (22)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(22))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(22))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(22))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(22))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(22))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(22))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(22, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (23)" should "AUTO-GEN-TEST (23)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(23))
		actorRef ! (Bus.Connect(23))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(23))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(23))
		actorRef ! (Bus.Unsubscribe(23))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(23))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(23))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(23))
		actorRef ! (Bus.SubscribeCallback(23))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(23))
		actorRef ! (Bus.CreateConnection(23))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(23))
		actorRef ! (Bus.DestroyConnection(23))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(23))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(23))
		actorRef ! (Bus.DestroyConnection(23))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(23))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(23))
		actorRef ! (Bus.CreateConnection(23))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(23))
		expectMsg(Bus.Error)

	}

	"The System-under-test (24)" should "AUTO-GEN-TEST (24)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(24))
		actorRef ! (Bus.UnsubscribeCallback(24))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(24))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(24))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(24))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(24, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(24))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(24))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(24, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(24))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(24))
		expectMsg(Bus.Error)

	}

	"The System-under-test (25)" should "AUTO-GEN-TEST (25)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(25))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(25))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(25, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(25))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(25, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(25))
		expectMsg(Bus.Error)

	}

	"The System-under-test (26)" should "AUTO-GEN-TEST (26)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(26))
		actorRef ! (Bus.CreateConnection(26))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(26))
		actorRef ! (Bus.Connect(26))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(26))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(26))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(26))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(26))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(26))
		expectMsg(Bus.Success)

	}

	"The System-under-test (27)" should "AUTO-GEN-TEST (27)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(27))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(27))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(27))
		actorRef ! (Bus.Connect(27))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(27))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(27))
		actorRef ! (Bus.CreateConnection(27))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(27))
		actorRef ! (Bus.SubscribeCallback(27))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(27))
		actorRef ! (Bus.Unsubscribe(27))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(27))
		actorRef ! (Bus.DestroyConnection(27))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(27))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(27))
		actorRef ! (Bus.Publish(27, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(27))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(27))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(27))
		actorRef ! (Bus.SubscribeCallback(27))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (28)" should "AUTO-GEN-TEST (28)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(28))
		actorRef ! (Bus.CreateConnection(28))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(28))
		actorRef ! (Bus.UnsubscribeCallback(28))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(28))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(28, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(28))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(28))
		expectMsg(Bus.Error)

	}

	"The System-under-test (29)" should "AUTO-GEN-TEST (29)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(29))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(29))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(29))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(29))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(29, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (30)" should "AUTO-GEN-TEST (30)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(30))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(30))
		actorRef ! (Bus.UnsubscribeCallback(30))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(30))
		actorRef ! (Bus.Disconnect(30))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(30))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(30))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(30))
		actorRef ! (Bus.Subscribe(30))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(30))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(30))
		actorRef ! (Bus.Unsubscribe(30))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(30))
		actorRef ! (Bus.CreateConnection(30))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(30))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(30))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(30))
		actorRef ! (Bus.SubscribeCallback(30))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(30))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(30))
		expectMsg(Bus.Error)

	}

	"The System-under-test (31)" should "AUTO-GEN-TEST (31)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(31))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(31))
		actorRef ! (Bus.SubscribeCallback(31))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(31))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(31))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(31, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(31))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(31))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(31))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(31))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(31))
		expectMsg(Bus.Error)

	}

	"The System-under-test (32)" should "AUTO-GEN-TEST (32)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(32))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(32))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(32))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(32))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(32, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(32))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(32))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(32))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(32))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(32))
		expectMsg(Bus.Error)

	}

	"The System-under-test (33)" should "AUTO-GEN-TEST (33)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(33))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(33))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(33))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(33))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(33))
		expectMsg(Bus.Success)

	}

	"The System-under-test (34)" should "AUTO-GEN-TEST (34)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(34))
		actorRef ! (Bus.UnsubscribeCallback(34))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(34, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(34))
		actorRef ! (Bus.SubscribeCallback(34))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(34, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(34))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(34))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(34))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(34, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(34))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(34))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(34))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(34, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (35)" should "AUTO-GEN-TEST (35)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(35))
		actorRef ! (Bus.SubscribeCallback(35))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(35))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(35))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(35))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(35))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(35))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(35))
		expectMsg(Bus.Error)

	}

	"The System-under-test (36)" should "AUTO-GEN-TEST (36)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(36))
		actorRef ! (Bus.Subscribe(36))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(36, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(36))
		actorRef ! (Bus.Disconnect(36))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(36))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(36))
		actorRef ! (Bus.Disconnect(36))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(36))
		actorRef ! (Bus.UnsubscribeCallback(36))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(36))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(36))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(36))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(36))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(36, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (37)" should "AUTO-GEN-TEST (37)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(37))
		actorRef ! (Bus.DestroyConnection(37))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(37))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(37))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(37))
		expectMsg(Bus.Error)

	}

	"The System-under-test (38)" should "AUTO-GEN-TEST (38)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(38))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(38))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(38, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(38))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(38))
		expectMsg(Bus.Error)

	}

	"The System-under-test (39)" should "AUTO-GEN-TEST (39)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(39))
		expectMsg(Bus.Success)

	}

	"The System-under-test (40)" should "AUTO-GEN-TEST (40)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(40))
		actorRef ! (Bus.UnsubscribeCallback(40))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(40))
		actorRef ! (Bus.Subscribe(40))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(40))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(40))
		actorRef ! (Bus.Disconnect(40))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(40))
		actorRef ! (Bus.SubscribeCallback(40))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(40))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(40))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(40, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (41)" should "AUTO-GEN-TEST (41)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(41))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(41))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(41))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(41))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(41))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(41))
		expectMsg(Bus.Success)

	}

	"The System-under-test (42)" should "AUTO-GEN-TEST (42)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(42))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(42))
		actorRef ! (Bus.Unsubscribe(42))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(42))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(42))
		actorRef ! (Bus.Publish(42, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(42))
		actorRef ! (Bus.UnsubscribeCallback(42))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(42, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(42))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(42))
		actorRef ! (Bus.Disconnect(42))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(42))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(42, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(42))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(42, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(42))
		actorRef ! (Bus.SubscribeCallback(42))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(42))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(42))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(42))
		expectMsg(Bus.Success)

	}

	"The System-under-test (43)" should "AUTO-GEN-TEST (43)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(43))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(43))
		actorRef ! (Bus.SubscribeCallback(43))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(43, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(43))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(43))
		expectMsg(Bus.Error)

	}

	"The System-under-test (44)" should "AUTO-GEN-TEST (44)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(44))
		actorRef ! (Bus.CreateConnection(44))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(44, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(44, "foo"))
		actorRef ! (Bus.DestroyConnection(44))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(44, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(44))
		actorRef ! (Bus.CreateConnection(44))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(44))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(44))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(44))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(44))
		actorRef ! (Bus.Disconnect(44))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(44))
		expectMsg(Bus.Error)

	}

	"The System-under-test (45)" should "AUTO-GEN-TEST (45)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(45))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(45, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(45, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(45, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(45, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(45, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(45))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(45))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(45))
		expectMsg(Bus.Error)

	}

	"The System-under-test (46)" should "AUTO-GEN-TEST (46)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(46))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(46))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(46))
		actorRef ! (Bus.Unsubscribe(46))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(46))
		actorRef ! (Bus.CreateConnection(46))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(46))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(46, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (47)" should "AUTO-GEN-TEST (47)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(47))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(47))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(47))
		expectMsg(Bus.Error)

	}

	"The System-under-test (48)" should "AUTO-GEN-TEST (48)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(48))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(48))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(48))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(48))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(48, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(48))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(48))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(48))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(48))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(48, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (49)" should "AUTO-GEN-TEST (49)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(49))
		actorRef ! (Bus.Subscribe(49))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(49))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(49))
		actorRef ! (Bus.SubscribeCallback(49))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(49, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(49, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(49))
		actorRef ! (Bus.Unsubscribe(49))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(49))
		actorRef ! (Bus.Subscribe(49))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(49))
		actorRef ! (Bus.Unsubscribe(49))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(49))
		actorRef ! (Bus.UnsubscribeCallback(49))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(49, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(49))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(49, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(49, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (50)" should "AUTO-GEN-TEST (50)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(50))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(50))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(50))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(50))
		expectMsg(Bus.Error)

	}

	"The System-under-test (51)" should "AUTO-GEN-TEST (51)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(51))
		actorRef ! (Bus.Connect(51))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(51))
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(51))
		actorRef ! (Bus.DestroyConnection(51))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(51))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(51))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(51))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(51))
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(51))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(51))
		expectMsg(Bus.Error)

	}

	"The System-under-test (52)" should "AUTO-GEN-TEST (52)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(52))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(52, "foo"))
		actorRef ! (Bus.DestroyConnection(52))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(52))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(52))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(52))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(52))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(52))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(52))
		expectMsg(Bus.Error)

	}

	"The System-under-test (53)" should "AUTO-GEN-TEST (53)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(53))
		actorRef ! (Bus.Publish(53, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(53, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(53, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(53))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(53))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(53))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(53))
		expectMsg(Bus.Error)

	}

	"The System-under-test (54)" should "AUTO-GEN-TEST (54)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(54))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(54))
		actorRef ! (Bus.SubscribeCallback(54))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(54))
		actorRef ! (Bus.SubscribeCallback(54))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(54))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(54))
		actorRef ! (Bus.SubscribeCallback(54))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(54))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(54, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (55)" should "AUTO-GEN-TEST (55)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(55))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(55))
		actorRef ! (Bus.Unsubscribe(55))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(55))
		actorRef ! (Bus.Unsubscribe(55))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(55))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(55))
		actorRef ! (Bus.Disconnect(55))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(55))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(55))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(55))
		expectMsg(Bus.Success)

	}

	"The System-under-test (56)" should "AUTO-GEN-TEST (56)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(56))
		actorRef ! (Bus.Connect(56))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(56))
		actorRef ! (Bus.Connect(56))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(56))
		actorRef ! (Bus.CreateConnection(56))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(56))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(56))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(56))
		expectMsg(Bus.Error)

	}

	"The System-under-test (57)" should "AUTO-GEN-TEST (57)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(57))
		actorRef ! (Bus.Disconnect(57))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(57))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(57))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(57))
		expectMsg(Bus.Error)

	}

	"The System-under-test (58)" should "AUTO-GEN-TEST (58)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(58))
		actorRef ! (Bus.Disconnect(58))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(58))
		actorRef ! (Bus.SubscribeCallback(58))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(58))
		actorRef ! (Bus.Unsubscribe(58))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(58))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(58))
		actorRef ! (Bus.Connect(58))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(58))
		actorRef ! (Bus.DestroyConnection(58))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(58))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(58))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(58))
		expectMsg(Bus.Error)

	}

	"The System-under-test (59)" should "AUTO-GEN-TEST (59)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(59))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(59))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(59))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(59))
		expectMsg(Bus.Success)

	}

	"The System-under-test (60)" should "AUTO-GEN-TEST (60)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(60))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(60))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(60))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(60))
		expectMsg(Bus.Success)

	}

	"The System-under-test (61)" should "AUTO-GEN-TEST (61)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(61))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(61, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(61))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(61, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(61))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(61))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(61))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(61))
		expectMsg(Bus.Error)

	}

	"The System-under-test (62)" should "AUTO-GEN-TEST (62)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(62))
		actorRef ! (Bus.SubscribeCallback(62))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(62))
		actorRef ! (Bus.CreateConnection(62))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(62))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(62))
		expectMsg(Bus.Success)

	}

	"The System-under-test (63)" should "AUTO-GEN-TEST (63)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(63))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(63))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(63))
		actorRef ! (Bus.Connect(63))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(63, "foo"))
		actorRef ! (Bus.Subscribe(63))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(63))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(63))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(63))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(63, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (64)" should "AUTO-GEN-TEST (64)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(64))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(64))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(64))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(64, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(64))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(64))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(64))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(64, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (65)" should "AUTO-GEN-TEST (65)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(65))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(65))
		actorRef ! (Bus.UnsubscribeCallback(65))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(65, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(65))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(65))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(65, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(65, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(65))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(65))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(65))
		expectMsg(Bus.Error)

	}

	"The System-under-test (66)" should "AUTO-GEN-TEST (66)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(66))
		expectMsg(Bus.Success)

	}

	"The System-under-test (67)" should "AUTO-GEN-TEST (67)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(67))
		actorRef ! (Bus.CreateConnection(67))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(67, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(67))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(67, "foo"))
		actorRef ! (Bus.Unsubscribe(67))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(67))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(67))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(67))
		expectMsg(Bus.Error)

	}

	"The System-under-test (68)" should "AUTO-GEN-TEST (68)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(68))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(68))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(68))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(68))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(68, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (69)" should "AUTO-GEN-TEST (69)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(69))
		actorRef ! (Bus.Subscribe(69))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(69))
		actorRef ! (Bus.Disconnect(69))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(69))
		actorRef ! (Bus.DestroyConnection(69))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(69))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(69))
		actorRef ! (Bus.CreateConnection(69))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(69, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(69))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(69))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(69))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(69))
		expectMsg(Bus.Success)

	}

	"The System-under-test (70)" should "AUTO-GEN-TEST (70)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(70))
		actorRef ! (Bus.Subscribe(70))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(70))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(70, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(70))
		expectMsg(Bus.Success)

	}

	"The System-under-test (71)" should "AUTO-GEN-TEST (71)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(71))
		actorRef ! (Bus.Publish(71, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(71))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(71))
		actorRef ! (Bus.Publish(71, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(71))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(71))
		actorRef ! (Bus.Publish(71, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(71))
		actorRef ! (Bus.DestroyConnection(71))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(71))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(71))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(71))
		expectMsg(Bus.Error)

	}

	"The System-under-test (72)" should "AUTO-GEN-TEST (72)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(72))
		actorRef ! (Bus.Connect(72))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(72))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(72, "foo"))
		actorRef ! (Bus.Connect(72))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(72))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(72))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(72, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (73)" should "AUTO-GEN-TEST (73)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(73))
		actorRef ! (Bus.Unsubscribe(73))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(73))
		actorRef ! (Bus.Connect(73))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(73))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(73))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(73))
		expectMsg(Bus.Success)

	}

	"The System-under-test (74)" should "AUTO-GEN-TEST (74)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(74))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(74))
		actorRef ! (Bus.DestroyConnection(74))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(74))
		actorRef ! (Bus.Publish(74, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(74))
		actorRef ! (Bus.Disconnect(74))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(74))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(74))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(74))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(74, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(74))
		actorRef ! (Bus.Subscribe(74))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(74))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(74))
		actorRef ! (Bus.Disconnect(74))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(74))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(74, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(74))
		actorRef ! (Bus.Disconnect(74))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (75)" should "AUTO-GEN-TEST (75)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(75))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(75))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(75, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(75))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(75))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(75))
		expectMsg(Bus.Error)

	}

	"The System-under-test (76)" should "AUTO-GEN-TEST (76)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(76))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(76))
		actorRef ! (Bus.Unsubscribe(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(76))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(76, "foo"))
		actorRef ! (Bus.Disconnect(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(76))
		actorRef ! (Bus.Disconnect(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(76, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(76))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(76))
		actorRef ! (Bus.UnsubscribeCallback(76))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(76))
		actorRef ! (Bus.Connect(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(76))
		actorRef ! (Bus.CreateConnection(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(76, "foo"))
		actorRef ! (Bus.SubscribeCallback(76))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(76))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(76))
		actorRef ! (Bus.CreateConnection(76))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(76))
		expectMsg(Bus.Success)

	}

	"The System-under-test (77)" should "AUTO-GEN-TEST (77)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(77))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(77))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(77))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(77))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(77))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(77, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(77))
		expectMsg(Bus.Error)

	}

	"The System-under-test (78)" should "AUTO-GEN-TEST (78)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(78))
		actorRef ! (Bus.UnsubscribeCallback(78))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(78))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(78, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(78))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(78))
		expectMsg(Bus.Error)

	}

	"The System-under-test (79)" should "AUTO-GEN-TEST (79)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(79))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(79))
		actorRef ! (Bus.Disconnect(79))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(79))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(79))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(79))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(79, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(79))
		actorRef ! (Bus.Subscribe(79))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(79))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(79))
		actorRef ! (Bus.Subscribe(79))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(79, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(79))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(79))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(79))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(79))
		actorRef ! (Bus.Subscribe(79))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(79))
		actorRef ! (Bus.Disconnect(79))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (80)" should "AUTO-GEN-TEST (80)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(80))
		actorRef ! (Bus.SubscribeCallback(80))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(80, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(80))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(80))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(80, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (81)" should "AUTO-GEN-TEST (81)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(81))
		actorRef ! (Bus.CreateConnection(81))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(81))
		actorRef ! (Bus.Connect(81))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(81))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(81))
		actorRef ! (Bus.Unsubscribe(81))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(81))
		actorRef ! (Bus.Disconnect(81))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(81))
		actorRef ! (Bus.Disconnect(81))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(81))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(81))
		expectMsg(Bus.Error)

	}

	"The System-under-test (82)" should "AUTO-GEN-TEST (82)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(82))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(82))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(82))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(82))
		actorRef ! (Bus.CreateConnection(82))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(82, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (83)" should "AUTO-GEN-TEST (83)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(83))
		actorRef ! (Bus.Unsubscribe(83))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(83))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(83))
		actorRef ! (Bus.Disconnect(83))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(83))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(83))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(83))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(83))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(83))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(83, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (84)" should "AUTO-GEN-TEST (84)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(84))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(84))
		actorRef ! (Bus.Publish(84, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(84))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(84))
		actorRef ! (Bus.CreateConnection(84))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(84))
		actorRef ! (Bus.SubscribeCallback(84))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(84))
		actorRef ! (Bus.Publish(84, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(84))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(84))
		expectMsg(Bus.Error)

	}

	"The System-under-test (85)" should "AUTO-GEN-TEST (85)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(85))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(85))
		actorRef ! (Bus.CreateConnection(85))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(85))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(85, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(85, "foo"))
		actorRef ! (Bus.Connect(85))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(85, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (86)" should "AUTO-GEN-TEST (86)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(86))
		actorRef ! (Bus.Publish(86, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(86))
		actorRef ! (Bus.Unsubscribe(86))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(86))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(86))
		actorRef ! (Bus.Disconnect(86))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(86))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(86))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(86))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(86))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(86, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(86))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(86))
		expectMsg(Bus.Error)

	}

	"The System-under-test (87)" should "AUTO-GEN-TEST (87)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(87))
		actorRef ! (Bus.Publish(87, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(87))
		actorRef ! (Bus.CreateConnection(87))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(87))
		actorRef ! (Bus.Unsubscribe(87))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(87))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(87))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(87))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(87))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(87))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(87, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (88)" should "AUTO-GEN-TEST (88)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(88))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(88, "foo"))
		actorRef ! (Bus.Unsubscribe(88))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(88, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(88, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(88, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(88))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(88))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(88))
		expectMsg(Bus.Error)

	}

	"The System-under-test (89)" should "AUTO-GEN-TEST (89)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(89))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(89))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(89))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(89))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(89))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(89))
		expectMsg(Bus.Error)

	}

	"The System-under-test (90)" should "AUTO-GEN-TEST (90)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(90))
		actorRef ! (Bus.CreateConnection(90))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(90))
		actorRef ! (Bus.Unsubscribe(90))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(90))
		actorRef ! (Bus.Disconnect(90))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(90))
		actorRef ! (Bus.DestroyConnection(90))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(90, "foo"))
		actorRef ! (Bus.Unsubscribe(90))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(90))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(90))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(90))
		expectMsg(Bus.Error)

	}

	"The System-under-test (91)" should "AUTO-GEN-TEST (91)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(91))
		actorRef ! (Bus.Publish(91, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(91))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(91))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(91))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(91))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(91))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(91))
		expectMsg(Bus.Error)

	}

	"The System-under-test (92)" should "AUTO-GEN-TEST (92)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(92))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(92))
		actorRef ! (Bus.Disconnect(92))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(92))
		actorRef ! (Bus.Publish(92, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(92))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(92))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(92))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(92))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(92))
		actorRef ! (Bus.SubscribeCallback(92))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(92))
		actorRef ! (Bus.Subscribe(92))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(92))
		actorRef ! (Bus.Disconnect(92))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(92))
		actorRef ! (Bus.UnsubscribeCallback(92))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(92))
		actorRef ! (Bus.DestroyConnection(92))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(92))
		expectMsg(Bus.Error)

	}

	"The System-under-test (93)" should "AUTO-GEN-TEST (93)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(93))
		actorRef ! (Bus.UnsubscribeCallback(93))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(93))
		actorRef ! (Bus.DestroyConnection(93))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(93))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(93))
		actorRef ! (Bus.DestroyConnection(93))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(93))
		actorRef ! (Bus.DestroyConnection(93))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(93, "foo"))
		actorRef ! (Bus.SubscribeCallback(93))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(93))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(93))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(93))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(93))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(93))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(93))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(93))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(93))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(93))
		expectMsg(Bus.Success)

	}

	"The System-under-test (94)" should "AUTO-GEN-TEST (94)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(94))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(94))
		expectMsg(Bus.Error)

	}

	"The System-under-test (95)" should "AUTO-GEN-TEST (95)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(95))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(95))
		actorRef ! (Bus.Unsubscribe(95))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(95, "foo"))
		actorRef ! (Bus.SubscribeCallback(95))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(95))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(95))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(95))
		actorRef ! (Bus.SubscribeCallback(95))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(95))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(95))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(95))
		expectMsg(Bus.Error)

	}

	"The System-under-test (96)" should "AUTO-GEN-TEST (96)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(96, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(96))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(96))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(96, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(96))
		expectMsg(Bus.Error)

	}

	"The System-under-test (97)" should "AUTO-GEN-TEST (97)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(97))
		actorRef ! (Bus.CreateConnection(97))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(97))
		actorRef ! (Bus.Connect(97))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(97, "foo"))
		actorRef ! (Bus.Publish(97, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (98)" should "AUTO-GEN-TEST (98)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(98))
		actorRef ! (Bus.Disconnect(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(98))
		actorRef ! (Bus.Disconnect(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(98))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(98, "foo"))
		actorRef ! (Bus.CreateConnection(98))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(98))
		actorRef ! (Bus.Disconnect(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(98, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(98))
		actorRef ! (Bus.Connect(98))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(98))
		actorRef ! (Bus.Subscribe(98))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(98))
		actorRef ! (Bus.CreateConnection(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(98))
		actorRef ! (Bus.SubscribeCallback(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(98))
		actorRef ! (Bus.DestroyConnection(98))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(98))
		expectMsg(Bus.Error)

	}

	"The System-under-test (99)" should "AUTO-GEN-TEST (99)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(99))
		actorRef ! (Bus.DestroyConnection(99))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(99))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(99))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(99))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(99, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (100)" should "AUTO-GEN-TEST (100)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(100))
		actorRef ! (Bus.SubscribeCallback(100))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(100))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(100))
		actorRef ! (Bus.Disconnect(100))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(100))
		actorRef ! (Bus.Unsubscribe(100))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(100, "foo"))
		actorRef ! (Bus.CreateConnection(100))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(100))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(100))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(100))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(100))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(100))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(100))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(100, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (101)" should "AUTO-GEN-TEST (101)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(101))
		actorRef ! (Bus.Publish(101, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(101))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(101, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(101))
		expectMsg(Bus.Error)

	}

	"The System-under-test (102)" should "AUTO-GEN-TEST (102)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(102))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(102, "foo"))
		actorRef ! (Bus.SubscribeCallback(102))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(102))
		actorRef ! (Bus.Subscribe(102))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(102))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(102, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(102, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(102))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(102))
		expectMsg(Bus.Error)

	}

	"The System-under-test (103)" should "AUTO-GEN-TEST (103)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(103))
		actorRef ! (Bus.CreateConnection(103))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(103))
		actorRef ! (Bus.Connect(103))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(103))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(103))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(103))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(103))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(103))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(103))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(103))
		actorRef ! (Bus.CreateConnection(103))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(103))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(103))
		actorRef ! (Bus.CreateConnection(103))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(103))
		actorRef ! (Bus.Disconnect(103))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(103))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(103))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(103))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(103))
		expectMsg(Bus.Success)

	}

	"The System-under-test (104)" should "AUTO-GEN-TEST (104)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(104))
		actorRef ! (Bus.Connect(104))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(104))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(104))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(104))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(104))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(104, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(104))
		expectMsg(Bus.Success)

	}

	"The System-under-test (105)" should "AUTO-GEN-TEST (105)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(105))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(105))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(105))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(105))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(105))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(105))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(105))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(105))
		expectMsg(Bus.Success)

	}

	"The System-under-test (106)" should "AUTO-GEN-TEST (106)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(106))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(106))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(106))
		expectMsg(Bus.Error)

	}

	"The System-under-test (107)" should "AUTO-GEN-TEST (107)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(107))
		actorRef ! (Bus.DestroyConnection(107))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(107))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(107))
		actorRef ! (Bus.UnsubscribeCallback(107))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(107))
		actorRef ! (Bus.Publish(107, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(107))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(107, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(107))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(107))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(107))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(107))
		expectMsg(Bus.Error)

	}

	"The System-under-test (108)" should "AUTO-GEN-TEST (108)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(108))
		actorRef ! (Bus.Connect(108))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(108))
		actorRef ! (Bus.DestroyConnection(108))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(108))
		actorRef ! (Bus.Unsubscribe(108))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(108))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(108))
		expectMsg(Bus.Error)

	}

	"The System-under-test (109)" should "AUTO-GEN-TEST (109)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(109))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(109))
		expectMsg(Bus.Success)

	}

	"The System-under-test (110)" should "AUTO-GEN-TEST (110)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(110))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(110, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(110))
		actorRef ! (Bus.Subscribe(110))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(110))
		actorRef ! (Bus.Connect(110))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(110, "foo"))
		actorRef ! (Bus.Subscribe(110))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(110))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(110))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(110))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(110))
		expectMsg(Bus.Error)

	}

	"The System-under-test (111)" should "AUTO-GEN-TEST (111)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(111))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(111))
		actorRef ! (Bus.DestroyConnection(111))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(111))
		actorRef ! (Bus.CreateConnection(111))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(111))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(111))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(111))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(111))
		expectMsg(Bus.Error)

	}

	"The System-under-test (112)" should "AUTO-GEN-TEST (112)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(112, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(112, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(112))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(112))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(112))
		expectMsg(Bus.Error)

	}

	"The System-under-test (113)" should "AUTO-GEN-TEST (113)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(113))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(113, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(113, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(113))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(113))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(113))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(113))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(113))
		expectMsg(Bus.Error)

	}

	"The System-under-test (114)" should "AUTO-GEN-TEST (114)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(114))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(114))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(114))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(114))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(114))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(114))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(114))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(114))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(114))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(114))
		expectMsg(Bus.Error)

	}

	"The System-under-test (115)" should "AUTO-GEN-TEST (115)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(115))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(115))
		actorRef ! (Bus.Connect(115))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(115))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(115))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(115))
		actorRef ! (Bus.CreateConnection(115))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(115))
		actorRef ! (Bus.DestroyConnection(115))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(115))
		actorRef ! (Bus.Subscribe(115))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(115))
		actorRef ! (Bus.Connect(115))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(115))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(115))
		expectMsg(Bus.Error)

	}

	"The System-under-test (116)" should "AUTO-GEN-TEST (116)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(116))
		actorRef ! (Bus.CreateConnection(116))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(116))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(116))
		actorRef ! (Bus.Disconnect(116))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(116))
		actorRef ! (Bus.Connect(116))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(116))
		actorRef ! (Bus.Connect(116))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(116))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(116))
		actorRef ! (Bus.Connect(116))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(116))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(116))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(116))
		actorRef ! (Bus.CreateConnection(116))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(116))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(116, "foo"))
		actorRef ! (Bus.DestroyConnection(116))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(116))
		expectMsg(Bus.Error)

	}

	"The System-under-test (117)" should "AUTO-GEN-TEST (117)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(117))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(117, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(117))
		actorRef ! (Bus.Disconnect(117))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(117, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(117))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(117))
		expectMsg(Bus.Success)

	}

	"The System-under-test (118)" should "AUTO-GEN-TEST (118)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(118))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(118, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(118))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(118, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(118, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(118))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(118))
		expectMsg(Bus.Error)

	}

	"The System-under-test (119)" should "AUTO-GEN-TEST (119)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(119))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(119))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(119))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(119, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (120)" should "AUTO-GEN-TEST (120)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(120))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(120))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(120))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(120))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(120, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(120, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(120))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(120))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(120, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (121)" should "AUTO-GEN-TEST (121)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(121))
		actorRef ! (Bus.Publish(121, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(121))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(121))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(121))
		actorRef ! (Bus.DestroyConnection(121))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(121, "foo"))
		actorRef ! (Bus.Unsubscribe(121))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(121, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(121))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(121))
		actorRef ! (Bus.Disconnect(121))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(121))
		actorRef ! (Bus.SubscribeCallback(121))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(121))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(121))
		actorRef ! (Bus.Unsubscribe(121))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(121))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(121))
		expectMsg(Bus.Error)

	}

	"The System-under-test (122)" should "AUTO-GEN-TEST (122)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(122))
		actorRef ! (Bus.UnsubscribeCallback(122))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (123)" should "AUTO-GEN-TEST (123)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(123))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(123))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(123))
		actorRef ! (Bus.Connect(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(123))
		actorRef ! (Bus.CreateConnection(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(123))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(123))
		actorRef ! (Bus.SubscribeCallback(123))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(123))
		actorRef ! (Bus.Unsubscribe(123))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(123, "foo"))
		actorRef ! (Bus.Subscribe(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(123))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(123, "foo"))
		actorRef ! (Bus.Subscribe(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(123))
		actorRef ! (Bus.SubscribeCallback(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(123))
		actorRef ! (Bus.Subscribe(123))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (124)" should "AUTO-GEN-TEST (124)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(124))
		actorRef ! (Bus.UnsubscribeCallback(124))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(124))
		actorRef ! (Bus.Disconnect(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(124, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(124))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(124))
		actorRef ! (Bus.DestroyConnection(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(124))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(124))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(124))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(124))
		actorRef ! (Bus.UnsubscribeCallback(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(124, "foo"))
		actorRef ! (Bus.Unsubscribe(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(124))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(124))
		actorRef ! (Bus.Unsubscribe(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(124))
		actorRef ! (Bus.UnsubscribeCallback(124))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (125)" should "AUTO-GEN-TEST (125)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(125))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(125))
		actorRef ! (Bus.Unsubscribe(125))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(125))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(125))
		actorRef ! (Bus.Unsubscribe(125))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(125))
		actorRef ! (Bus.Connect(125))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(125))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(125))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(125))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(125))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(125, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(125, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(125))
		actorRef ! (Bus.Unsubscribe(125))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(125))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(125))
		actorRef ! (Bus.Connect(125))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(125))
		expectMsg(Bus.Error)

	}

	"The System-under-test (126)" should "AUTO-GEN-TEST (126)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(126))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(126))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(126))
		actorRef ! (Bus.UnsubscribeCallback(126))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(126, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(126))
		actorRef ! (Bus.CreateConnection(126))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(126))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(126))
		actorRef ! (Bus.Publish(126, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(126))
		actorRef ! (Bus.SubscribeCallback(126))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(126))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(126, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (127)" should "AUTO-GEN-TEST (127)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(127))
		actorRef ! (Bus.Connect(127))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(127))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(127))
		actorRef ! (Bus.Unsubscribe(127))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(127))
		actorRef ! (Bus.Disconnect(127))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(127, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(127))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(127))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(127))
		expectMsg(Bus.Error)

	}

	"The System-under-test (128)" should "AUTO-GEN-TEST (128)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(128))
		actorRef ! (Bus.SubscribeCallback(128))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(128))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(128))
		actorRef ! (Bus.SubscribeCallback(128))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(128))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(128))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(128))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(128))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(128))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(128))
		expectMsg(Bus.Error)

	}

	"The System-under-test (129)" should "AUTO-GEN-TEST (129)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(129))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(129))
		actorRef ! (Bus.Publish(129, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(129))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(129))
		actorRef ! (Bus.Disconnect(129))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(129))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(129))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(129))
		actorRef ! (Bus.Publish(129, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(129))
		expectMsg(Bus.Error)

	}

	"The System-under-test (130)" should "AUTO-GEN-TEST (130)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(130))
		actorRef ! (Bus.SubscribeCallback(130))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(130))
		expectMsg(Bus.Error)

	}

	"The System-under-test (131)" should "AUTO-GEN-TEST (131)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(131))
		expectMsg(Bus.Success)

	}

	"The System-under-test (132)" should "AUTO-GEN-TEST (132)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(132))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(132))
		actorRef ! (Bus.UnsubscribeCallback(132))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(132, "foo"))
		actorRef ! (Bus.Connect(132))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(132))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(132))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(132))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(132, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(132))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(132))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(132))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(132, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (133)" should "AUTO-GEN-TEST (133)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(133))
		actorRef ! (Bus.Subscribe(133))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(133))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(133, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(133))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(133))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(133))
		expectMsg(Bus.Error)

	}

	"The System-under-test (134)" should "AUTO-GEN-TEST (134)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(134))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(134))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(134, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(134))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(134))
		expectMsg(Bus.Error)

	}

	"The System-under-test (135)" should "AUTO-GEN-TEST (135)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(135))
		actorRef ! (Bus.CreateConnection(135))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(135))
		actorRef ! (Bus.CreateConnection(135))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(135))
		actorRef ! (Bus.Subscribe(135))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(135))
		actorRef ! (Bus.Unsubscribe(135))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(135, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(135))
		actorRef ! (Bus.Disconnect(135))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(135))
		actorRef ! (Bus.SubscribeCallback(135))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(135))
		expectMsg(Bus.Error)

	}

	"The System-under-test (136)" should "AUTO-GEN-TEST (136)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(136))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(136))
		actorRef ! (Bus.SubscribeCallback(136))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(136))
		actorRef ! (Bus.Publish(136, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(136))
		actorRef ! (Bus.Publish(136, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(136))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(136))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(136))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(136))
		expectMsg(Bus.Error)

	}

	"The System-under-test (137)" should "AUTO-GEN-TEST (137)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(137))
		actorRef ! (Bus.UnsubscribeCallback(137))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(137))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(137))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(137, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(137))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(137))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(137))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(137, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(137))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(137))
		expectMsg(Bus.Error)

	}

	"The System-under-test (138)" should "AUTO-GEN-TEST (138)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(138))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(138, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(138))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(138))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(138))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(138))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(138))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(138))
		expectMsg(Bus.Error)

	}

	"The System-under-test (139)" should "AUTO-GEN-TEST (139)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(139))
		actorRef ! (Bus.Disconnect(139))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(139))
		actorRef ! (Bus.Subscribe(139))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(139))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(139))
		actorRef ! (Bus.CreateConnection(139))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(139))
		actorRef ! (Bus.UnsubscribeCallback(139))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(139))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(139))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(139))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(139))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(139))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(139))
		expectMsg(Bus.Success)

	}

	"The System-under-test (140)" should "AUTO-GEN-TEST (140)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(140))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(140))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(140))
		actorRef ! (Bus.SubscribeCallback(140))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(140))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(140))
		actorRef ! (Bus.Unsubscribe(140))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(140))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(140, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(140))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(140))
		expectMsg(Bus.Error)

	}

	"The System-under-test (141)" should "AUTO-GEN-TEST (141)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(141))
		actorRef ! (Bus.Subscribe(141))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(141))
		actorRef ! (Bus.Disconnect(141))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		actorRef ! (Bus.Subscribe(141))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(141))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(141, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (142)" should "AUTO-GEN-TEST (142)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(142))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(142))
		expectMsg(Bus.Error)

	}

	"The System-under-test (143)" should "AUTO-GEN-TEST (143)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(143))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(143))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(143))
		expectMsg(Bus.Success)

	}

	"The System-under-test (144)" should "AUTO-GEN-TEST (144)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(144))
		actorRef ! (Bus.Subscribe(144))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(144))
		actorRef ! (Bus.DestroyConnection(144))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(144, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(144))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(144))
		actorRef ! (Bus.Unsubscribe(144))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(144))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(144))
		actorRef ! (Bus.Unsubscribe(144))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(144))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(144))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(144))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(144))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(144))
		expectMsg(Bus.Success)

	}

	"The System-under-test (145)" should "AUTO-GEN-TEST (145)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(145))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(145))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(145))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(145))
		expectMsg(Bus.Error)

	}

	"The System-under-test (146)" should "AUTO-GEN-TEST (146)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(146))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(146))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(146))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(146, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (147)" should "AUTO-GEN-TEST (147)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(147))
		actorRef ! (Bus.SubscribeCallback(147))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(147))
		actorRef ! (Bus.Publish(147, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(147))
		actorRef ! (Bus.SubscribeCallback(147))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(147))
		actorRef ! (Bus.Publish(147, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(147, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(147))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(147, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (148)" should "AUTO-GEN-TEST (148)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(148))
		actorRef ! (Bus.CreateConnection(148))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(148))
		actorRef ! (Bus.Subscribe(148))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(148))
		actorRef ! (Bus.CreateConnection(148))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(148))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(148))
		actorRef ! (Bus.CreateConnection(148))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(148))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(148))
		expectMsg(Bus.Error)

	}

	"The System-under-test (149)" should "AUTO-GEN-TEST (149)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(149))
		actorRef ! (Bus.UnsubscribeCallback(149))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(149))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(149))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(149))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(149, "foo"))
		actorRef ! (Bus.Publish(149, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (150)" should "AUTO-GEN-TEST (150)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(150))
		actorRef ! (Bus.UnsubscribeCallback(150))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(150))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(150, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(150))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(150))
		expectMsg(Bus.Error)

	}

	"The System-under-test (151)" should "AUTO-GEN-TEST (151)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(151))
		actorRef ! (Bus.CreateConnection(151))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(151, "foo"))
		actorRef ! (Bus.Disconnect(151))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(151))
		actorRef ! (Bus.UnsubscribeCallback(151))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(151))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(151))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(151))
		expectMsg(Bus.Error)

	}

	"The System-under-test (152)" should "AUTO-GEN-TEST (152)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(152))
		actorRef ! (Bus.CreateConnection(152))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(152))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(152))
		actorRef ! (Bus.UnsubscribeCallback(152))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(152, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(152))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(152))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(152))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(152))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(152))
		expectMsg(Bus.Error)

	}

	"The System-under-test (153)" should "AUTO-GEN-TEST (153)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(153))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(153, "foo"))
		actorRef ! (Bus.DestroyConnection(153))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(153, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(153))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(153))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(153))
		expectMsg(Bus.Success)

	}

	"The System-under-test (154)" should "AUTO-GEN-TEST (154)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(154))
		actorRef ! (Bus.Subscribe(154))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(154))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(154))
		actorRef ! (Bus.Subscribe(154))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(154))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(154, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(154))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(154))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(154))
		expectMsg(Bus.Error)

	}

	"The System-under-test (155)" should "AUTO-GEN-TEST (155)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(155))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(155))
		actorRef ! (Bus.Publish(155, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(155))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(155))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(155))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(155))
		actorRef ! (Bus.Publish(155, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(155))
		actorRef ! (Bus.DestroyConnection(155))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (156)" should "AUTO-GEN-TEST (156)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(156))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(156, "foo"))
		actorRef ! (Bus.Publish(156, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(156, "foo"))
		actorRef ! (Bus.Unsubscribe(156))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(156))
		actorRef ! (Bus.Publish(156, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(156))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(156, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(156))
		actorRef ! (Bus.Subscribe(156))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(156))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(156))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(156))
		actorRef ! (Bus.Subscribe(156))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(156))
		actorRef ! (Bus.Connect(156))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(156))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(156))
		actorRef ! (Bus.Connect(156))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(156, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (157)" should "AUTO-GEN-TEST (157)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(157, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(157, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(157))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(157, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(157))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(157))
		expectMsg(Bus.Error)

	}

	"The System-under-test (158)" should "AUTO-GEN-TEST (158)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(158))
		actorRef ! (Bus.Disconnect(158))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(158))
		actorRef ! (Bus.Unsubscribe(158))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(158))
		actorRef ! (Bus.DestroyConnection(158))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(158))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(158))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(158))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(158))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(158))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(158, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (159)" should "AUTO-GEN-TEST (159)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(159))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(159))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(159))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(159, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(159))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(159))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(159))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(159))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(159))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(159))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(159))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(159))
		expectMsg(Bus.Error)

	}

	"The System-under-test (160)" should "AUTO-GEN-TEST (160)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(160))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(160))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(160))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(160))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(160))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(160))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(160, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (161)" should "AUTO-GEN-TEST (161)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(161))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(161))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(161))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(161))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(161))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(161))
		actorRef ! (Bus.Publish(161, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (162)" should "AUTO-GEN-TEST (162)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(162))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(162))
		actorRef ! (Bus.SubscribeCallback(162))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(162))
		actorRef ! (Bus.CreateConnection(162))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(162))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(162))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(162))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(162))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(162))
		actorRef ! (Bus.Connect(162))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(162))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(162))
		actorRef ! (Bus.UnsubscribeCallback(162))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(162))
		actorRef ! (Bus.Disconnect(162))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(162, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(162))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(162))
		expectMsg(Bus.Error)

	}

	"The System-under-test (163)" should "AUTO-GEN-TEST (163)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(163))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(163))
		actorRef ! (Bus.Publish(163, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(163))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(163, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (164)" should "AUTO-GEN-TEST (164)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(164))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(164))
		expectMsg(Bus.Error)

	}

	"The System-under-test (165)" should "AUTO-GEN-TEST (165)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(165))
		actorRef ! (Bus.Connect(165))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(165))
		actorRef ! (Bus.Disconnect(165))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(165))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(165))
		expectMsg(Bus.Error)

	}

	"The System-under-test (166)" should "AUTO-GEN-TEST (166)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(166))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(166))
		actorRef ! (Bus.Unsubscribe(166))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(166))
		actorRef ! (Bus.Publish(166, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(166))
		actorRef ! (Bus.Subscribe(166))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(166))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(166))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(166))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(166, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (167)" should "AUTO-GEN-TEST (167)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(167))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(167))
		actorRef ! (Bus.SubscribeCallback(167))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(167))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(167))
		actorRef ! (Bus.Publish(167, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(167))
		actorRef ! (Bus.Disconnect(167))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(167))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(167))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(167))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(167))
		actorRef ! (Bus.UnsubscribeCallback(167))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(167, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(167))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(167))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(167, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(167))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(167))
		expectMsg(Bus.Error)

	}

	"The System-under-test (168)" should "AUTO-GEN-TEST (168)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(168))
		actorRef ! (Bus.CreateConnection(168))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(168))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(168))
		actorRef ! (Bus.Subscribe(168))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(168))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(168))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(168))
		actorRef ! (Bus.Publish(168, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(168))
		actorRef ! (Bus.Unsubscribe(168))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(168))
		actorRef ! (Bus.Disconnect(168))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(168))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(168))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(168))
		actorRef ! (Bus.Publish(168, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(168))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(168))
		actorRef ! (Bus.Disconnect(168))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (169)" should "AUTO-GEN-TEST (169)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(169))
		actorRef ! (Bus.Publish(169, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(169))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(169))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(169))
		actorRef ! (Bus.DestroyConnection(169))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(169))
		actorRef ! (Bus.Unsubscribe(169))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(169))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(169))
		expectMsg(Bus.Error)

	}

	"The System-under-test (170)" should "AUTO-GEN-TEST (170)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(170))
		actorRef ! (Bus.Publish(170, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(170))
		actorRef ! (Bus.Unsubscribe(170))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(170))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(170, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(170))
		actorRef ! (Bus.DestroyConnection(170))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(170, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(170))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(170))
		expectMsg(Bus.Error)

	}

	"The System-under-test (171)" should "AUTO-GEN-TEST (171)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(171))
		actorRef ! (Bus.Publish(171, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(171))
		actorRef ! (Bus.SubscribeCallback(171))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(171))
		actorRef ! (Bus.UnsubscribeCallback(171))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(171))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(171))
		actorRef ! (Bus.DestroyConnection(171))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(171))
		actorRef ! (Bus.Disconnect(171))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(171))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(171))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(171))
		actorRef ! (Bus.UnsubscribeCallback(171))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(171, "foo"))
		actorRef ! (Bus.Subscribe(171))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(171))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(171, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (172)" should "AUTO-GEN-TEST (172)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(172))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(172))
		actorRef ! (Bus.CreateConnection(172))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(172))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(172, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(172))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(172, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(172))
		expectMsg(Bus.Error)

	}

	"The System-under-test (173)" should "AUTO-GEN-TEST (173)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(173))
		actorRef ! (Bus.UnsubscribeCallback(173))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(173, "foo"))
		actorRef ! (Bus.Subscribe(173))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(173))
		actorRef ! (Bus.UnsubscribeCallback(173))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(173))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(173))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(173))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(173, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (174)" should "AUTO-GEN-TEST (174)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(174))
		actorRef ! (Bus.Publish(174, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(174))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(174))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(174))
		actorRef ! (Bus.Unsubscribe(174))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(174))
		actorRef ! (Bus.CreateConnection(174))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(174))
		expectMsg(Bus.Error)

	}

	"The System-under-test (175)" should "AUTO-GEN-TEST (175)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(175))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(175))
		actorRef ! (Bus.CreateConnection(175))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(175))
		actorRef ! (Bus.Publish(175, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(175))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(175))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(175))
		expectMsg(Bus.Error)

	}

	"The System-under-test (176)" should "AUTO-GEN-TEST (176)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(176))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(176))
		actorRef ! (Bus.CreateConnection(176))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(176))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(176))
		actorRef ! (Bus.Unsubscribe(176))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(176))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(176, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(176))
		expectMsg(Bus.Error)

	}

	"The System-under-test (177)" should "AUTO-GEN-TEST (177)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(177))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(177, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(177))
		expectMsg(Bus.Error)

	}

	"The System-under-test (178)" should "AUTO-GEN-TEST (178)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(178))
		actorRef ! (Bus.Disconnect(178))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(178))
		actorRef ! (Bus.Connect(178))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(178))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(178))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(178))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(178))
		expectMsg(Bus.Error)

	}

	"The System-under-test (179)" should "AUTO-GEN-TEST (179)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(179))
		actorRef ! (Bus.Subscribe(179))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(179))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(179))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(179))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(179))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(179))
		expectMsg(Bus.Success)

	}

	"The System-under-test (180)" should "AUTO-GEN-TEST (180)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(180))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(180))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(180))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(180))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(180, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (181)" should "AUTO-GEN-TEST (181)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(181))
		actorRef ! (Bus.Connect(181))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(181))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(181))
		actorRef ! (Bus.DestroyConnection(181))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(181, "foo"))
		actorRef ! (Bus.CreateConnection(181))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(181))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(181))
		actorRef ! (Bus.DestroyConnection(181))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(181))
		actorRef ! (Bus.DestroyConnection(181))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(181))
		actorRef ! (Bus.Disconnect(181))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(181))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(181))
		actorRef ! (Bus.Connect(181))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(181, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(181))
		expectMsg(Bus.Error)

	}

	"The System-under-test (182)" should "AUTO-GEN-TEST (182)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(182))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(182))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(182, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(182))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(182))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(182))
		expectMsg(Bus.Error)

	}

	"The System-under-test (183)" should "AUTO-GEN-TEST (183)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(183))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(183))
		expectMsg(Bus.Error)

	}

	"The System-under-test (184)" should "AUTO-GEN-TEST (184)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(184))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(184, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(184))
		actorRef ! (Bus.Disconnect(184))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(184))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(184))
		actorRef ! (Bus.UnsubscribeCallback(184))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(184))
		actorRef ! (Bus.SubscribeCallback(184))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(184))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(184))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(184))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(184))
		expectMsg(Bus.Error)

	}

	"The System-under-test (185)" should "AUTO-GEN-TEST (185)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(185))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(185, "foo"))
		actorRef ! (Bus.Disconnect(185))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(185))
		actorRef ! (Bus.DestroyConnection(185))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(185))
		actorRef ! (Bus.Unsubscribe(185))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(185))
		actorRef ! (Bus.Publish(185, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(185, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(185))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(185))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(185))
		actorRef ! (Bus.CreateConnection(185))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(185))
		expectMsg(Bus.Success)

	}

	"The System-under-test (186)" should "AUTO-GEN-TEST (186)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(186))
		actorRef ! (Bus.CreateConnection(186))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(186))
		actorRef ! (Bus.Unsubscribe(186))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(186))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(186))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(186))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(186))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(186))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(186))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(186))
		expectMsg(Bus.Error)

	}

	"The System-under-test (187)" should "AUTO-GEN-TEST (187)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(187))
		actorRef ! (Bus.Unsubscribe(187))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(187))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(187))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(187))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(187))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(187))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(187))
		expectMsg(Bus.Error)

	}

	"The System-under-test (188)" should "AUTO-GEN-TEST (188)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(188))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(188, "foo"))
		actorRef ! (Bus.Unsubscribe(188))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(188))
		actorRef ! (Bus.Subscribe(188))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(188))
		actorRef ! (Bus.Publish(188, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(188))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(188, "foo"))
		actorRef ! (Bus.Subscribe(188))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(188))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(188, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(188))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(188))
		expectMsg(Bus.Success)

	}

	"The System-under-test (189)" should "AUTO-GEN-TEST (189)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(189))
		actorRef ! (Bus.UnsubscribeCallback(189))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(189))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(189))
		actorRef ! (Bus.Connect(189))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(189))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(189))
		actorRef ! (Bus.CreateConnection(189))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(189))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(189))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(189))
		actorRef ! (Bus.UnsubscribeCallback(189))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(189))
		actorRef ! (Bus.Subscribe(189))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(189))
		expectMsg(Bus.Error)

	}

	"The System-under-test (190)" should "AUTO-GEN-TEST (190)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(190))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(190))
		actorRef ! (Bus.Subscribe(190))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(190))
		actorRef ! (Bus.Disconnect(190))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(190))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(190, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(190))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(190))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(190))
		actorRef ! (Bus.Unsubscribe(190))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(190, "foo"))
		actorRef ! (Bus.DestroyConnection(190))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(190))
		actorRef ! (Bus.SubscribeCallback(190))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(190))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(190))
		actorRef ! (Bus.SubscribeCallback(190))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(190))
		actorRef ! (Bus.Publish(190, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (191)" should "AUTO-GEN-TEST (191)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(191))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(191))
		actorRef ! (Bus.Disconnect(191))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(191))
		actorRef ! (Bus.SubscribeCallback(191))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(191))
		actorRef ! (Bus.Disconnect(191))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(191))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(191))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(191, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(191))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(191))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(191))
		expectMsg(Bus.Error)

	}

	"The System-under-test (192)" should "AUTO-GEN-TEST (192)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(192))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(192, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (193)" should "AUTO-GEN-TEST (193)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(193))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(193))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(193))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(193))
		actorRef ! (Bus.SubscribeCallback(193))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(193))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(193))
		expectMsg(Bus.Error)

	}

	"The System-under-test (194)" should "AUTO-GEN-TEST (194)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(194))
		actorRef ! (Bus.UnsubscribeCallback(194))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(194))
		actorRef ! (Bus.UnsubscribeCallback(194))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(194))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(194))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(194, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(194))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(194))
		expectMsg(Bus.Error)

	}

	"The System-under-test (195)" should "AUTO-GEN-TEST (195)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(195))
		actorRef ! (Bus.UnsubscribeCallback(195))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(195))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(195))
		actorRef ! (Bus.UnsubscribeCallback(195))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(195))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(195))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(195))
		actorRef ! (Bus.CreateConnection(195))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(195))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(195))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(195))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(195))
		expectMsg(Bus.Success)

	}

	"The System-under-test (196)" should "AUTO-GEN-TEST (196)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(196))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(196, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(196))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(196))
		expectMsg(Bus.Error)

	}

	"The System-under-test (197)" should "AUTO-GEN-TEST (197)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(197))
		actorRef ! (Bus.CreateConnection(197))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(197))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(197))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(197))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(197))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(197))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(197))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(197))
		expectMsg(Bus.Success)

	}

	"The System-under-test (198)" should "AUTO-GEN-TEST (198)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(198))
		actorRef ! (Bus.Publish(198, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(198))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(198))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(198, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(198))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(198))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(198))
		expectMsg(Bus.Error)

	}

	"The System-under-test (199)" should "AUTO-GEN-TEST (199)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(199))
		actorRef ! (Bus.Subscribe(199))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(199, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(199))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(199))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(199))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(199))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(199, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(199))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(199))
		expectMsg(Bus.Error)

	}

	"The System-under-test (200)" should "AUTO-GEN-TEST (200)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(200))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(200))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(200))
		actorRef ! (Bus.UnsubscribeCallback(200))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(200, "foo"))
		actorRef ! (Bus.DestroyConnection(200))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(200))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(200))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(200))
		expectMsg(Bus.Error)

	}

	"The System-under-test (201)" should "AUTO-GEN-TEST (201)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(201))
		actorRef ! (Bus.Unsubscribe(201))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(201))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(201, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(201, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (202)" should "AUTO-GEN-TEST (202)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(202))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(202))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(202))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(202))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(202))
		expectMsg(Bus.Success)

	}

	"The System-under-test (203)" should "AUTO-GEN-TEST (203)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(203))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(203))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(203))
		actorRef ! (Bus.Disconnect(203))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(203))
		actorRef ! (Bus.Publish(203, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(203))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(203))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(203))
		actorRef ! (Bus.Subscribe(203))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(203))
		actorRef ! (Bus.DestroyConnection(203))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(203))
		actorRef ! (Bus.Connect(203))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(203, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(203))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(203))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(203))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(203))
		expectMsg(Bus.Error)

	}

	"The System-under-test (204)" should "AUTO-GEN-TEST (204)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(204))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(204))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(204, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(204))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(204))
		expectMsg(Bus.Error)

	}

	"The System-under-test (205)" should "AUTO-GEN-TEST (205)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(205))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(205))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(205))
		actorRef ! (Bus.Connect(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(205))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(205))
		actorRef ! (Bus.Connect(205))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(205))
		actorRef ! (Bus.Unsubscribe(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(205))
		actorRef ! (Bus.SubscribeCallback(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(205))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(205))
		actorRef ! (Bus.CreateConnection(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(205, "foo"))
		actorRef ! (Bus.CreateConnection(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(205, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(205))
		actorRef ! (Bus.DestroyConnection(205))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(205))
		actorRef ! (Bus.Connect(205))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(205))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(205))
		expectMsg(Bus.Error)

	}

	"The System-under-test (206)" should "AUTO-GEN-TEST (206)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(206))
		actorRef ! (Bus.DestroyConnection(206))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(206))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(206))
		actorRef ! (Bus.SubscribeCallback(206))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(206))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(206))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(206))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(206, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (207)" should "AUTO-GEN-TEST (207)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(207))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(207))
		actorRef ! (Bus.UnsubscribeCallback(207))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(207))
		actorRef ! (Bus.Publish(207, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(207))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(207))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(207))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(207))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(207))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(207, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (208)" should "AUTO-GEN-TEST (208)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(208))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(208))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(208))
		actorRef ! (Bus.Subscribe(208))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(208))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(208))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(208))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(208))
		expectMsg(Bus.Success)

	}

	"The System-under-test (209)" should "AUTO-GEN-TEST (209)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(209))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(209))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(209, "foo"))
		actorRef ! (Bus.Unsubscribe(209))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(209))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(209))
		actorRef ! (Bus.Disconnect(209))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (210)" should "AUTO-GEN-TEST (210)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(210))
		actorRef ! (Bus.CreateConnection(210))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(210))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(210, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(210))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(210))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(210, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(210))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(210, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (211)" should "AUTO-GEN-TEST (211)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(211))
		actorRef ! (Bus.Connect(211))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (212)" should "AUTO-GEN-TEST (212)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(212))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(212))
		actorRef ! (Bus.Connect(212))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(212))
		actorRef ! (Bus.Subscribe(212))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(212))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(212, "foo"))
		actorRef ! (Bus.Unsubscribe(212))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(212))
		actorRef ! (Bus.Connect(212))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(212))
		actorRef ! (Bus.Subscribe(212))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(212))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(212))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(212))
		actorRef ! (Bus.SubscribeCallback(212))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(212))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(212, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(212))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(212))
		expectMsg(Bus.Error)

	}

	"The System-under-test (213)" should "AUTO-GEN-TEST (213)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(213))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(213))
		actorRef ! (Bus.CreateConnection(213))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(213))
		actorRef ! (Bus.UnsubscribeCallback(213))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(213))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(213))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(213))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(213))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(213))
		expectMsg(Bus.Error)

	}

	"The System-under-test (214)" should "AUTO-GEN-TEST (214)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(214))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(214))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(214))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(214))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(214))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(214))
		expectMsg(Bus.Error)

	}

	"The System-under-test (215)" should "AUTO-GEN-TEST (215)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(215))
		actorRef ! (Bus.SubscribeCallback(215))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(215, "foo"))
		actorRef ! (Bus.Subscribe(215))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(215, "foo"))
		actorRef ! (Bus.Connect(215))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(215))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(215))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(215))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(215))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(215))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(215))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(215))
		expectMsg(Bus.Error)

	}

	"The System-under-test (216)" should "AUTO-GEN-TEST (216)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(216, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(216))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(216, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(216))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(216))
		expectMsg(Bus.Error)

	}

	"The System-under-test (217)" should "AUTO-GEN-TEST (217)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(217))
		actorRef ! (Bus.Connect(217))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(217))
		actorRef ! (Bus.Connect(217))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(217))
		expectMsg(Bus.Error)

	}

	"The System-under-test (218)" should "AUTO-GEN-TEST (218)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(218))
		actorRef ! (Bus.Unsubscribe(218))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(218))
		actorRef ! (Bus.Subscribe(218))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(218))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(218))
		actorRef ! (Bus.Subscribe(218))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(218))
		actorRef ! (Bus.Unsubscribe(218))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(218))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(218))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(218))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(218))
		expectMsg(Bus.Success)

	}

	"The System-under-test (219)" should "AUTO-GEN-TEST (219)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(219))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(219))
		actorRef ! (Bus.DestroyConnection(219))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(219))
		actorRef ! (Bus.DestroyConnection(219))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(219, "foo"))
		actorRef ! (Bus.Disconnect(219))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(219))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(219))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(219))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(219))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(219))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(219))
		expectMsg(Bus.Error)

	}

	"The System-under-test (220)" should "AUTO-GEN-TEST (220)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(220))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(220))
		actorRef ! (Bus.Unsubscribe(220))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(220))
		actorRef ! (Bus.DestroyConnection(220))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(220))
		actorRef ! (Bus.Connect(220))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(220))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(220))
		actorRef ! (Bus.Unsubscribe(220))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(220, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(220))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(220))
		expectMsg(Bus.Success)

	}

	"The System-under-test (221)" should "AUTO-GEN-TEST (221)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(221))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(221))
		actorRef ! (Bus.Connect(221))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(221))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(221))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(221, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (222)" should "AUTO-GEN-TEST (222)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(222))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(222))
		actorRef ! (Bus.SubscribeCallback(222))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(222, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (223)" should "AUTO-GEN-TEST (223)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(223))
		actorRef ! (Bus.Subscribe(223))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(223))
		actorRef ! (Bus.Unsubscribe(223))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(223))
		actorRef ! (Bus.Unsubscribe(223))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(223))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(223))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(223))
		actorRef ! (Bus.UnsubscribeCallback(223))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(223))
		expectMsg(Bus.Error)

	}

	"The System-under-test (224)" should "AUTO-GEN-TEST (224)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(224))
		actorRef ! (Bus.UnsubscribeCallback(224))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(224))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(224, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(224))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(224))
		actorRef ! (Bus.DestroyConnection(224))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(224))
		actorRef ! (Bus.Disconnect(224))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(224))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(224))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(224))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(224))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(224))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(224))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(224))
		expectMsg(Bus.Success)

	}

	"The System-under-test (225)" should "AUTO-GEN-TEST (225)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(225))
		actorRef ! (Bus.Disconnect(225))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(225))
		actorRef ! (Bus.Publish(225, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(225, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(225))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(225))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(225, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (226)" should "AUTO-GEN-TEST (226)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(226))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(226))
		actorRef ! (Bus.UnsubscribeCallback(226))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(226))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(226, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(226, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(226))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(226))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(226, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(226))
		expectMsg(Bus.Error)

	}

	"The System-under-test (227)" should "AUTO-GEN-TEST (227)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(227))
		actorRef ! (Bus.Disconnect(227))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(227))
		actorRef ! (Bus.UnsubscribeCallback(227))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(227))
		actorRef ! (Bus.DestroyConnection(227))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(227))
		actorRef ! (Bus.Unsubscribe(227))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(227))
		actorRef ! (Bus.Connect(227))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(227, "foo"))
		actorRef ! (Bus.Disconnect(227))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(227))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(227))
		expectMsg(Bus.Error)

	}

	"The System-under-test (228)" should "AUTO-GEN-TEST (228)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(228))
		actorRef ! (Bus.Disconnect(228))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(228, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(228))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(228))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(228, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (229)" should "AUTO-GEN-TEST (229)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(229))
		actorRef ! (Bus.Subscribe(229))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(229))
		actorRef ! (Bus.Connect(229))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(229))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(229))
		expectMsg(Bus.Error)

	}

	"The System-under-test (230)" should "AUTO-GEN-TEST (230)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(230))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(230))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(230))
		actorRef ! (Bus.Connect(230))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(230))
		actorRef ! (Bus.Subscribe(230))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(230))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(230, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(230))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(230, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(230))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(230))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(230, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(230))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(230))
		expectMsg(Bus.Success)

	}

	"The System-under-test (231)" should "AUTO-GEN-TEST (231)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(231))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(231))
		actorRef ! (Bus.Disconnect(231))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(231))
		actorRef ! (Bus.Disconnect(231))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(231))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(231))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(231))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(231))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(231))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(231))
		expectMsg(Bus.Error)

	}

	"The System-under-test (232)" should "AUTO-GEN-TEST (232)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(232))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(232))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(232))
		expectMsg(Bus.Error)

	}

	"The System-under-test (233)" should "AUTO-GEN-TEST (233)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(233))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(233, "foo"))
		actorRef ! (Bus.Disconnect(233))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(233))
		actorRef ! (Bus.Disconnect(233))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(233))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(233))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(233, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (234)" should "AUTO-GEN-TEST (234)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(234))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(234))
		actorRef ! (Bus.Disconnect(234))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(234))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(234))
		actorRef ! (Bus.Connect(234))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(234))
		actorRef ! (Bus.Publish(234, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(234, "foo"))
		actorRef ! (Bus.SubscribeCallback(234))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(234))
		actorRef ! (Bus.UnsubscribeCallback(234))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(234))
		actorRef ! (Bus.Publish(234, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(234))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(234))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(234))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(234))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(234))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(234))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(234))
		expectMsg(Bus.Success)

	}

	"The System-under-test (235)" should "AUTO-GEN-TEST (235)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(235))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(235, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(235, "foo"))
		actorRef ! (Bus.Connect(235))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(235))
		actorRef ! (Bus.Connect(235))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(235))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(235))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(235))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(235))
		expectMsg(Bus.Error)

	}

	"The System-under-test (236)" should "AUTO-GEN-TEST (236)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(236))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(236))
		actorRef ! (Bus.SubscribeCallback(236))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(236))
		actorRef ! (Bus.DestroyConnection(236))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (237)" should "AUTO-GEN-TEST (237)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(237))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(237))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(237))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(237))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(237))
		actorRef ! (Bus.Connect(237))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(237, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(237))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(237))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(237))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(237))
		expectMsg(Bus.Error)

	}

	"The System-under-test (238)" should "AUTO-GEN-TEST (238)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(238))
		actorRef ! (Bus.Publish(238, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(238))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(238))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(238))
		actorRef ! (Bus.DestroyConnection(238))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(238, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(238))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(238))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(238))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(238, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(238))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(238))
		expectMsg(Bus.Error)

	}

	"The System-under-test (239)" should "AUTO-GEN-TEST (239)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(239))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(239))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(239))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(239))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(239))
		actorRef ! (Bus.CreateConnection(239))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (240)" should "AUTO-GEN-TEST (240)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(240))
		actorRef ! (Bus.SubscribeCallback(240))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(240))
		actorRef ! (Bus.Connect(240))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(240))
		actorRef ! (Bus.Unsubscribe(240))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(240))
		actorRef ! (Bus.Publish(240, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(240, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (241)" should "AUTO-GEN-TEST (241)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(241))
		actorRef ! (Bus.Publish(241, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(241))
		actorRef ! (Bus.Connect(241))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(241))
		actorRef ! (Bus.Connect(241))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(241))
		actorRef ! (Bus.Unsubscribe(241))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(241))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(241))
		actorRef ! (Bus.DestroyConnection(241))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(241))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(241))
		actorRef ! (Bus.Connect(241))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(241))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(241))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(241))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(241))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(241))
		expectMsg(Bus.Error)

	}

	"The System-under-test (242)" should "AUTO-GEN-TEST (242)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(242))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(242))
		actorRef ! (Bus.UnsubscribeCallback(242))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(242))
		actorRef ! (Bus.Disconnect(242))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(242))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(242))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(242))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(242, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (243)" should "AUTO-GEN-TEST (243)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(243))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(243))
		actorRef ! (Bus.Unsubscribe(243))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(243))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(243))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(243))
		actorRef ! (Bus.Disconnect(243))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(243, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(243, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(243))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(243))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(243, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (244)" should "AUTO-GEN-TEST (244)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(244))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(244))
		actorRef ! (Bus.Connect(244))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(244))
		actorRef ! (Bus.SubscribeCallback(244))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(244, "foo"))
		actorRef ! (Bus.SubscribeCallback(244))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(244))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(244))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(244))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(244))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(244, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(244))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(244, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(244, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(244))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(244))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(244))
		expectMsg(Bus.Error)

	}

	"The System-under-test (245)" should "AUTO-GEN-TEST (245)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(245))
		actorRef ! (Bus.UnsubscribeCallback(245))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(245))
		actorRef ! (Bus.DestroyConnection(245))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(245))
		actorRef ! (Bus.DestroyConnection(245))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(245))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(245, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(245))
		actorRef ! (Bus.Disconnect(245))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(245))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(245))
		expectMsg(Bus.Error)

	}

	"The System-under-test (246)" should "AUTO-GEN-TEST (246)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(246))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(246))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(246))
		actorRef ! (Bus.Publish(246, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(246))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(246))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(246))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(246))
		expectMsg(Bus.Error)

	}

	"The System-under-test (247)" should "AUTO-GEN-TEST (247)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(247))
		actorRef ! (Bus.DestroyConnection(247))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(247))
		actorRef ! (Bus.SubscribeCallback(247))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(247))
		expectMsg(Bus.Success)

	}

	"The System-under-test (248)" should "AUTO-GEN-TEST (248)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(248))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(248))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(248))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(248))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(248))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(248, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(248))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(248))
		expectMsg(Bus.Success)

	}

	"The System-under-test (249)" should "AUTO-GEN-TEST (249)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(249))
		actorRef ! (Bus.Publish(249, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(249))
		actorRef ! (Bus.Publish(249, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(249))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(249))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(249, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(249))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(249))
		expectMsg(Bus.Error)

	}

	"The System-under-test (250)" should "AUTO-GEN-TEST (250)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(250))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(250, "foo"))
		actorRef ! (Bus.Connect(250))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(250, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (251)" should "AUTO-GEN-TEST (251)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(251))
		actorRef ! (Bus.Publish(251, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(251))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(251))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(251))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(251))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(251, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(251))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(251))
		expectMsg(Bus.Success)

	}

	"The System-under-test (252)" should "AUTO-GEN-TEST (252)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(252))
		actorRef ! (Bus.UnsubscribeCallback(252))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(252))
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(252))
		actorRef ! (Bus.SubscribeCallback(252))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(252))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(252, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(252, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(252))
		expectMsg(Bus.Error)

	}

	"The System-under-test (253)" should "AUTO-GEN-TEST (253)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(253))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(253))
		actorRef ! (Bus.UnsubscribeCallback(253))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(253))
		actorRef ! (Bus.Disconnect(253))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(253))
		actorRef ! (Bus.UnsubscribeCallback(253))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(253))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(253))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(253, "foo"))
		actorRef ! (Bus.Publish(253, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(253, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(253, "foo"))
		actorRef ! (Bus.Subscribe(253))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(253))
		expectMsg(Bus.Error)

	}

	"The System-under-test (254)" should "AUTO-GEN-TEST (254)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(254))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(254))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(254))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(254))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(254))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(254))
		actorRef ! (Bus.Disconnect(254))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(254, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(254))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(254))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(254))
		expectMsg(Bus.Error)

	}

	"The System-under-test (255)" should "AUTO-GEN-TEST (255)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(255))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(255))
		actorRef ! (Bus.Publish(255, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(255))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(255))
		actorRef ! (Bus.UnsubscribeCallback(255))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(255))
		actorRef ! (Bus.Unsubscribe(255))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(255))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(255))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(255))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(255))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(255))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(255))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(255))
		expectMsg(Bus.Error)

	}

	"The System-under-test (256)" should "AUTO-GEN-TEST (256)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(256))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(256))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(256, "foo"))
		actorRef ! (Bus.CreateConnection(256))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(256))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(256))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(256))
		expectMsg(Bus.Error)

	}

	"The System-under-test (257)" should "AUTO-GEN-TEST (257)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(257))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(257))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(257))
		actorRef ! (Bus.Unsubscribe(257))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(257))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(257))
		actorRef ! (Bus.Publish(257, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(257))
		actorRef ! (Bus.DestroyConnection(257))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(257))
		actorRef ! (Bus.UnsubscribeCallback(257))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(257))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(257))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(257))
		actorRef ! (Bus.Unsubscribe(257))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(257))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(257))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(257))
		actorRef ! (Bus.Disconnect(257))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(257))
		expectMsg(Bus.Error)

	}

	"The System-under-test (258)" should "AUTO-GEN-TEST (258)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(258))
		actorRef ! (Bus.UnsubscribeCallback(258))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(258))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(258))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(258))
		actorRef ! (Bus.Disconnect(258))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(258))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(258))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(258))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(258))
		expectMsg(Bus.Error)

	}

	"The System-under-test (259)" should "AUTO-GEN-TEST (259)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(259))
		actorRef ! (Bus.SubscribeCallback(259))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(259, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(259))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(259, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(259))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(259))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(259))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(259))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(259))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(259))
		expectMsg(Bus.Error)

	}

	"The System-under-test (260)" should "AUTO-GEN-TEST (260)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(260))
		actorRef ! (Bus.SubscribeCallback(260))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(260))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(260, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(260))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(260))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(260))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(260, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (261)" should "AUTO-GEN-TEST (261)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(261))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(261))
		actorRef ! (Bus.Unsubscribe(261))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(261))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(261))
		expectMsg(Bus.Error)

	}

	"The System-under-test (262)" should "AUTO-GEN-TEST (262)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(262))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(262))
		actorRef ! (Bus.Connect(262))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(262, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(262))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(262))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(262))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(262))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(262))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(262))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(262))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(262))
		expectMsg(Bus.Error)

	}

	"The System-under-test (263)" should "AUTO-GEN-TEST (263)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(263))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(263))
		actorRef ! (Bus.CreateConnection(263))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(263))
		actorRef ! (Bus.Connect(263))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(263, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(263))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(263))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(263))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(263))
		actorRef ! (Bus.Unsubscribe(263))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(263))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(263))
		actorRef ! (Bus.Connect(263))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(263))
		actorRef ! (Bus.Publish(263, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(263))
		actorRef ! (Bus.DestroyConnection(263))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(263, "foo"))
		actorRef ! (Bus.Disconnect(263))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (264)" should "AUTO-GEN-TEST (264)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(264))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(264))
		actorRef ! (Bus.SubscribeCallback(264))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(264))
		actorRef ! (Bus.SubscribeCallback(264))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(264))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(264))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(264))
		expectMsg(Bus.Error)

	}

	"The System-under-test (265)" should "AUTO-GEN-TEST (265)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(265))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(265))
		actorRef ! (Bus.UnsubscribeCallback(265))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(265))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(265, "foo"))
		actorRef ! (Bus.Subscribe(265))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(265))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(265, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(265))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(265))
		expectMsg(Bus.Error)

	}

	"The System-under-test (266)" should "AUTO-GEN-TEST (266)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(266))
		actorRef ! (Bus.Disconnect(266))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(266))
		actorRef ! (Bus.Subscribe(266))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(266))
		actorRef ! (Bus.DestroyConnection(266))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(266, "foo"))
		actorRef ! (Bus.SubscribeCallback(266))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(266))
		actorRef ! (Bus.UnsubscribeCallback(266))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(266, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(266, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(266))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(266))
		expectMsg(Bus.Error)

	}

	"The System-under-test (267)" should "AUTO-GEN-TEST (267)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(267))
		actorRef ! (Bus.Subscribe(267))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(267))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(267))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(267))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(267))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(267))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(267))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(267, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(267))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(267))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(267))
		expectMsg(Bus.Error)

	}

	"The System-under-test (268)" should "AUTO-GEN-TEST (268)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(268))
		actorRef ! (Bus.Unsubscribe(268))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(268))
		actorRef ! (Bus.CreateConnection(268))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(268, "foo"))
		actorRef ! (Bus.SubscribeCallback(268))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(268, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(268))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(268))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(268))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(268))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(268, "foo"))
		actorRef ! (Bus.SubscribeCallback(268))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(268))
		actorRef ! (Bus.CreateConnection(268))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(268))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(268))
		actorRef ! (Bus.CreateConnection(268))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(268))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(268))
		expectMsg(Bus.Error)

	}

	"The System-under-test (269)" should "AUTO-GEN-TEST (269)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(269))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(269, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(269))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(269))
		expectMsg(Bus.Success)

	}

	"The System-under-test (270)" should "AUTO-GEN-TEST (270)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(270))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(270))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(270))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(270))
		actorRef ! (Bus.Subscribe(270))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(270))
		actorRef ! (Bus.Publish(270, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(270))
		actorRef ! (Bus.DestroyConnection(270))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(270))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(270, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(270, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(270))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(270))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(270))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(270))
		expectMsg(Bus.Error)

	}

	"The System-under-test (271)" should "AUTO-GEN-TEST (271)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(271))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(271))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(271))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(271))
		expectMsg(Bus.Error)

	}

	"The System-under-test (272)" should "AUTO-GEN-TEST (272)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(272))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(272))
		actorRef ! (Bus.CreateConnection(272))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(272, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(272, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(272))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(272))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(272, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(272))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(272, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (273)" should "AUTO-GEN-TEST (273)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(273))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(273))
		actorRef ! (Bus.Publish(273, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(273))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(273))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(273, "foo"))
		actorRef ! (Bus.Disconnect(273))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(273))
		actorRef ! (Bus.Disconnect(273))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(273))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(273))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(273))
		expectMsg(Bus.Error)

	}

	"The System-under-test (274)" should "AUTO-GEN-TEST (274)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(274))
		actorRef ! (Bus.SubscribeCallback(274))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(274))
		expectMsg(Bus.Error)

	}

	"The System-under-test (275)" should "AUTO-GEN-TEST (275)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(275))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(275))
		actorRef ! (Bus.Publish(275, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(275, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(275))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(275))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(275))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(275, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(275))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(275))
		expectMsg(Bus.Success)

	}

	"The System-under-test (276)" should "AUTO-GEN-TEST (276)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(276))
		actorRef ! (Bus.CreateConnection(276))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(276))
		actorRef ! (Bus.SubscribeCallback(276))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(276))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(276))
		actorRef ! (Bus.Publish(276, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(276))
		actorRef ! (Bus.UnsubscribeCallback(276))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(276))
		actorRef ! (Bus.DestroyConnection(276))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(276))
		actorRef ! (Bus.Connect(276))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(276, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(276))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(276))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(276))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(276))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(276))
		expectMsg(Bus.Error)

	}

	"The System-under-test (277)" should "AUTO-GEN-TEST (277)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(277))
		actorRef ! (Bus.UnsubscribeCallback(277))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(277, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(277))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(277))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(277))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(277))
		expectMsg(Bus.Error)

	}

	"The System-under-test (278)" should "AUTO-GEN-TEST (278)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(278))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(278))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(278))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(278))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(278, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(278))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(278))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(278))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(278))
		expectMsg(Bus.Success)

	}

	"The System-under-test (279)" should "AUTO-GEN-TEST (279)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(279))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(279))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(279))
		actorRef ! (Bus.Unsubscribe(279))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(279))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(279))
		actorRef ! (Bus.Unsubscribe(279))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(279))
		actorRef ! (Bus.Subscribe(279))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (280)" should "AUTO-GEN-TEST (280)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(280))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(280))
		actorRef ! (Bus.Unsubscribe(280))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(280, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(280))
		actorRef ! (Bus.Unsubscribe(280))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(280))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(280))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(280))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(280))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(280))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(280))
		expectMsg(Bus.Error)

	}

	"The System-under-test (281)" should "AUTO-GEN-TEST (281)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(281))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(281))
		actorRef ! (Bus.Unsubscribe(281))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(281))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(281))
		actorRef ! (Bus.Subscribe(281))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(281))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(281))
		actorRef ! (Bus.DestroyConnection(281))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(281))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(281))
		actorRef ! (Bus.CreateConnection(281))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(281))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(281))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(281))
		actorRef ! (Bus.UnsubscribeCallback(281))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(281))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(281))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(281, "foo"))
		actorRef ! (Bus.Connect(281))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (282)" should "AUTO-GEN-TEST (282)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(282))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(282))
		actorRef ! (Bus.CreateConnection(282))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(282))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(282))
		actorRef ! (Bus.UnsubscribeCallback(282))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(282))
		actorRef ! (Bus.Publish(282, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(282))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(282))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(282))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(282))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(282))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(282))
		expectMsg(Bus.Error)

	}

	"The System-under-test (283)" should "AUTO-GEN-TEST (283)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(283))
		actorRef ! (Bus.Disconnect(283))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(283))
		actorRef ! (Bus.DestroyConnection(283))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(283))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(283))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(283))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(283))
		expectMsg(Bus.Error)

	}

	"The System-under-test (284)" should "AUTO-GEN-TEST (284)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(284))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(284, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(284))
		actorRef ! (Bus.Disconnect(284))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(284))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(284))
		actorRef ! (Bus.SubscribeCallback(284))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(284))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(284, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(284))
		expectMsg(Bus.Success)

	}

	"The System-under-test (285)" should "AUTO-GEN-TEST (285)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(285))
		actorRef ! (Bus.CreateConnection(285))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(285, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(285))
		actorRef ! (Bus.DestroyConnection(285))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(285))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(285, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(285))
		actorRef ! (Bus.DestroyConnection(285))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(285))
		actorRef ! (Bus.Unsubscribe(285))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(285))
		actorRef ! (Bus.SubscribeCallback(285))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(285))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(285))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(285))
		actorRef ! (Bus.Unsubscribe(285))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(285))
		actorRef ! (Bus.Publish(285, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(285))
		expectMsg(Bus.Error)

	}

	"The System-under-test (286)" should "AUTO-GEN-TEST (286)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(286))
		actorRef ! (Bus.DestroyConnection(286))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(286))
		actorRef ! (Bus.Disconnect(286))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(286))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(286))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(286, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(286))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(286))
		expectMsg(Bus.Error)

	}

	"The System-under-test (287)" should "AUTO-GEN-TEST (287)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(287))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(287, "foo"))
		actorRef ! (Bus.Unsubscribe(287))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(287, "foo"))
		actorRef ! (Bus.Publish(287, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(287))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(287))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(287))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(287))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(287, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (288)" should "AUTO-GEN-TEST (288)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(288))
		actorRef ! (Bus.Connect(288))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(288))
		expectMsg(Bus.Success)

	}

	"The System-under-test (289)" should "AUTO-GEN-TEST (289)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(289))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(289))
		actorRef ! (Bus.CreateConnection(289))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(289, "foo"))
		actorRef ! (Bus.Disconnect(289))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(289))
		actorRef ! (Bus.UnsubscribeCallback(289))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(289))
		actorRef ! (Bus.SubscribeCallback(289))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(289))
		actorRef ! (Bus.Subscribe(289))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(289))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(289))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(289, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (290)" should "AUTO-GEN-TEST (290)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(290))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(290))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(290, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(290))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(290))
		expectMsg(Bus.Error)

	}

	"The System-under-test (291)" should "AUTO-GEN-TEST (291)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(291))
		actorRef ! (Bus.Subscribe(291))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(291))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(291))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(291))
		actorRef ! (Bus.CreateConnection(291))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(291))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(291))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(291))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(291))
		expectMsg(Bus.Success)

	}

	"The System-under-test (292)" should "AUTO-GEN-TEST (292)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(292))
		actorRef ! (Bus.Unsubscribe(292))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(292))
		actorRef ! (Bus.CreateConnection(292))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(292))
		actorRef ! (Bus.CreateConnection(292))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(292))
		actorRef ! (Bus.DestroyConnection(292))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(292))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(292, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(292))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(292))
		expectMsg(Bus.Error)

	}

	"The System-under-test (293)" should "AUTO-GEN-TEST (293)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(293))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(293))
		actorRef ! (Bus.Unsubscribe(293))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(293))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(293))
		expectMsg(Bus.Success)

	}

	"The System-under-test (294)" should "AUTO-GEN-TEST (294)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(294))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(294))
		actorRef ! (Bus.DestroyConnection(294))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(294))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(294))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(294))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(294))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(294))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(294))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(294))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(294))
		expectMsg(Bus.Error)

	}

	"The System-under-test (295)" should "AUTO-GEN-TEST (295)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(295))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(295))
		actorRef ! (Bus.Connect(295))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(295))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(295, "foo"))
		actorRef ! (Bus.Subscribe(295))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(295))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(295))
		expectMsg(Bus.Error)

	}

	"The System-under-test (296)" should "AUTO-GEN-TEST (296)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(296))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(296))
		actorRef ! (Bus.Connect(296))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(296))
		actorRef ! (Bus.Publish(296, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (297)" should "AUTO-GEN-TEST (297)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(297))
		actorRef ! (Bus.DestroyConnection(297))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(297))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(297))
		expectMsg(Bus.Error)

	}

	"The System-under-test (298)" should "AUTO-GEN-TEST (298)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(298))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(298, "foo"))
		actorRef ! (Bus.Disconnect(298))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(298))
		actorRef ! (Bus.Disconnect(298))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(298))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(298))
		actorRef ! (Bus.Disconnect(298))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(298))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(298))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(298))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(298))
		actorRef ! (Bus.Unsubscribe(298))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(298))
		actorRef ! (Bus.Unsubscribe(298))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(298))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(298, "foo"))
		actorRef ! (Bus.SubscribeCallback(298))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(298))
		expectMsg(Bus.Error)

	}

	"The System-under-test (299)" should "AUTO-GEN-TEST (299)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(299))
		actorRef ! (Bus.Disconnect(299))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(299))
		actorRef ! (Bus.Publish(299, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(299))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(299))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(299))
		expectMsg(Bus.Success)

	}

	"The System-under-test (300)" should "AUTO-GEN-TEST (300)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(300))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(300))
		actorRef ! (Bus.Subscribe(300))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(300))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(300))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(300))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(300))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(300))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(300))
		expectMsg(Bus.Error)

	}

	"The System-under-test (301)" should "AUTO-GEN-TEST (301)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(301))
		expectMsg(Bus.Success)

	}

	"The System-under-test (302)" should "AUTO-GEN-TEST (302)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(302))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(302))
		actorRef ! (Bus.Connect(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(302))
		actorRef ! (Bus.Unsubscribe(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(302))
		actorRef ! (Bus.Disconnect(302))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(302))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(302))
		actorRef ! (Bus.SubscribeCallback(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(302))
		actorRef ! (Bus.UnsubscribeCallback(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(302))
		actorRef ! (Bus.SubscribeCallback(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(302, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(302))
		actorRef ! (Bus.DestroyConnection(302))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(302))
		actorRef ! (Bus.Disconnect(302))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(302))
		expectMsg(Bus.Error)

	}

	"The System-under-test (303)" should "AUTO-GEN-TEST (303)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(303))
		actorRef ! (Bus.Publish(303, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(303))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(303))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(303))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(303))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(303))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(303))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(303))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(303))
		expectMsg(Bus.Error)

	}

	"The System-under-test (304)" should "AUTO-GEN-TEST (304)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(304))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(304))
		actorRef ! (Bus.SubscribeCallback(304))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(304))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(304))
		actorRef ! (Bus.Disconnect(304))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(304))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(304))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(304, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(304, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(304))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(304))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(304))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(304))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(304))
		expectMsg(Bus.Error)

	}

	"The System-under-test (305)" should "AUTO-GEN-TEST (305)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(305))
		actorRef ! (Bus.DestroyConnection(305))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(305))
		actorRef ! (Bus.Unsubscribe(305))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(305))
		actorRef ! (Bus.Connect(305))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(305))
		actorRef ! (Bus.Publish(305, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(305))
		actorRef ! (Bus.Subscribe(305))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(305))
		actorRef ! (Bus.Publish(305, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(305))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(305))
		actorRef ! (Bus.DestroyConnection(305))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (306)" should "AUTO-GEN-TEST (306)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(306))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(306, "foo"))
		actorRef ! (Bus.CreateConnection(306))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(306))
		actorRef ! (Bus.Subscribe(306))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(306))
		actorRef ! (Bus.Subscribe(306))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(306))
		actorRef ! (Bus.Disconnect(306))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(306))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(306))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(306))
		expectMsg(Bus.Error)

	}

	"The System-under-test (307)" should "AUTO-GEN-TEST (307)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(307))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(307))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(307))
		actorRef ! (Bus.Disconnect(307))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(307))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(307))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(307, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(307))
		actorRef ! (Bus.UnsubscribeCallback(307))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(307))
		expectMsg(Bus.Success)

	}

	"The System-under-test (308)" should "AUTO-GEN-TEST (308)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(308))
		actorRef ! (Bus.SubscribeCallback(308))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(308))
		actorRef ! (Bus.DestroyConnection(308))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(308, "foo"))
		actorRef ! (Bus.DestroyConnection(308))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(308))
		actorRef ! (Bus.Publish(308, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(308, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(308))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(308))
		actorRef ! (Bus.Publish(308, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(308))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(308))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(308))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(308))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(308))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(308))
		expectMsg(Bus.Success)

	}

	"The System-under-test (309)" should "AUTO-GEN-TEST (309)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(309))
		actorRef ! (Bus.Publish(309, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(309))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(309, "foo"))
		actorRef ! (Bus.DestroyConnection(309))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(309))
		actorRef ! (Bus.DestroyConnection(309))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(309))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(309))
		expectMsg(Bus.Error)

	}

	"The System-under-test (310)" should "AUTO-GEN-TEST (310)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(310))
		actorRef ! (Bus.UnsubscribeCallback(310))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(310))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(310))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(310, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(310))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(310))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(310))
		expectMsg(Bus.Error)

	}

	"The System-under-test (311)" should "AUTO-GEN-TEST (311)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(311))
		actorRef ! (Bus.Connect(311))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(311))
		actorRef ! (Bus.DestroyConnection(311))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(311))
		actorRef ! (Bus.Subscribe(311))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(311))
		actorRef ! (Bus.Connect(311))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(311))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(311))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(311))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(311, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(311))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(311))
		expectMsg(Bus.Error)

	}

	"The System-under-test (312)" should "AUTO-GEN-TEST (312)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(312))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(312))
		actorRef ! (Bus.DestroyConnection(312))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(312, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(312))
		actorRef ! (Bus.SubscribeCallback(312))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(312))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(312))
		expectMsg(Bus.Error)

	}

	"The System-under-test (313)" should "AUTO-GEN-TEST (313)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(313))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(313))
		actorRef ! (Bus.Subscribe(313))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(313))
		actorRef ! (Bus.CreateConnection(313))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(313, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(313))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(313))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(313))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(313, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (314)" should "AUTO-GEN-TEST (314)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(314))
		actorRef ! (Bus.Connect(314))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (315)" should "AUTO-GEN-TEST (315)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(315))
		actorRef ! (Bus.Disconnect(315))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(315))
		actorRef ! (Bus.SubscribeCallback(315))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(315))
		actorRef ! (Bus.SubscribeCallback(315))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(315))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(315))
		actorRef ! (Bus.Unsubscribe(315))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(315))
		actorRef ! (Bus.SubscribeCallback(315))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(315))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(315))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(315))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(315, "foo"))
		actorRef ! (Bus.Connect(315))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(315))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(315))
		expectMsg(Bus.Error)

	}

	"The System-under-test (316)" should "AUTO-GEN-TEST (316)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(316, "foo"))
		actorRef ! (Bus.Publish(316, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(316))
		actorRef ! (Bus.Subscribe(316))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(316))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(316))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(316))
		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(316, "foo"))
		actorRef ! (Bus.Unsubscribe(316))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(316))
		actorRef ! (Bus.Unsubscribe(316))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(316))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(316))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(316))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(316))
		expectMsg(Bus.Error)

	}

	"The System-under-test (317)" should "AUTO-GEN-TEST (317)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(317))
		actorRef ! (Bus.Publish(317, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(317))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(317))
		actorRef ! (Bus.Connect(317))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(317))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(317))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(317, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (318)" should "AUTO-GEN-TEST (318)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(318))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(318))
		actorRef ! (Bus.Connect(318))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(318))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(318, "foo"))
		actorRef ! (Bus.Publish(318, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (319)" should "AUTO-GEN-TEST (319)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(319))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(319))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(319))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(319))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(319))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(319))
		actorRef ! (Bus.SubscribeCallback(319))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(319, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(319))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(319))
		expectMsg(Bus.Error)

	}

	"The System-under-test (320)" should "AUTO-GEN-TEST (320)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(320))
		actorRef ! (Bus.Unsubscribe(320))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(320))
		actorRef ! (Bus.UnsubscribeCallback(320))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(320))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(320))
		actorRef ! (Bus.SubscribeCallback(320))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(320))
		actorRef ! (Bus.DestroyConnection(320))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(320))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(320, "foo"))
		actorRef ! (Bus.Subscribe(320))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(320))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(320))
		expectMsg(Bus.Error)

	}

	"The System-under-test (321)" should "AUTO-GEN-TEST (321)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(321))
		actorRef ! (Bus.CreateConnection(321))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(321))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(321))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(321))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(321))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(321))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(321))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(321))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(321))
		expectMsg(Bus.Error)

	}

	"The System-under-test (322)" should "AUTO-GEN-TEST (322)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(322))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(322))
		actorRef ! (Bus.DestroyConnection(322))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(322))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(322))
		actorRef ! (Bus.CreateConnection(322))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(322, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(322))
		expectMsg(Bus.Error)

	}

	"The System-under-test (323)" should "AUTO-GEN-TEST (323)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(323))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(323))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(323))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(323, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(323, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(323, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(323))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(323))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(323))
		expectMsg(Bus.Error)

	}

	"The System-under-test (324)" should "AUTO-GEN-TEST (324)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(324))
		actorRef ! (Bus.Unsubscribe(324))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(324))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(324, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(324))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(324))
		actorRef ! (Bus.Unsubscribe(324))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(324))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(324))
		actorRef ! (Bus.CreateConnection(324))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(324))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(324))
		actorRef ! (Bus.Connect(324))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(324))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(324))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(324))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(324))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(324))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(324))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(324))
		expectMsg(Bus.Error)

	}

	"The System-under-test (325)" should "AUTO-GEN-TEST (325)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(325))
		actorRef ! (Bus.SubscribeCallback(325))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(325))
		actorRef ! (Bus.CreateConnection(325))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(325))
		actorRef ! (Bus.DestroyConnection(325))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(325))
		actorRef ! (Bus.CreateConnection(325))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(325))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(325))
		actorRef ! (Bus.SubscribeCallback(325))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(325))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(325, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(325))
		actorRef ! (Bus.Unsubscribe(325))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(325))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(325, "foo"))
		actorRef ! (Bus.Unsubscribe(325))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(325))
		expectMsg(Bus.Error)

	}

	"The System-under-test (326)" should "AUTO-GEN-TEST (326)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(326))
		actorRef ! (Bus.Publish(326, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(326))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(326))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(326))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(326))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(326))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(326))
		expectMsg(Bus.Error)

	}

	"The System-under-test (327)" should "AUTO-GEN-TEST (327)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(327))
		actorRef ! (Bus.SubscribeCallback(327))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(327, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(327))
		actorRef ! (Bus.Publish(327, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(327))
		expectMsg(Bus.Error)

	}

	"The System-under-test (328)" should "AUTO-GEN-TEST (328)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(328))
		actorRef ! (Bus.SubscribeCallback(328))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(328, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(328))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(328))
		actorRef ! (Bus.Disconnect(328))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(328))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(328))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(328))
		actorRef ! (Bus.DestroyConnection(328))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(328))
		actorRef ! (Bus.Subscribe(328))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(328))
		actorRef ! (Bus.Subscribe(328))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(328))
		actorRef ! (Bus.SubscribeCallback(328))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(328))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(328, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(328))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(328))
		expectMsg(Bus.Success)

	}

	"The System-under-test (329)" should "AUTO-GEN-TEST (329)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(329))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(329))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(329))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(329))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(329, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(329, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(329))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(329))
		expectMsg(Bus.Error)

	}

	"The System-under-test (330)" should "AUTO-GEN-TEST (330)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(330))
		actorRef ! (Bus.Subscribe(330))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(330))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(330))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(330))
		actorRef ! (Bus.SubscribeCallback(330))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(330))
		actorRef ! (Bus.CreateConnection(330))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(330))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(330))
		actorRef ! (Bus.UnsubscribeCallback(330))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(330, "foo"))
		actorRef ! (Bus.Unsubscribe(330))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(330))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(330))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(330, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (331)" should "AUTO-GEN-TEST (331)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(331))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(331))
		actorRef ! (Bus.UnsubscribeCallback(331))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(331, "foo"))
		actorRef ! (Bus.Subscribe(331))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(331))
		actorRef ! (Bus.Disconnect(331))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(331))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(331))
		actorRef ! (Bus.UnsubscribeCallback(331))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(331))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(331))
		actorRef ! (Bus.Unsubscribe(331))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(331))
		actorRef ! (Bus.Connect(331))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(331, "foo"))
		actorRef ! (Bus.Disconnect(331))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(331))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(331))
		expectMsg(Bus.Error)

	}

	"The System-under-test (332)" should "AUTO-GEN-TEST (332)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(332))
		actorRef ! (Bus.CreateConnection(332))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(332))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(332))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(332))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(332))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(332))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(332))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(332, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (333)" should "AUTO-GEN-TEST (333)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(333))
		actorRef ! (Bus.CreateConnection(333))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(333))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(333))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(333, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (334)" should "AUTO-GEN-TEST (334)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(334))
		actorRef ! (Bus.CreateConnection(334))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(334, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(334))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(334, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(334, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(334))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(334))
		expectMsg(Bus.Error)

	}

	"The System-under-test (335)" should "AUTO-GEN-TEST (335)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(335))
		actorRef ! (Bus.UnsubscribeCallback(335))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(335))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(335))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(335))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(335))
		expectMsg(Bus.Error)

	}

	"The System-under-test (336)" should "AUTO-GEN-TEST (336)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(336))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(336))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(336))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(336))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(336))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(336))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(336))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(336))
		expectMsg(Bus.Error)

	}

	"The System-under-test (337)" should "AUTO-GEN-TEST (337)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(337))
		actorRef ! (Bus.SubscribeCallback(337))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(337))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(337))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(337))
		expectMsg(Bus.Error)

	}

	"The System-under-test (338)" should "AUTO-GEN-TEST (338)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(338))
		actorRef ! (Bus.SubscribeCallback(338))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(338))
		actorRef ! (Bus.DestroyConnection(338))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(338))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(338))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(338))
		actorRef ! (Bus.UnsubscribeCallback(338))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(338))
		actorRef ! (Bus.UnsubscribeCallback(338))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (339)" should "AUTO-GEN-TEST (339)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(339))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(339))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(339))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(339))
		expectMsg(Bus.Error)

	}

	"The System-under-test (340)" should "AUTO-GEN-TEST (340)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(340))
		expectMsg(Bus.Success)

	}

	"The System-under-test (341)" should "AUTO-GEN-TEST (341)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(341))
		actorRef ! (Bus.UnsubscribeCallback(341))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(341))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(341))
		expectMsg(Bus.Success)

	}

	"The System-under-test (342)" should "AUTO-GEN-TEST (342)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(342))
		actorRef ! (Bus.DestroyConnection(342))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(342, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(342))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(342))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(342))
		expectMsg(Bus.Success)

	}

	"The System-under-test (343)" should "AUTO-GEN-TEST (343)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(343))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(343))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(343))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(343))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(343))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(343))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(343, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(343))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(343))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(343))
		expectMsg(Bus.Error)

	}

	"The System-under-test (344)" should "AUTO-GEN-TEST (344)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(344))
		actorRef ! (Bus.DestroyConnection(344))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(344, "foo"))
		actorRef ! (Bus.Unsubscribe(344))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(344, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(344, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(344, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(344))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(344))
		expectMsg(Bus.Error)

	}

	"The System-under-test (345)" should "AUTO-GEN-TEST (345)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(345))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(345, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(345, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(345, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(345, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(345))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(345))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(345))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(345))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(345))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(345))
		expectMsg(Bus.Error)

	}

	"The System-under-test (346)" should "AUTO-GEN-TEST (346)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(346))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(346))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(346))
		actorRef ! (Bus.Connect(346))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(346, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(346))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(346))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(346))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(346))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(346, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (347)" should "AUTO-GEN-TEST (347)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(347))
		actorRef ! (Bus.Connect(347))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(347))
		expectMsg(Bus.Error)

	}

	"The System-under-test (348)" should "AUTO-GEN-TEST (348)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(348))
		actorRef ! (Bus.Connect(348))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(348))
		actorRef ! (Bus.UnsubscribeCallback(348))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(348))
		actorRef ! (Bus.Publish(348, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(348))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(348))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(348))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(348))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(348))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(348))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(348))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(348))
		expectMsg(Bus.Error)

	}

	"The System-under-test (349)" should "AUTO-GEN-TEST (349)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(349, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(349, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(349))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(349))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(349))
		expectMsg(Bus.Success)

	}

	"The System-under-test (350)" should "AUTO-GEN-TEST (350)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(350))
		actorRef ! (Bus.Disconnect(350))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(350))
		actorRef ! (Bus.Connect(350))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (351)" should "AUTO-GEN-TEST (351)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(351))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(351))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(351))
		actorRef ! (Bus.Subscribe(351))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(351))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(351))
		actorRef ! (Bus.Unsubscribe(351))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(351))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(351))
		expectMsg(Bus.Error)

	}

	"The System-under-test (352)" should "AUTO-GEN-TEST (352)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(352))
		actorRef ! (Bus.CreateConnection(352))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(352))
		actorRef ! (Bus.DestroyConnection(352))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(352))
		actorRef ! (Bus.SubscribeCallback(352))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(352, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(352))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(352, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (353)" should "AUTO-GEN-TEST (353)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(353))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(353))
		actorRef ! (Bus.CreateConnection(353))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(353))
		actorRef ! (Bus.UnsubscribeCallback(353))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(353))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(353))
		expectMsg(Bus.Error)

	}

	"The System-under-test (354)" should "AUTO-GEN-TEST (354)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(354))
		actorRef ! (Bus.Unsubscribe(354))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(354))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(354, "foo"))
		actorRef ! (Bus.DestroyConnection(354))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(354, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(354))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(354))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(354))
		expectMsg(Bus.Error)

	}

	"The System-under-test (355)" should "AUTO-GEN-TEST (355)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(355))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(355, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(355))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(355))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(355))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(355, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(355, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(355))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(355))
		expectMsg(Bus.Error)

	}

	"The System-under-test (356)" should "AUTO-GEN-TEST (356)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(356))
		actorRef ! (Bus.Disconnect(356))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(356))
		actorRef ! (Bus.Unsubscribe(356))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(356))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(356))
		actorRef ! (Bus.DestroyConnection(356))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(356))
		actorRef ! (Bus.CreateConnection(356))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(356))
		actorRef ! (Bus.Subscribe(356))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(356, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(356))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(356))
		expectMsg(Bus.Success)

	}

	"The System-under-test (357)" should "AUTO-GEN-TEST (357)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(357))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(357))
		actorRef ! (Bus.UnsubscribeCallback(357))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(357, "foo"))
		actorRef ! (Bus.DestroyConnection(357))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(357))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(357))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(357))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(357, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(357))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(357))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(357))
		expectMsg(Bus.Error)

	}

	"The System-under-test (358)" should "AUTO-GEN-TEST (358)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(358))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(358))
		actorRef ! (Bus.DestroyConnection(358))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(358))
		actorRef ! (Bus.UnsubscribeCallback(358))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(358))
		actorRef ! (Bus.DestroyConnection(358))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(358))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(358))
		expectMsg(Bus.Error)

	}

	"The System-under-test (359)" should "AUTO-GEN-TEST (359)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(359))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(359))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(359))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(359))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(359))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(359, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(359))
		expectMsg(Bus.Error)

	}

	"The System-under-test (360)" should "AUTO-GEN-TEST (360)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(360))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(360))
		actorRef ! (Bus.UnsubscribeCallback(360))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(360))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(360))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(360))
		actorRef ! (Bus.Subscribe(360))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(360))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(360))
		actorRef ! (Bus.SubscribeCallback(360))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(360, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(360))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(360, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (361)" should "AUTO-GEN-TEST (361)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(361))
		actorRef ! (Bus.Connect(361))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(361, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (362)" should "AUTO-GEN-TEST (362)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(362))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(362))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(362))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(362))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(362))
		actorRef ! (Bus.Unsubscribe(362))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(362, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(362))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(362))
		actorRef ! (Bus.CreateConnection(362))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(362))
		expectMsg(Bus.Error)

	}

	"The System-under-test (363)" should "AUTO-GEN-TEST (363)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(363))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(363))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(363))
		actorRef ! (Bus.Subscribe(363))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(363))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(363))
		actorRef ! (Bus.Connect(363))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(363))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(363))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(363))
		actorRef ! (Bus.UnsubscribeCallback(363))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(363))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(363))
		actorRef ! (Bus.UnsubscribeCallback(363))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(363))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(363))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(363))
		actorRef ! (Bus.Publish(363, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(363))
		actorRef ! (Bus.CreateConnection(363))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(363))
		expectMsg(Bus.Success)

	}

	"The System-under-test (364)" should "AUTO-GEN-TEST (364)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(364))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(364))
		actorRef ! (Bus.Disconnect(364))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(364))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(364))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(364))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(364))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(364))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(364))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(364))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(364, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (365)" should "AUTO-GEN-TEST (365)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(365))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(365))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(365))
		actorRef ! (Bus.Disconnect(365))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(365))
		actorRef ! (Bus.Publish(365, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(365))
		actorRef ! (Bus.UnsubscribeCallback(365))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(365, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(365))
		actorRef ! (Bus.Unsubscribe(365))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(365))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(365))
		actorRef ! (Bus.Connect(365))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(365))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(365))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(365, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(365))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(365, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (366)" should "AUTO-GEN-TEST (366)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(366))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(366))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(366))
		actorRef ! (Bus.Unsubscribe(366))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(366, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(366))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(366))
		expectMsg(Bus.Error)

	}

	"The System-under-test (367)" should "AUTO-GEN-TEST (367)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(367))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(367))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(367))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(367))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(367, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(367))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(367))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(367))
		expectMsg(Bus.Success)

	}

	"The System-under-test (368)" should "AUTO-GEN-TEST (368)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(368))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(368))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(368))
		actorRef ! (Bus.Unsubscribe(368))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(368))
		actorRef ! (Bus.Unsubscribe(368))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(368))
		actorRef ! (Bus.DestroyConnection(368))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(368))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(368))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(368))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(368))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(368))
		expectMsg(Bus.Error)

	}

	"The System-under-test (369)" should "AUTO-GEN-TEST (369)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(369))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(369))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(369))
		actorRef ! (Bus.Unsubscribe(369))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(369, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(369))
		expectMsg(Bus.Success)

	}

	"The System-under-test (370)" should "AUTO-GEN-TEST (370)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(370))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(370))
		actorRef ! (Bus.DestroyConnection(370))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(370, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(370))
		actorRef ! (Bus.SubscribeCallback(370))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(370, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(370))
		actorRef ! (Bus.Publish(370, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(370))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(370))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(370))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(370))
		actorRef ! (Bus.Connect(370))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(370))
		actorRef ! (Bus.Publish(370, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(370))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(370))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(370))
		expectMsg(Bus.Error)

	}

	"The System-under-test (371)" should "AUTO-GEN-TEST (371)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(371))
		actorRef ! (Bus.UnsubscribeCallback(371))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(371))
		actorRef ! (Bus.Connect(371))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(371))
		actorRef ! (Bus.Publish(371, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(371))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(371))
		actorRef ! (Bus.Subscribe(371))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(371))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(371))
		actorRef ! (Bus.Unsubscribe(371))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(371))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(371))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(371))
		actorRef ! (Bus.UnsubscribeCallback(371))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(371))
		actorRef ! (Bus.CreateConnection(371))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(371))
		actorRef ! (Bus.SubscribeCallback(371))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (372)" should "AUTO-GEN-TEST (372)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(372))
		actorRef ! (Bus.DestroyConnection(372))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(372))
		actorRef ! (Bus.Publish(372, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(372))
		actorRef ! (Bus.UnsubscribeCallback(372))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(372))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(372))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(372))
		expectMsg(Bus.Error)

	}

	"The System-under-test (373)" should "AUTO-GEN-TEST (373)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(373))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(373))
		actorRef ! (Bus.Unsubscribe(373))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(373))
		actorRef ! (Bus.Disconnect(373))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(373))
		actorRef ! (Bus.Publish(373, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(373))
		actorRef ! (Bus.CreateConnection(373))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(373))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(373))
		actorRef ! (Bus.Disconnect(373))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(373, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(373))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(373, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(373))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(373))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(373))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(373))
		expectMsg(Bus.Error)

	}

	"The System-under-test (374)" should "AUTO-GEN-TEST (374)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(374))
		actorRef ! (Bus.Publish(374, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(374))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(374))
		actorRef ! (Bus.Subscribe(374))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(374))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(374))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(374, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(374, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (375)" should "AUTO-GEN-TEST (375)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(375))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(375))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(375))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(375, "foo"))
		actorRef ! (Bus.Publish(375, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(375))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(375, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (376)" should "AUTO-GEN-TEST (376)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(376))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(376))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(376))
		actorRef ! (Bus.Unsubscribe(376))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(376))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(376, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(376))
		actorRef ! (Bus.UnsubscribeCallback(376))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(376))
		actorRef ! (Bus.Subscribe(376))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(376))
		actorRef ! (Bus.Publish(376, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(376))
		actorRef ! (Bus.Unsubscribe(376))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(376))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(376))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(376))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(376))
		actorRef ! (Bus.Connect(376))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(376))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(376))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(376))
		expectMsg(Bus.Success)

	}

	"The System-under-test (377)" should "AUTO-GEN-TEST (377)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(377))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(377, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(377))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(377, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(377))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(377))
		expectMsg(Bus.Success)

	}

	"The System-under-test (378)" should "AUTO-GEN-TEST (378)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(378))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(378))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(378))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(378))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(378))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(378))
		expectMsg(Bus.Error)

	}

	"The System-under-test (379)" should "AUTO-GEN-TEST (379)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(379))
		actorRef ! (Bus.DestroyConnection(379))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(379))
		actorRef ! (Bus.SubscribeCallback(379))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(379))
		actorRef ! (Bus.CreateConnection(379))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(379))
		actorRef ! (Bus.UnsubscribeCallback(379))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(379))
		actorRef ! (Bus.Disconnect(379))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(379, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(379))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(379))
		expectMsg(Bus.Error)

	}

	"The System-under-test (380)" should "AUTO-GEN-TEST (380)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(380))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(380))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(380))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(380))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(380))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(380, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(380))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(380))
		expectMsg(Bus.Error)

	}

	"The System-under-test (381)" should "AUTO-GEN-TEST (381)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(381))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(381))
		actorRef ! (Bus.SubscribeCallback(381))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(381))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(381))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(381))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(381))
		expectMsg(Bus.Error)

	}

	"The System-under-test (382)" should "AUTO-GEN-TEST (382)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(382))
		actorRef ! (Bus.Subscribe(382))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(382))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(382))
		expectMsg(Bus.Success)

	}

	"The System-under-test (383)" should "AUTO-GEN-TEST (383)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(383))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(383))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(383))
		actorRef ! (Bus.CreateConnection(383))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(383, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(383))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(383))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(383))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(383))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(383))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(383))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(383, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (384)" should "AUTO-GEN-TEST (384)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(384))
		actorRef ! (Bus.UnsubscribeCallback(384))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(384))
		actorRef ! (Bus.Disconnect(384))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(384))
		actorRef ! (Bus.UnsubscribeCallback(384))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(384, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(384))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(384, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(384))
		expectMsg(Bus.Success)

	}

	"The System-under-test (385)" should "AUTO-GEN-TEST (385)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(385))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(385))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(385))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(385))
		actorRef ! (Bus.Publish(385, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(385))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(385, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (386)" should "AUTO-GEN-TEST (386)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(386))
		actorRef ! (Bus.Publish(386, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(386))
		actorRef ! (Bus.CreateConnection(386))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(386))
		actorRef ! (Bus.SubscribeCallback(386))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(386))
		actorRef ! (Bus.Connect(386))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(386))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(386, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(386))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(386, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (387)" should "AUTO-GEN-TEST (387)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(387))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(387))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(387))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(387))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(387))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(387))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(387))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(387))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(387))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(387))
		expectMsg(Bus.Error)

	}

	"The System-under-test (388)" should "AUTO-GEN-TEST (388)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(388))
		actorRef ! (Bus.Subscribe(388))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(388))
		actorRef ! (Bus.UnsubscribeCallback(388))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(388))
		actorRef ! (Bus.Disconnect(388))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(388))
		actorRef ! (Bus.DestroyConnection(388))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(388))
		actorRef ! (Bus.Disconnect(388))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(388))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(388))
		expectMsg(Bus.Error)

	}

	"The System-under-test (389)" should "AUTO-GEN-TEST (389)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(389))
		actorRef ! (Bus.Unsubscribe(389))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(389))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(389, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(389, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(389))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(389))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(389))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(389))
		expectMsg(Bus.Error)

	}

	"The System-under-test (390)" should "AUTO-GEN-TEST (390)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(390))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(390))
		actorRef ! (Bus.Unsubscribe(390))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(390, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(390))
		actorRef ! (Bus.SubscribeCallback(390))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(390))
		actorRef ! (Bus.Publish(390, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(390))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(390))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(390))
		expectMsg(Bus.Error)

	}

	"The System-under-test (391)" should "AUTO-GEN-TEST (391)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(391))
		actorRef ! (Bus.UnsubscribeCallback(391))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(391))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(391))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(391, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(391))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(391))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(391))
		expectMsg(Bus.Error)

	}

	"The System-under-test (392)" should "AUTO-GEN-TEST (392)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(392))
		actorRef ! (Bus.CreateConnection(392))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(392))
		actorRef ! (Bus.DestroyConnection(392))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(392))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(392))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(392))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(392))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(392))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(392))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(392, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(392))
		expectMsg(Bus.Error)

	}

	"The System-under-test (393)" should "AUTO-GEN-TEST (393)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(393))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(393))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(393))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(393))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(393))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(393, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (394)" should "AUTO-GEN-TEST (394)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(394))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(394))
		actorRef ! (Bus.Disconnect(394))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(394))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(394))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(394))
		actorRef ! (Bus.SubscribeCallback(394))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(394))
		actorRef ! (Bus.SubscribeCallback(394))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(394))
		actorRef ! (Bus.UnsubscribeCallback(394))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(394, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(394, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(394))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(394))
		expectMsg(Bus.Error)

	}

	"The System-under-test (395)" should "AUTO-GEN-TEST (395)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(395))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(395))
		actorRef ! (Bus.Connect(395))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(395, "foo"))
		actorRef ! (Bus.Publish(395, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (396)" should "AUTO-GEN-TEST (396)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(396))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(396))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(396))
		expectMsg(Bus.Success)

	}

	"The System-under-test (397)" should "AUTO-GEN-TEST (397)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(397))
		actorRef ! (Bus.Connect(397))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(397, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(397))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(397))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(397, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (398)" should "AUTO-GEN-TEST (398)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(398))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(398))
		actorRef ! (Bus.Connect(398))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(398))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(398))
		actorRef ! (Bus.Subscribe(398))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(398, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(398))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(398))
		actorRef ! (Bus.Connect(398))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(398))
		actorRef ! (Bus.UnsubscribeCallback(398))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(398))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(398))
		expectMsg(Bus.Error)

	}

	"The System-under-test (399)" should "AUTO-GEN-TEST (399)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(399))
		actorRef ! (Bus.Disconnect(399))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(399, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(399))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(399))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(399))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(399))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(399))
		expectMsg(Bus.Error)

	}

	"The System-under-test (400)" should "AUTO-GEN-TEST (400)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(400))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(400))
		actorRef ! (Bus.Unsubscribe(400))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(400))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(400, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(400, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(400))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(400, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (401)" should "AUTO-GEN-TEST (401)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(401))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(401))
		expectMsg(Bus.Error)

	}

	"The System-under-test (402)" should "AUTO-GEN-TEST (402)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(402))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(402))
		actorRef ! (Bus.CreateConnection(402))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(402))
		actorRef ! (Bus.UnsubscribeCallback(402))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(402))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(402))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(402))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(402))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(402))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(402))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(402))
		expectMsg(Bus.Error)

	}

	"The System-under-test (403)" should "AUTO-GEN-TEST (403)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(403))
		actorRef ! (Bus.SubscribeCallback(403))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(403))
		actorRef ! (Bus.Disconnect(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(403, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(403))
		actorRef ! (Bus.DestroyConnection(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(403))
		actorRef ! (Bus.DestroyConnection(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(403))
		actorRef ! (Bus.SubscribeCallback(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(403))
		actorRef ! (Bus.Connect(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(403))
		actorRef ! (Bus.SubscribeCallback(403))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(403))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(403))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(403))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(403))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(403))
		expectMsg(Bus.Error)

	}

	"The System-under-test (404)" should "AUTO-GEN-TEST (404)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(404))
		actorRef ! (Bus.Disconnect(404))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(404))
		actorRef ! (Bus.DestroyConnection(404))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(404))
		actorRef ! (Bus.Connect(404))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(404))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(404))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(404, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(404))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(404, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(404))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(404))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(404))
		expectMsg(Bus.Error)

	}

	"The System-under-test (405)" should "AUTO-GEN-TEST (405)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(405))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(405))
		actorRef ! (Bus.Disconnect(405))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(405))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(405))
		expectMsg(Bus.Success)

	}

	"The System-under-test (406)" should "AUTO-GEN-TEST (406)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(406))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(406))
		actorRef ! (Bus.Subscribe(406))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(406))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(406))
		actorRef ! (Bus.Unsubscribe(406))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(406))
		actorRef ! (Bus.UnsubscribeCallback(406))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(406, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (407)" should "AUTO-GEN-TEST (407)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(407))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(407))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(407))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(407))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(407))
		actorRef ! (Bus.Subscribe(407))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(407))
		actorRef ! (Bus.Subscribe(407))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(407))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(407))
		actorRef ! (Bus.Unsubscribe(407))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(407))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(407))
		actorRef ! (Bus.Subscribe(407))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(407))
		actorRef ! (Bus.Disconnect(407))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(407))
		actorRef ! (Bus.Connect(407))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(407, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(407))
		expectMsg(Bus.Error)

	}

	"The System-under-test (408)" should "AUTO-GEN-TEST (408)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(408))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(408, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(408))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(408))
		expectMsg(Bus.Error)

	}

	"The System-under-test (409)" should "AUTO-GEN-TEST (409)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(409))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(409))
		actorRef ! (Bus.DestroyConnection(409))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(409))
		actorRef ! (Bus.Connect(409))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(409, "foo"))
		actorRef ! (Bus.CreateConnection(409))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(409))
		actorRef ! (Bus.Connect(409))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(409))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(409))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(409))
		expectMsg(Bus.Error)

	}

	"The System-under-test (410)" should "AUTO-GEN-TEST (410)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(410))
		actorRef ! (Bus.Publish(410, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(410))
		actorRef ! (Bus.Publish(410, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(410))
		actorRef ! (Bus.SubscribeCallback(410))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(410, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(410))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(410))
		expectMsg(Bus.Error)

	}

	"The System-under-test (411)" should "AUTO-GEN-TEST (411)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(411))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(411))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(411))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(411))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(411))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(411))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(411))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(411))
		expectMsg(Bus.Error)

	}

	"The System-under-test (412)" should "AUTO-GEN-TEST (412)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(412))
		actorRef ! (Bus.UnsubscribeCallback(412))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(412))
		actorRef ! (Bus.Subscribe(412))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(412))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(412))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(412))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(412))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(412))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(412))
		expectMsg(Bus.Success)

	}

	"The System-under-test (413)" should "AUTO-GEN-TEST (413)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(413))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(413))
		actorRef ! (Bus.Unsubscribe(413))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(413, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (414)" should "AUTO-GEN-TEST (414)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(414))
		actorRef ! (Bus.Unsubscribe(414))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(414))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(414))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(414))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(414))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(414, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(414))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(414))
		expectMsg(Bus.Success)

	}

	"The System-under-test (415)" should "AUTO-GEN-TEST (415)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(415))
		actorRef ! (Bus.UnsubscribeCallback(415))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(415))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(415))
		actorRef ! (Bus.Unsubscribe(415))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(415))
		actorRef ! (Bus.Disconnect(415))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(415, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(415))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(415))
		expectMsg(Bus.Error)

	}

	"The System-under-test (416)" should "AUTO-GEN-TEST (416)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(416))
		actorRef ! (Bus.SubscribeCallback(416))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(416))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(416))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(416, "foo"))
		actorRef ! (Bus.Subscribe(416))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(416))
		actorRef ! (Bus.CreateConnection(416))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(416))
		expectMsg(Bus.Success)

	}

	"The System-under-test (417)" should "AUTO-GEN-TEST (417)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(417))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(417))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(417, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(417))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(417))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(417))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(417))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(417, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (418)" should "AUTO-GEN-TEST (418)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(418))
		actorRef ! (Bus.Unsubscribe(418))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(418))
		actorRef ! (Bus.Connect(418))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(418))
		actorRef ! (Bus.Subscribe(418))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(418))
		actorRef ! (Bus.Unsubscribe(418))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(418))
		actorRef ! (Bus.Connect(418))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(418))
		actorRef ! (Bus.Publish(418, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(418, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(418))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(418))
		expectMsg(Bus.Error)

	}

	"The System-under-test (419)" should "AUTO-GEN-TEST (419)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(419))
		actorRef ! (Bus.Subscribe(419))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(419))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(419))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(419))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(419))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(419))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(419))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(419))
		expectMsg(Bus.Error)

	}

	"The System-under-test (420)" should "AUTO-GEN-TEST (420)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(420))
		actorRef ! (Bus.SubscribeCallback(420))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(420))
		actorRef ! (Bus.Disconnect(420))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(420))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(420))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(420))
		expectMsg(Bus.Error)

	}

	"The System-under-test (421)" should "AUTO-GEN-TEST (421)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(421))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(421))
		actorRef ! (Bus.Subscribe(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(421, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(421))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(421))
		actorRef ! (Bus.Unsubscribe(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(421, "foo"))
		actorRef ! (Bus.Unsubscribe(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(421, "foo"))
		actorRef ! (Bus.CreateConnection(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(421))
		actorRef ! (Bus.Unsubscribe(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(421))
		actorRef ! (Bus.CreateConnection(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(421))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(421))
		actorRef ! (Bus.CreateConnection(421))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(421))
		expectMsg(Bus.Error)

	}

	"The System-under-test (422)" should "AUTO-GEN-TEST (422)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(422))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(422))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(422))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(422, "foo"))
		actorRef ! (Bus.Disconnect(422))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(422))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(422))
		expectMsg(Bus.Success)

	}

	"The System-under-test (423)" should "AUTO-GEN-TEST (423)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(423))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(423))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(423, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(423, "foo"))
		actorRef ! (Bus.Unsubscribe(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(423))
		actorRef ! (Bus.DestroyConnection(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(423))
		actorRef ! (Bus.Unsubscribe(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(423))
		actorRef ! (Bus.Publish(423, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(423))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(423))
		actorRef ! (Bus.Subscribe(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(423))
		actorRef ! (Bus.UnsubscribeCallback(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(423))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(423))
		actorRef ! (Bus.SubscribeCallback(423))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (424)" should "AUTO-GEN-TEST (424)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(424))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(424))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(424))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(424))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(424))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(424))
		expectMsg(Bus.Error)

	}

	"The System-under-test (425)" should "AUTO-GEN-TEST (425)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(425))
		actorRef ! (Bus.Connect(425))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(425))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(425))
		actorRef ! (Bus.SubscribeCallback(425))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(425))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(425))
		actorRef ! (Bus.Publish(425, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(425))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(425))
		actorRef ! (Bus.Disconnect(425))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(425))
		actorRef ! (Bus.Subscribe(425))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(425))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(425))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(425))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(425))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(425))
		expectMsg(Bus.Success)

	}

	"The System-under-test (426)" should "AUTO-GEN-TEST (426)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(426))
		actorRef ! (Bus.Unsubscribe(426))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (427)" should "AUTO-GEN-TEST (427)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(427))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(427))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(427))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(427))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(427, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(427))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(427))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(427))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(427))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(427, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (428)" should "AUTO-GEN-TEST (428)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(428))
		actorRef ! (Bus.Unsubscribe(428))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(428, "foo"))
		actorRef ! (Bus.Connect(428))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(428))
		actorRef ! (Bus.Subscribe(428))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(428))
		actorRef ! (Bus.Subscribe(428))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(428))
		actorRef ! (Bus.Subscribe(428))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(428))
		actorRef ! (Bus.Connect(428))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(428))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(428))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(428))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(428))
		actorRef ! (Bus.Connect(428))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(428))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(428))
		expectMsg(Bus.Error)

	}

	"The System-under-test (429)" should "AUTO-GEN-TEST (429)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(429))
		actorRef ! (Bus.DestroyConnection(429))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(429))
		actorRef ! (Bus.UnsubscribeCallback(429))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(429))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(429))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(429, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(429))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(429))
		actorRef ! (Bus.Publish(429, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(429))
		expectMsg(Bus.Success)

	}

	"The System-under-test (430)" should "AUTO-GEN-TEST (430)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(430))
		actorRef ! (Bus.Disconnect(430))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(430))
		actorRef ! (Bus.Publish(430, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(430))
		actorRef ! (Bus.Publish(430, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(430))
		actorRef ! (Bus.Publish(430, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(430))
		actorRef ! (Bus.Publish(430, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(430))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(430))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(430))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(430, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (431)" should "AUTO-GEN-TEST (431)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(431))
		actorRef ! (Bus.DestroyConnection(431))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(431))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(431))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(431))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(431))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(431))
		expectMsg(Bus.Success)

	}

	"The System-under-test (432)" should "AUTO-GEN-TEST (432)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(432))
		actorRef ! (Bus.Publish(432, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(432))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(432))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(432))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(432))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(432))
		expectMsg(Bus.Error)

	}

	"The System-under-test (433)" should "AUTO-GEN-TEST (433)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(433))
		actorRef ! (Bus.Unsubscribe(433))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(433))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(433))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(433))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(433))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(433))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(433))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(433, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (434)" should "AUTO-GEN-TEST (434)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(434))
		actorRef ! (Bus.Disconnect(434))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(434))
		actorRef ! (Bus.Subscribe(434))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(434))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(434, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(434, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(434))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(434))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(434))
		expectMsg(Bus.Error)

	}

	"The System-under-test (435)" should "AUTO-GEN-TEST (435)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(435))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(435))
		actorRef ! (Bus.Publish(435, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(435))
		actorRef ! (Bus.Connect(435))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(435, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (436)" should "AUTO-GEN-TEST (436)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(436))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(436))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(436))
		expectMsg(Bus.Error)

	}

	"The System-under-test (437)" should "AUTO-GEN-TEST (437)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(437))
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(437))
		actorRef ! (Bus.UnsubscribeCallback(437))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(437))
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(437))
		actorRef ! (Bus.Disconnect(437))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(437))
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(437))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(437))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(437))
		expectMsg(Bus.Error)

	}

	"The System-under-test (438)" should "AUTO-GEN-TEST (438)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(438))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(438))
		actorRef ! (Bus.DestroyConnection(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(438))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(438))
		actorRef ! (Bus.UnsubscribeCallback(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(438))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(438))
		actorRef ! (Bus.Subscribe(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(438))
		actorRef ! (Bus.CreateConnection(438))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(438, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(438))
		actorRef ! (Bus.Unsubscribe(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(438))
		actorRef ! (Bus.SubscribeCallback(438))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(438))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(438))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(438, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (439)" should "AUTO-GEN-TEST (439)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(439))
		actorRef ! (Bus.CreateConnection(439))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(439))
		actorRef ! (Bus.Subscribe(439))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(439))
		actorRef ! (Bus.Subscribe(439))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(439))
		actorRef ! (Bus.Subscribe(439))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(439))
		actorRef ! (Bus.DestroyConnection(439))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(439))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(439, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(439))
		actorRef ! (Bus.SubscribeCallback(439))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(439))
		actorRef ! (Bus.UnsubscribeCallback(439))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(439))
		actorRef ! (Bus.Disconnect(439))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(439))
		actorRef ! (Bus.UnsubscribeCallback(439))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (440)" should "AUTO-GEN-TEST (440)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(440))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(440))
		actorRef ! (Bus.Publish(440, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(440))
		actorRef ! (Bus.DestroyConnection(440))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(440))
		actorRef ! (Bus.Subscribe(440))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(440))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(440))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(440))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(440))
		expectMsg(Bus.Success)

	}

	"The System-under-test (441)" should "AUTO-GEN-TEST (441)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(441))
		actorRef ! (Bus.Disconnect(441))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(441))
		actorRef ! (Bus.SubscribeCallback(441))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(441))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(441, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(441))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(441))
		expectMsg(Bus.Error)

	}

	"The System-under-test (442)" should "AUTO-GEN-TEST (442)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(442))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(442))
		actorRef ! (Bus.Subscribe(442))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(442))
		actorRef ! (Bus.Unsubscribe(442))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(442))
		actorRef ! (Bus.Publish(442, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(442))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(442))
		actorRef ! (Bus.Unsubscribe(442))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(442))
		actorRef ! (Bus.CreateConnection(442))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(442))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(442))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(442))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(442))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(442))
		actorRef ! (Bus.Connect(442))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(442, "foo"))
		actorRef ! (Bus.Publish(442, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (443)" should "AUTO-GEN-TEST (443)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(443))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(443))
		actorRef ! (Bus.Subscribe(443))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(443))
		actorRef ! (Bus.SubscribeCallback(443))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(443))
		actorRef ! (Bus.Publish(443, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(443, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(443))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(443))
		expectMsg(Bus.Error)

	}

	"The System-under-test (444)" should "AUTO-GEN-TEST (444)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(444))
		actorRef ! (Bus.Connect(444))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(444))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(444))
		actorRef ! (Bus.UnsubscribeCallback(444))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(444))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(444))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(444))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(444))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(444))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(444))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(444, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (445)" should "AUTO-GEN-TEST (445)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(445))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(445))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(445))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(445))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(445))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(445))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(445, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (446)" should "AUTO-GEN-TEST (446)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(446))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(446))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(446))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(446))
		expectMsg(Bus.Success)

	}

	"The System-under-test (447)" should "AUTO-GEN-TEST (447)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(447))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(447))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(447))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(447, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(447))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(447))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(447))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(447, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(447))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(447))
		expectMsg(Bus.Error)

	}

	"The System-under-test (448)" should "AUTO-GEN-TEST (448)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(448))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(448))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(448))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(448))
		expectMsg(Bus.Error)

	}

	"The System-under-test (449)" should "AUTO-GEN-TEST (449)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(449))
		actorRef ! (Bus.Subscribe(449))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(449))
		actorRef ! (Bus.Subscribe(449))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(449))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(449))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(449))
		actorRef ! (Bus.SubscribeCallback(449))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(449))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(449))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(449, "foo"))
		actorRef ! (Bus.CreateConnection(449))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(449))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(449, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (450)" should "AUTO-GEN-TEST (450)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(450))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(450))
		actorRef ! (Bus.Connect(450))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(450))
		actorRef ! (Bus.UnsubscribeCallback(450))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(450))
		actorRef ! (Bus.Subscribe(450))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(450))
		actorRef ! (Bus.UnsubscribeCallback(450))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(450, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(450, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(450))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(450))
		expectMsg(Bus.Error)

	}

	"The System-under-test (451)" should "AUTO-GEN-TEST (451)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(451))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(451))
		actorRef ! (Bus.UnsubscribeCallback(451))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(451))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(451, "foo"))
		actorRef ! (Bus.SubscribeCallback(451))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(451))
		actorRef ! (Bus.Unsubscribe(451))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(451))
		actorRef ! (Bus.CreateConnection(451))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(451))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(451))
		actorRef ! (Bus.UnsubscribeCallback(451))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(451))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(451))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(451))
		expectMsg(Bus.Error)

	}

	"The System-under-test (452)" should "AUTO-GEN-TEST (452)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(452))
		actorRef ! (Bus.CreateConnection(452))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(452))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(452))
		actorRef ! (Bus.UnsubscribeCallback(452))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(452))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(452))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(452))
		expectMsg(Bus.Error)

	}

	"The System-under-test (453)" should "AUTO-GEN-TEST (453)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(453))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(453))
		actorRef ! (Bus.Publish(453, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(453))
		actorRef ! (Bus.Disconnect(453))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(453))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(453))
		actorRef ! (Bus.Subscribe(453))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(453))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(453))
		actorRef ! (Bus.SubscribeCallback(453))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(453))
		actorRef ! (Bus.Subscribe(453))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(453))
		actorRef ! (Bus.Connect(453))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(453))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(453))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(453))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(453))
		expectMsg(Bus.Error)

	}

	"The System-under-test (454)" should "AUTO-GEN-TEST (454)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(454))
		actorRef ! (Bus.Publish(454, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(454))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(454))
		actorRef ! (Bus.Unsubscribe(454))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(454))
		actorRef ! (Bus.UnsubscribeCallback(454))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(454))
		actorRef ! (Bus.Publish(454, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(454))
		actorRef ! (Bus.UnsubscribeCallback(454))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(454))
		actorRef ! (Bus.Publish(454, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(454))
		actorRef ! (Bus.DestroyConnection(454))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(454))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(454))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(454))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(454))
		expectMsg(Bus.Error)

	}

	"The System-under-test (455)" should "AUTO-GEN-TEST (455)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(455))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(455))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(455))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(455, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(455))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(455, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(455, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(455))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(455))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(455, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (456)" should "AUTO-GEN-TEST (456)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(456))
		actorRef ! (Bus.Publish(456, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(456))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(456, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(456))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(456, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(456))
		expectMsg(Bus.Error)

	}

	"The System-under-test (457)" should "AUTO-GEN-TEST (457)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(457))
		actorRef ! (Bus.SubscribeCallback(457))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(457))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(457))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(457))
		actorRef ! (Bus.CreateConnection(457))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(457))
		expectMsg(Bus.Error)

	}

	"The System-under-test (458)" should "AUTO-GEN-TEST (458)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(458))
		actorRef ! (Bus.DestroyConnection(458))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(458))
		actorRef ! (Bus.UnsubscribeCallback(458))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(458))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(458))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(458))
		actorRef ! (Bus.CreateConnection(458))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(458))
		actorRef ! (Bus.Disconnect(458))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(458))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(458))
		expectMsg(Bus.Error)

	}

	"The System-under-test (459)" should "AUTO-GEN-TEST (459)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(459))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(459))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(459))
		actorRef ! (Bus.Unsubscribe(459))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(459))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(459))
		actorRef ! (Bus.Unsubscribe(459))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(459))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(459))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(459))
		actorRef ! (Bus.Connect(459))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(459))
		expectMsg(Bus.Error)

	}

	"The System-under-test (460)" should "AUTO-GEN-TEST (460)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(460))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(460))
		actorRef ! (Bus.DestroyConnection(460))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(460))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(460))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(460))
		expectMsg(Bus.Error)

	}

	"The System-under-test (461)" should "AUTO-GEN-TEST (461)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(461))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(461))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(461))
		actorRef ! (Bus.Publish(461, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(461, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(461))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(461, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(461))
		actorRef ! (Bus.UnsubscribeCallback(461))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(461))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(461))
		actorRef ! (Bus.UnsubscribeCallback(461))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(461))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(461))
		actorRef ! (Bus.SubscribeCallback(461))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(461))
		actorRef ! (Bus.Connect(461))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(461))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(461))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(461))
		expectMsg(Bus.Error)

	}

	"The System-under-test (462)" should "AUTO-GEN-TEST (462)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(462))
		actorRef ! (Bus.Publish(462, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(462))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(462))
		expectMsg(Bus.Error)

	}

	"The System-under-test (463)" should "AUTO-GEN-TEST (463)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(463))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(463))
		actorRef ! (Bus.Disconnect(463))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(463))
		actorRef ! (Bus.DestroyConnection(463))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(463))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(463))
		actorRef ! (Bus.Unsubscribe(463))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(463))
		actorRef ! (Bus.UnsubscribeCallback(463))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(463))
		actorRef ! (Bus.Disconnect(463))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(463))
		actorRef ! (Bus.Unsubscribe(463))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(463))
		actorRef ! (Bus.Publish(463, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(463))
		actorRef ! (Bus.CreateConnection(463))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(463))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(463))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(463))
		expectMsg(Bus.Success)

	}

	"The System-under-test (464)" should "AUTO-GEN-TEST (464)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(464))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(464, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(464, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(464))
		actorRef ! (Bus.Connect(464))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(464))
		actorRef ! (Bus.UnsubscribeCallback(464))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(464, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(464))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(464))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(464))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(464))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(464, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (465)" should "AUTO-GEN-TEST (465)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(465))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(465))
		actorRef ! (Bus.CreateConnection(465))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(465))
		actorRef ! (Bus.Unsubscribe(465))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(465, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(465))
		actorRef ! (Bus.Unsubscribe(465))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(465))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(465))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(465))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(465))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(465))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(465))
		actorRef ! (Bus.Connect(465))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(465))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(465, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(465))
		actorRef ! (Bus.Disconnect(465))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(465))
		expectMsg(Bus.Error)

	}

	"The System-under-test (466)" should "AUTO-GEN-TEST (466)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(466))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(466))
		expectMsg(Bus.Error)

	}

	"The System-under-test (467)" should "AUTO-GEN-TEST (467)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(467))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(467))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(467))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(467))
		expectMsg(Bus.Error)

	}

	"The System-under-test (468)" should "AUTO-GEN-TEST (468)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(468))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(468))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(468))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(468))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(468, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(468))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(468))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(468))
		expectMsg(Bus.Error)

	}

	"The System-under-test (469)" should "AUTO-GEN-TEST (469)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(469))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(469))
		actorRef ! (Bus.Publish(469, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(469))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(469))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(469))
		actorRef ! (Bus.Connect(469))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(469))
		actorRef ! (Bus.Disconnect(469))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(469))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(469))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(469))
		expectMsg(Bus.Error)

	}

	"The System-under-test (470)" should "AUTO-GEN-TEST (470)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(470))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(470))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(470))
		actorRef ! (Bus.Subscribe(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(470, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(470))
		actorRef ! (Bus.Unsubscribe(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(470))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(470, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(470))
		actorRef ! (Bus.UnsubscribeCallback(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(470))
		actorRef ! (Bus.SubscribeCallback(470))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(470))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(470, "foo"))
		actorRef ! (Bus.Subscribe(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(470))
		actorRef ! (Bus.Unsubscribe(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(470))
		actorRef ! (Bus.CreateConnection(470))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (471)" should "AUTO-GEN-TEST (471)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(471))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(471))
		actorRef ! (Bus.CreateConnection(471))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(471))
		actorRef ! (Bus.Connect(471))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(471))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(471, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(471))
		actorRef ! (Bus.Connect(471))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(471))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(471))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(471))
		actorRef ! (Bus.Publish(471, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (472)" should "AUTO-GEN-TEST (472)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(472))
		actorRef ! (Bus.DestroyConnection(472))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(472))
		actorRef ! (Bus.DestroyConnection(472))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(472, "foo"))
		actorRef ! (Bus.Subscribe(472))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(472))
		actorRef ! (Bus.Disconnect(472))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(472, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(472))
		actorRef ! (Bus.DestroyConnection(472))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(472))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(472, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(472))
		expectMsg(Bus.Error)

	}

	"The System-under-test (473)" should "AUTO-GEN-TEST (473)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(473))
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(473, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(473))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(473))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(473, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(473))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(473))
		expectMsg(Bus.Error)

	}

	"The System-under-test (474)" should "AUTO-GEN-TEST (474)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(474))
		actorRef ! (Bus.UnsubscribeCallback(474))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(474))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(474))
		expectMsg(Bus.Error)

	}

	"The System-under-test (475)" should "AUTO-GEN-TEST (475)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(475))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(475))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(475, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(475))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(475))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(475))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(475))
		actorRef ! (Bus.Publish(475, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(475))
		actorRef ! (Bus.SubscribeCallback(475))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(475))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(475))
		actorRef ! (Bus.UnsubscribeCallback(475))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(475))
		actorRef ! (Bus.Connect(475))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(475))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(475))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(475))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(475))
		expectMsg(Bus.Error)

	}

	"The System-under-test (476)" should "AUTO-GEN-TEST (476)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(476))
		expectMsg(Bus.Success)

	}

	"The System-under-test (477)" should "AUTO-GEN-TEST (477)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(477))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(477))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(477))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(477))
		actorRef ! (Bus.SubscribeCallback(477))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(477))
		actorRef ! (Bus.Subscribe(477))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(477))
		actorRef ! (Bus.Connect(477))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(477, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(477))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(477))
		actorRef ! (Bus.Unsubscribe(477))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(477))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(477))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(477))
		expectMsg(Bus.Error)

	}

	"The System-under-test (478)" should "AUTO-GEN-TEST (478)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(478))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(478))
		actorRef ! (Bus.UnsubscribeCallback(478))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(478))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(478))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(478))
		actorRef ! (Bus.Connect(478))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(478))
		actorRef ! (Bus.Connect(478))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(478))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(478))
		actorRef ! (Bus.CreateConnection(478))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(478))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(478))
		actorRef ! (Bus.Disconnect(478))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(478))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(478))
		actorRef ! (Bus.Publish(478, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(478))
		actorRef ! (Bus.SubscribeCallback(478))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (479)" should "AUTO-GEN-TEST (479)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(479))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(479))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(479))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(479))
		actorRef ! (Bus.Connect(479))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(479))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(479))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(479))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(479))
		actorRef ! (Bus.CreateConnection(479))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(479))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(479))
		actorRef ! (Bus.Subscribe(479))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(479))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(479))
		actorRef ! (Bus.Subscribe(479))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(479, "foo"))
		actorRef ! (Bus.CreateConnection(479))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(479))
		actorRef ! (Bus.Publish(479, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (480)" should "AUTO-GEN-TEST (480)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(480))
		actorRef ! (Bus.Unsubscribe(480))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(480))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(480))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(480))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(480))
		expectMsg(Bus.Error)

	}

	"The System-under-test (481)" should "AUTO-GEN-TEST (481)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(481))
		actorRef ! (Bus.SubscribeCallback(481))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(481))
		actorRef ! (Bus.SubscribeCallback(481))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(481))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(481))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(481))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(481))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(481))
		expectMsg(Bus.Error)

	}

	"The System-under-test (482)" should "AUTO-GEN-TEST (482)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(482))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(482))
		actorRef ! (Bus.CreateConnection(482))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(482))
		actorRef ! (Bus.CreateConnection(482))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(482))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(482))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(482))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(482, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (483)" should "AUTO-GEN-TEST (483)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(483))
		actorRef ! (Bus.CreateConnection(483))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(483, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(483))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(483, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(483))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(483))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(483))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(483))
		expectMsg(Bus.Error)

	}

	"The System-under-test (484)" should "AUTO-GEN-TEST (484)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(484))
		actorRef ! (Bus.Disconnect(484))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(484))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(484))
		actorRef ! (Bus.Unsubscribe(484))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(484))
		actorRef ! (Bus.SubscribeCallback(484))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(484))
		actorRef ! (Bus.Subscribe(484))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(484))
		actorRef ! (Bus.Publish(484, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(484, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(484))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(484))
		expectMsg(Bus.Error)

	}

	"The System-under-test (485)" should "AUTO-GEN-TEST (485)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(485))
		actorRef ! (Bus.Subscribe(485))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(485))
		actorRef ! (Bus.Publish(485, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(485))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(485))
		actorRef ! (Bus.Connect(485))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(485))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(485))
		actorRef ! (Bus.Unsubscribe(485))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(485))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(485))
		expectMsg(Bus.Error)

	}

	"The System-under-test (486)" should "AUTO-GEN-TEST (486)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(486))
		actorRef ! (Bus.UnsubscribeCallback(486))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(486))
		actorRef ! (Bus.CreateConnection(486))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(486))
		actorRef ! (Bus.Connect(486))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(486))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(486))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(486))
		expectMsg(Bus.Error)

	}

	"The System-under-test (487)" should "AUTO-GEN-TEST (487)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(487))
		actorRef ! (Bus.UnsubscribeCallback(487))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(487))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(487))
		expectMsg(Bus.Success)

	}

	"The System-under-test (488)" should "AUTO-GEN-TEST (488)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(488))
		actorRef ! (Bus.Unsubscribe(488))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(488))
		actorRef ! (Bus.UnsubscribeCallback(488))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(488))
		actorRef ! (Bus.Connect(488))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(488))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(488))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(488))
		expectMsg(Bus.Error)

	}

	"The System-under-test (489)" should "AUTO-GEN-TEST (489)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(489))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(489))
		actorRef ! (Bus.Unsubscribe(489))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(489))
		actorRef ! (Bus.Publish(489, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(489))
		actorRef ! (Bus.Subscribe(489))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(489))
		expectMsg(Bus.Success)

	}

	"The System-under-test (490)" should "AUTO-GEN-TEST (490)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(490))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(490))
		actorRef ! (Bus.Subscribe(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(490))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(490))
		actorRef ! (Bus.UnsubscribeCallback(490))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(490))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(490))
		actorRef ! (Bus.Disconnect(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(490))
		actorRef ! (Bus.CreateConnection(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(490))
		actorRef ! (Bus.Disconnect(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(490))
		actorRef ! (Bus.UnsubscribeCallback(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(490))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(490))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(490))
		actorRef ! (Bus.Unsubscribe(490))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(490))
		expectMsg(Bus.Error)

	}

	"The System-under-test (491)" should "AUTO-GEN-TEST (491)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(491))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(491))
		actorRef ! (Bus.SubscribeCallback(491))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(491))
		actorRef ! (Bus.SubscribeCallback(491))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(491))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(491))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(491))
		expectMsg(Bus.Error)

	}

	"The System-under-test (492)" should "AUTO-GEN-TEST (492)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(492))
		actorRef ! (Bus.Disconnect(492))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(492))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(492))
		actorRef ! (Bus.Publish(492, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(492))
		actorRef ! (Bus.Disconnect(492))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(492))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(492))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(492))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(492))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(492, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(492, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(492))
		expectMsg(Bus.Error)

	}

	"The System-under-test (493)" should "AUTO-GEN-TEST (493)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(493))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(493))
		actorRef ! (Bus.DestroyConnection(493))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(493, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(493))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(493))
		expectMsg(Bus.Error)

	}

	"The System-under-test (494)" should "AUTO-GEN-TEST (494)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(494))
		actorRef ! (Bus.DestroyConnection(494))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(494))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(494))
		actorRef ! (Bus.UnsubscribeCallback(494))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(494))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(494))
		expectMsg(Bus.Error)

	}

	"The System-under-test (495)" should "AUTO-GEN-TEST (495)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(495))
		actorRef ! (Bus.Subscribe(495))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(495))
		actorRef ! (Bus.CreateConnection(495))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(495))
		actorRef ! (Bus.Subscribe(495))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(495, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(495))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(495))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(495))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(495))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(495))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(495))
		expectMsg(Bus.Error)

	}

	"The System-under-test (496)" should "AUTO-GEN-TEST (496)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(496))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(496, "foo"))
		actorRef ! (Bus.Disconnect(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(496))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(496))
		actorRef ! (Bus.SubscribeCallback(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(496))
		actorRef ! (Bus.Publish(496, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(496))
		actorRef ! (Bus.Publish(496, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(496, "foo"))
		actorRef ! (Bus.Subscribe(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(496))
		actorRef ! (Bus.SubscribeCallback(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(496))
		actorRef ! (Bus.DestroyConnection(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(496))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(496))
		actorRef ! (Bus.DestroyConnection(496))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(496))
		expectMsg(Bus.Error)

	}

	"The System-under-test (497)" should "AUTO-GEN-TEST (497)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(497))
		actorRef ! (Bus.Unsubscribe(497))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(497))
		actorRef ! (Bus.Subscribe(497))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(497))
		actorRef ! (Bus.Connect(497))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(497))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(497))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(497))
		actorRef ! (Bus.Connect(497))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(497))
		actorRef ! (Bus.Publish(497, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(497))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(497))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(497))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(497, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (498)" should "AUTO-GEN-TEST (498)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(498))
		actorRef ! (Bus.Disconnect(498))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(498))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(498))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(498))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(498))
		actorRef ! (Bus.DestroyConnection(498))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(498))
		actorRef ! (Bus.UnsubscribeCallback(498))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(498))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(498))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(498))
		actorRef ! (Bus.UnsubscribeCallback(498))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(498))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(498))
		actorRef ! (Bus.DestroyConnection(498))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (499)" should "AUTO-GEN-TEST (499)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(499))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(499))
		actorRef ! (Bus.Unsubscribe(499))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(499))
		actorRef ! (Bus.Disconnect(499))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(499))
		actorRef ! (Bus.Disconnect(499))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(499))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(499))
		actorRef ! (Bus.Connect(499))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(499))
		actorRef ! (Bus.Unsubscribe(499))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(499, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(499))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(499))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(499))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(499))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(499))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(499))
		expectMsg(Bus.Error)

	}

	"The System-under-test (500)" should "AUTO-GEN-TEST (500)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(500))
		actorRef ! (Bus.Publish(500, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(500, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(500, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(500))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(500, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(500))
		expectMsg(Bus.Error)

	}

	"The System-under-test (501)" should "AUTO-GEN-TEST (501)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(501))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(501))
		actorRef ! (Bus.Unsubscribe(501))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(501))
		actorRef ! (Bus.CreateConnection(501))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(501))
		actorRef ! (Bus.Disconnect(501))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(501))
		actorRef ! (Bus.DestroyConnection(501))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(501))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(501))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(501))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(501))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(501, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(501))
		expectMsg(Bus.Error)

	}

	"The System-under-test (502)" should "AUTO-GEN-TEST (502)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(502))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(502))
		actorRef ! (Bus.Disconnect(502))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(502))
		actorRef ! (Bus.Disconnect(502))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(502))
		actorRef ! (Bus.Publish(502, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(502))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(502))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(502, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(502))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(502))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(502))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(502))
		expectMsg(Bus.Error)

	}

	"The System-under-test (503)" should "AUTO-GEN-TEST (503)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(503))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(503))
		actorRef ! (Bus.CreateConnection(503))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(503))
		actorRef ! (Bus.CreateConnection(503))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(503))
		actorRef ! (Bus.SubscribeCallback(503))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(503))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(503))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(503))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(503))
		expectMsg(Bus.Error)

	}

	"The System-under-test (504)" should "AUTO-GEN-TEST (504)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(504))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(504))
		actorRef ! (Bus.DestroyConnection(504))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(504, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(504, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(504))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(504, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(504, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (505)" should "AUTO-GEN-TEST (505)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(505))
		actorRef ! (Bus.Publish(505, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(505, "foo"))
		actorRef ! (Bus.Subscribe(505))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(505))
		actorRef ! (Bus.Subscribe(505))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(505))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(505))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(505))
		expectMsg(Bus.Error)

	}

	"The System-under-test (506)" should "AUTO-GEN-TEST (506)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(506))
		expectMsg(Bus.Success)

	}

	"The System-under-test (507)" should "AUTO-GEN-TEST (507)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(507))
		actorRef ! (Bus.Connect(507))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(507))
		actorRef ! (Bus.CreateConnection(507))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(507))
		actorRef ! (Bus.Publish(507, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(507))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(507))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(507))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(507))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(507))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(507))
		expectMsg(Bus.Error)

	}

	"The System-under-test (508)" should "AUTO-GEN-TEST (508)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(508))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(508))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(508))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(508, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (509)" should "AUTO-GEN-TEST (509)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(509))
		actorRef ! (Bus.Disconnect(509))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(509))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(509))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(509))
		actorRef ! (Bus.UnsubscribeCallback(509))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(509))
		actorRef ! (Bus.DestroyConnection(509))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(509))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(509))
		expectMsg(Bus.Error)

	}

	"The System-under-test (510)" should "AUTO-GEN-TEST (510)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(510))
		actorRef ! (Bus.Disconnect(510))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(510))
		actorRef ! (Bus.Publish(510, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(510))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(510, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(510))
		actorRef ! (Bus.Unsubscribe(510))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(510))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(510))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(510))
		expectMsg(Bus.Error)

	}

	"The System-under-test (511)" should "AUTO-GEN-TEST (511)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(511))
		actorRef ! (Bus.Unsubscribe(511))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(511))
		actorRef ! (Bus.SubscribeCallback(511))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(511, "foo"))
		actorRef ! (Bus.Disconnect(511))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(511))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(511))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(511, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(511, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(511))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(511))
		expectMsg(Bus.Error)

	}

	"The System-under-test (512)" should "AUTO-GEN-TEST (512)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(512))
		actorRef ! (Bus.Disconnect(512))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(512))
		actorRef ! (Bus.Publish(512, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(512))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(512))
		expectMsg(Bus.Error)

	}

	"The System-under-test (513)" should "AUTO-GEN-TEST (513)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(513))
		actorRef ! (Bus.Subscribe(513))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(513))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(513, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(513))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(513))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(513))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(513))
		expectMsg(Bus.Error)

	}

	"The System-under-test (514)" should "AUTO-GEN-TEST (514)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(514))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(514))
		actorRef ! (Bus.SubscribeCallback(514))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(514))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(514))
		actorRef ! (Bus.Disconnect(514))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(514, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(514))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(514))
		actorRef ! (Bus.CreateConnection(514))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(514))
		actorRef ! (Bus.UnsubscribeCallback(514))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(514))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(514))
		actorRef ! (Bus.Subscribe(514))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(514))
		actorRef ! (Bus.Disconnect(514))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(514))
		actorRef ! (Bus.Publish(514, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(514))
		expectMsg(Bus.Error)

	}

	"The System-under-test (515)" should "AUTO-GEN-TEST (515)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(515))
		actorRef ! (Bus.Connect(515))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(515, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(515))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(515))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(515))
		expectMsg(Bus.Error)

	}

	"The System-under-test (516)" should "AUTO-GEN-TEST (516)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(516))
		actorRef ! (Bus.DestroyConnection(516))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(516))
		actorRef ! (Bus.UnsubscribeCallback(516))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(516))
		actorRef ! (Bus.SubscribeCallback(516))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(516))
		actorRef ! (Bus.Connect(516))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(516))
		actorRef ! (Bus.Publish(516, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(516))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(516))
		actorRef ! (Bus.SubscribeCallback(516))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(516))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(516))
		expectMsg(Bus.Error)

	}

	"The System-under-test (517)" should "AUTO-GEN-TEST (517)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(517))
		actorRef ! (Bus.Subscribe(517))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(517))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(517))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(517))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(517))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(517, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(517))
		expectMsg(Bus.Error)

	}

	"The System-under-test (518)" should "AUTO-GEN-TEST (518)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(518))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(518))
		actorRef ! (Bus.CreateConnection(518))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(518, "foo"))
		actorRef ! (Bus.Connect(518))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(518))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(518))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(518, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(518))
		expectMsg(Bus.Error)

	}

	"The System-under-test (519)" should "AUTO-GEN-TEST (519)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(519))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(519, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(519))
		actorRef ! (Bus.Unsubscribe(519))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(519))
		actorRef ! (Bus.Subscribe(519))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(519, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(519, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(519))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(519))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(519))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(519))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(519))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(519))
		expectMsg(Bus.Error)

	}

	"The System-under-test (520)" should "AUTO-GEN-TEST (520)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(520))
		actorRef ! (Bus.Publish(520, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(520))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(520))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(520))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(520, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(520))
		expectMsg(Bus.Error)

	}

	"The System-under-test (521)" should "AUTO-GEN-TEST (521)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(521))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(521, "foo"))
		actorRef ! (Bus.Publish(521, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(521, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (522)" should "AUTO-GEN-TEST (522)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(522))
		actorRef ! (Bus.SubscribeCallback(522))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(522))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(522))
		actorRef ! (Bus.Unsubscribe(522))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(522))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(522))
		actorRef ! (Bus.Unsubscribe(522))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(522))
		actorRef ! (Bus.DestroyConnection(522))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(522))
		actorRef ! (Bus.Connect(522))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(522))
		actorRef ! (Bus.Publish(522, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(522))
		actorRef ! (Bus.UnsubscribeCallback(522))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(522))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(522))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(522))
		expectMsg(Bus.Error)

	}

	"The System-under-test (523)" should "AUTO-GEN-TEST (523)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(523))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(523))
		actorRef ! (Bus.DestroyConnection(523))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(523, "foo"))
		actorRef ! (Bus.CreateConnection(523))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(523))
		actorRef ! (Bus.SubscribeCallback(523))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(523))
		actorRef ! (Bus.Subscribe(523))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(523))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(523))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(523))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(523))
		expectMsg(Bus.Error)

	}

	"The System-under-test (524)" should "AUTO-GEN-TEST (524)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(524))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(524))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(524))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(524))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(524, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(524))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(524))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(524, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (525)" should "AUTO-GEN-TEST (525)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(525))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(525))
		actorRef ! (Bus.SubscribeCallback(525))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(525))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(525))
		actorRef ! (Bus.Connect(525))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(525))
		actorRef ! (Bus.Disconnect(525))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(525, "foo"))
		actorRef ! (Bus.Subscribe(525))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(525))
		actorRef ! (Bus.CreateConnection(525))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(525))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(525))
		expectMsg(Bus.Success)

	}

	"The System-under-test (526)" should "AUTO-GEN-TEST (526)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(526))
		actorRef ! (Bus.Publish(526, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(526))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(526))
		actorRef ! (Bus.CreateConnection(526))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(526))
		actorRef ! (Bus.Connect(526))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(526))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(526, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(526, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (527)" should "AUTO-GEN-TEST (527)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(527))
		actorRef ! (Bus.Subscribe(527))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(527))
		actorRef ! (Bus.CreateConnection(527))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(527))
		actorRef ! (Bus.UnsubscribeCallback(527))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(527))
		actorRef ! (Bus.DestroyConnection(527))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(527))
		actorRef ! (Bus.Disconnect(527))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(527))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(527))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(527))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(527, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(527))
		expectMsg(Bus.Error)

	}

	"The System-under-test (528)" should "AUTO-GEN-TEST (528)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(528))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(528))
		actorRef ! (Bus.UnsubscribeCallback(528))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(528))
		actorRef ! (Bus.Connect(528))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(528, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(528))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(528))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(528))
		expectMsg(Bus.Error)

	}

	"The System-under-test (529)" should "AUTO-GEN-TEST (529)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(529))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(529))
		actorRef ! (Bus.Publish(529, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(529))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(529))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(529))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(529))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(529))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(529))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(529, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (530)" should "AUTO-GEN-TEST (530)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(530))
		actorRef ! (Bus.DestroyConnection(530))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(530, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(530))
		actorRef ! (Bus.Subscribe(530))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(530, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(530))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(530))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(530))
		expectMsg(Bus.Error)

	}

	"The System-under-test (531)" should "AUTO-GEN-TEST (531)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(531))
		actorRef ! (Bus.Unsubscribe(531))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(531))
		actorRef ! (Bus.SubscribeCallback(531))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(531))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(531))
		actorRef ! (Bus.SubscribeCallback(531))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(531))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(531))
		actorRef ! (Bus.CreateConnection(531))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(531))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(531))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(531))
		expectMsg(Bus.Error)

	}

	"The System-under-test (532)" should "AUTO-GEN-TEST (532)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(532))
		actorRef ! (Bus.Connect(532))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(532))
		actorRef ! (Bus.Connect(532))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(532))
		actorRef ! (Bus.Publish(532, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(532))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(532))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(532))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(532))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(532))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(532))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(532))
		expectMsg(Bus.Success)

	}

	"The System-under-test (533)" should "AUTO-GEN-TEST (533)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(533))
		actorRef ! (Bus.Subscribe(533))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(533))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(533))
		actorRef ! (Bus.CreateConnection(533))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(533))
		actorRef ! (Bus.SubscribeCallback(533))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(533))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(533))
		expectMsg(Bus.Error)

	}

	"The System-under-test (534)" should "AUTO-GEN-TEST (534)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(534))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(534))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(534))
		expectMsg(Bus.Error)

	}

	"The System-under-test (535)" should "AUTO-GEN-TEST (535)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(535))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(535, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(535))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(535))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(535))
		expectMsg(Bus.Error)

	}

	"The System-under-test (536)" should "AUTO-GEN-TEST (536)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(536))
		actorRef ! (Bus.Disconnect(536))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(536))
		actorRef ! (Bus.DestroyConnection(536))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(536))
		actorRef ! (Bus.SubscribeCallback(536))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(536))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(536))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(536))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(536, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (537)" should "AUTO-GEN-TEST (537)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(537))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(537))
		actorRef ! (Bus.CreateConnection(537))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(537))
		actorRef ! (Bus.Connect(537))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(537))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(537))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(537))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(537))
		actorRef ! (Bus.CreateConnection(537))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(537))
		actorRef ! (Bus.Disconnect(537))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(537, "foo"))
		actorRef ! (Bus.Disconnect(537))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(537, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(537))
		actorRef ! (Bus.Publish(537, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(537))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(537))
		expectMsg(Bus.Error)

	}

	"The System-under-test (538)" should "AUTO-GEN-TEST (538)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(538))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(538))
		actorRef ! (Bus.DestroyConnection(538))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(538, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(538))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(538))
		actorRef ! (Bus.Publish(538, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(538))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(538))
		actorRef ! (Bus.Unsubscribe(538))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(538))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(538))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(538, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(538))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(538))
		expectMsg(Bus.Error)

	}

	"The System-under-test (539)" should "AUTO-GEN-TEST (539)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(539))
		actorRef ! (Bus.DestroyConnection(539))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(539, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(539, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(539))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(539))
		actorRef ! (Bus.Publish(539, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(539))
		actorRef ! (Bus.Disconnect(539))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(539))
		actorRef ! (Bus.UnsubscribeCallback(539))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(539))
		actorRef ! (Bus.SubscribeCallback(539))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(539))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(539))
		expectMsg(Bus.Error)

	}

	"The System-under-test (540)" should "AUTO-GEN-TEST (540)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(540))
		actorRef ! (Bus.DestroyConnection(540))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(540, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(540))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(540))
		actorRef ! (Bus.Publish(540, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(540))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(540, "foo"))
		actorRef ! (Bus.Subscribe(540))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(540))
		actorRef ! (Bus.CreateConnection(540))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(540))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(540))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(540))
		actorRef ! (Bus.UnsubscribeCallback(540))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(540))
		actorRef ! (Bus.UnsubscribeCallback(540))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(540))
		actorRef ! (Bus.CreateConnection(540))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(540))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(540))
		expectMsg(Bus.Error)

	}

	"The System-under-test (541)" should "AUTO-GEN-TEST (541)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(541))
		actorRef ! (Bus.CreateConnection(541))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(541))
		actorRef ! (Bus.SubscribeCallback(541))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(541, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(541))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(541))
		expectMsg(Bus.Success)

	}

	"The System-under-test (542)" should "AUTO-GEN-TEST (542)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(542))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(542))
		actorRef ! (Bus.SubscribeCallback(542))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(542))
		actorRef ! (Bus.Disconnect(542))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(542))
		actorRef ! (Bus.Publish(542, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(542))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(542))
		actorRef ! (Bus.Subscribe(542))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(542))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(542))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(542))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(542))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(542))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(542, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(542))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(542))
		expectMsg(Bus.Error)

	}

	"The System-under-test (543)" should "AUTO-GEN-TEST (543)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(543))
		actorRef ! (Bus.DestroyConnection(543))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(543))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(543, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(543))
		expectMsg(Bus.Error)

	}

	"The System-under-test (544)" should "AUTO-GEN-TEST (544)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(544))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(544))
		actorRef ! (Bus.UnsubscribeCallback(544))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(544))
		actorRef ! (Bus.Publish(544, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(544, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(544))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(544))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(544, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (545)" should "AUTO-GEN-TEST (545)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(545))
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(545, "foo"))
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(545))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(545))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(545, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(545))
		expectMsg(Bus.Success)

	}

	"The System-under-test (546)" should "AUTO-GEN-TEST (546)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(546))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(546))
		actorRef ! (Bus.Publish(546, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(546))
		actorRef ! (Bus.Subscribe(546))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(546))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(546, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(546, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(546))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(546))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(546))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(546))
		expectMsg(Bus.Error)

	}

	"The System-under-test (547)" should "AUTO-GEN-TEST (547)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(547))
		actorRef ! (Bus.Subscribe(547))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(547))
		actorRef ! (Bus.Subscribe(547))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(547))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(547))
		actorRef ! (Bus.Disconnect(547))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(547))
		actorRef ! (Bus.SubscribeCallback(547))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(547))
		actorRef ! (Bus.CreateConnection(547))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(547))
		actorRef ! (Bus.Publish(547, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(547, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (548)" should "AUTO-GEN-TEST (548)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(548))
		actorRef ! (Bus.Connect(548))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(548))
		actorRef ! (Bus.Disconnect(548))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(548))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(548, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (549)" should "AUTO-GEN-TEST (549)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(549))
		actorRef ! (Bus.Publish(549, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(549))
		actorRef ! (Bus.Publish(549, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(549))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(549))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(549))
		actorRef ! (Bus.Disconnect(549))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(549))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(549))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(549))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(549))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(549))
		actorRef ! (Bus.Unsubscribe(549))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(549))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(549))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(549, "foo"))
		actorRef ! (Bus.Publish(549, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(549))
		actorRef ! (Bus.Unsubscribe(549))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (550)" should "AUTO-GEN-TEST (550)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(550))
		actorRef ! (Bus.Unsubscribe(550))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(550))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(550))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(550))
		actorRef ! (Bus.Subscribe(550))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(550))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(550))
		actorRef ! (Bus.CreateConnection(550))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(550))
		actorRef ! (Bus.Publish(550, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(550, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(550))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(550))
		expectMsg(Bus.Error)

	}

	"The System-under-test (551)" should "AUTO-GEN-TEST (551)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(551))
		actorRef ! (Bus.DestroyConnection(551))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(551))
		actorRef ! (Bus.SubscribeCallback(551))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(551))
		actorRef ! (Bus.Disconnect(551))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(551))
		actorRef ! (Bus.Connect(551))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(551, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(551))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(551))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(551))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(551))
		expectMsg(Bus.Error)

	}

	"The System-under-test (552)" should "AUTO-GEN-TEST (552)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(552))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(552))
		actorRef ! (Bus.CreateConnection(552))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(552))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(552, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(552, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(552))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(552, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (553)" should "AUTO-GEN-TEST (553)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(553))
		actorRef ! (Bus.Publish(553, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(553))
		actorRef ! (Bus.Disconnect(553))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(553))
		actorRef ! (Bus.Connect(553))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(553))
		actorRef ! (Bus.UnsubscribeCallback(553))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(553, "foo"))
		actorRef ! (Bus.Publish(553, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(553))
		actorRef ! (Bus.UnsubscribeCallback(553))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(553))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(553))
		actorRef ! (Bus.Unsubscribe(553))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(553, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(553))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(553))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(553))
		expectMsg(Bus.Error)

	}

	"The System-under-test (554)" should "AUTO-GEN-TEST (554)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(554))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(554))
		expectMsg(Bus.Error)

	}

	"The System-under-test (555)" should "AUTO-GEN-TEST (555)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(555))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(555))
		actorRef ! (Bus.Connect(555))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(555))
		actorRef ! (Bus.Publish(555, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(555, "foo"))
		actorRef ! (Bus.Subscribe(555))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(555))
		actorRef ! (Bus.SubscribeCallback(555))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(555))
		actorRef ! (Bus.Connect(555))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(555))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(555))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(555))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(555))
		expectMsg(Bus.Error)

	}

	"The System-under-test (556)" should "AUTO-GEN-TEST (556)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(556))
		actorRef ! (Bus.Subscribe(556))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(556))
		actorRef ! (Bus.SubscribeCallback(556))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(556))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(556, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(556))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(556))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(556))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(556))
		expectMsg(Bus.Error)

	}

	"The System-under-test (557)" should "AUTO-GEN-TEST (557)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(557))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(557))
		actorRef ! (Bus.CreateConnection(557))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(557))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(557, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(557))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(557))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(557, "foo"))
		actorRef ! (Bus.Publish(557, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(557))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(557))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(557))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(557))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(557))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(557, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(557))
		expectMsg(Bus.Success)

	}

	"The System-under-test (558)" should "AUTO-GEN-TEST (558)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(558))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(558))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(558, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(558, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(558))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(558))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(558))
		actorRef ! (Bus.Disconnect(558))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(558, "foo"))
		actorRef ! (Bus.Disconnect(558))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(558))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(558))
		actorRef ! (Bus.UnsubscribeCallback(558))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(558))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(558))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(558))
		actorRef ! (Bus.UnsubscribeCallback(558))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(558))
		actorRef ! (Bus.Connect(558))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(558))
		expectMsg(Bus.Error)

	}

	"The System-under-test (559)" should "AUTO-GEN-TEST (559)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(559))
		actorRef ! (Bus.Subscribe(559))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(559))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(559))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(559))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(559))
		expectMsg(Bus.Success)

	}

	"The System-under-test (560)" should "AUTO-GEN-TEST (560)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(560))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(560, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(560))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(560))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(560))
		expectMsg(Bus.Error)

	}

	"The System-under-test (561)" should "AUTO-GEN-TEST (561)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(561))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(561))
		actorRef ! (Bus.DestroyConnection(561))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(561))
		actorRef ! (Bus.DestroyConnection(561))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(561))
		actorRef ! (Bus.Publish(561, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(561))
		actorRef ! (Bus.SubscribeCallback(561))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(561, "foo"))
		actorRef ! (Bus.SubscribeCallback(561))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(561))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(561))
		expectMsg(Bus.Error)

	}

	"The System-under-test (562)" should "AUTO-GEN-TEST (562)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(562))
		actorRef ! (Bus.Subscribe(562))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(562))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(562))
		actorRef ! (Bus.SubscribeCallback(562))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(562))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(562))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(562))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(562))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(562))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(562))
		expectMsg(Bus.Error)

	}

	"The System-under-test (563)" should "AUTO-GEN-TEST (563)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(563))
		actorRef ! (Bus.SubscribeCallback(563))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(563, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(563))
		actorRef ! (Bus.DestroyConnection(563))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(563))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(563))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(563))
		expectMsg(Bus.Success)

	}

	"The System-under-test (564)" should "AUTO-GEN-TEST (564)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(564))
		actorRef ! (Bus.CreateConnection(564))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(564))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(564))
		actorRef ! (Bus.CreateConnection(564))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(564, "foo"))
		actorRef ! (Bus.Publish(564, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(564))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(564))
		actorRef ! (Bus.Connect(564))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(564))
		actorRef ! (Bus.Subscribe(564))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(564))
		actorRef ! (Bus.Connect(564))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(564, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(564))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(564))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(564, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(564))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(564))
		expectMsg(Bus.Error)

	}

	"The System-under-test (565)" should "AUTO-GEN-TEST (565)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(565))
		actorRef ! (Bus.Publish(565, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(565, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(565))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(565))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(565))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(565))
		expectMsg(Bus.Success)

	}

	"The System-under-test (566)" should "AUTO-GEN-TEST (566)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(566))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(566))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(566))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(566))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(566, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (567)" should "AUTO-GEN-TEST (567)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(567))
		actorRef ! (Bus.DestroyConnection(567))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(567))
		actorRef ! (Bus.Unsubscribe(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(567))
		actorRef ! (Bus.Publish(567, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(567))
		actorRef ! (Bus.Unsubscribe(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(567))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(567))
		actorRef ! (Bus.CreateConnection(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(567))
		actorRef ! (Bus.SubscribeCallback(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(567))
		actorRef ! (Bus.Subscribe(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(567, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(567))
		actorRef ! (Bus.UnsubscribeCallback(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(567))
		actorRef ! (Bus.SubscribeCallback(567))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (568)" should "AUTO-GEN-TEST (568)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(568))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(568))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(568))
		actorRef ! (Bus.CreateConnection(568))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(568))
		actorRef ! (Bus.DestroyConnection(568))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(568))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(568))
		expectMsg(Bus.Success)

	}

	"The System-under-test (569)" should "AUTO-GEN-TEST (569)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(569))
		actorRef ! (Bus.Connect(569))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(569))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(569))
		actorRef ! (Bus.SubscribeCallback(569))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(569))
		actorRef ! (Bus.Disconnect(569))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(569))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(569))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(569, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(569, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (570)" should "AUTO-GEN-TEST (570)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(570))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(570))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(570))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(570))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(570))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(570))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(570))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(570))
		expectMsg(Bus.Error)

	}

	"The System-under-test (571)" should "AUTO-GEN-TEST (571)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(571))
		actorRef ! (Bus.SubscribeCallback(571))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(571))
		expectMsg(Bus.Error)

	}

	"The System-under-test (572)" should "AUTO-GEN-TEST (572)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(572))
		actorRef ! (Bus.SubscribeCallback(572))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(572, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(572))
		actorRef ! (Bus.SubscribeCallback(572))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(572))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(572))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(572))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(572))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(572))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(572))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(572))
		expectMsg(Bus.Success)

	}

	"The System-under-test (573)" should "AUTO-GEN-TEST (573)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(573))
		actorRef ! (Bus.Connect(573))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(573))
		actorRef ! (Bus.Disconnect(573))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(573, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(573, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(573))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(573))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(573))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(573))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(573, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (574)" should "AUTO-GEN-TEST (574)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(574))
		actorRef ! (Bus.Unsubscribe(574))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(574, "foo"))
		actorRef ! (Bus.Disconnect(574))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(574))
		actorRef ! (Bus.Publish(574, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(574))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(574))
		expectMsg(Bus.Error)

	}

	"The System-under-test (575)" should "AUTO-GEN-TEST (575)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(575))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(575))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(575))
		actorRef ! (Bus.SubscribeCallback(575))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(575))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(575))
		actorRef ! (Bus.SubscribeCallback(575))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(575))
		expectMsg(Bus.Error)

	}

	"The System-under-test (576)" should "AUTO-GEN-TEST (576)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(576))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(576))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(576))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(576))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(576))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(576, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(576, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(576))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(576))
		expectMsg(Bus.Error)

	}

	"The System-under-test (577)" should "AUTO-GEN-TEST (577)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(577))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(577))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(577, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(577))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(577))
		expectMsg(Bus.Error)

	}

	"The System-under-test (578)" should "AUTO-GEN-TEST (578)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(578))
		actorRef ! (Bus.DestroyConnection(578))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(578))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(578, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(578, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(578))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(578))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(578))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(578))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(578, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (579)" should "AUTO-GEN-TEST (579)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(579))
		actorRef ! (Bus.CreateConnection(579))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(579))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(579))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(579))
		actorRef ! (Bus.UnsubscribeCallback(579))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(579))
		actorRef ! (Bus.DestroyConnection(579))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(579))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(579))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(579, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(579))
		expectMsg(Bus.Error)

	}

	"The System-under-test (580)" should "AUTO-GEN-TEST (580)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(580))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(580, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(580, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(580, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(580))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(580))
		expectMsg(Bus.Error)

	}

	"The System-under-test (581)" should "AUTO-GEN-TEST (581)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(581))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(581))
		actorRef ! (Bus.SubscribeCallback(581))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(581))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(581))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(581))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(581))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(581))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(581, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (582)" should "AUTO-GEN-TEST (582)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(582))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(582))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(582))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(582))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(582))
		expectMsg(Bus.Success)

	}

	"The System-under-test (583)" should "AUTO-GEN-TEST (583)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(583))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(583))
		actorRef ! (Bus.CreateConnection(583))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(583))
		actorRef ! (Bus.SubscribeCallback(583))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(583))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(583, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(583, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (584)" should "AUTO-GEN-TEST (584)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(584))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(584, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(584))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(584))
		expectMsg(Bus.Error)

	}

	"The System-under-test (585)" should "AUTO-GEN-TEST (585)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(585))
		actorRef ! (Bus.Unsubscribe(585))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(585))
		actorRef ! (Bus.Unsubscribe(585))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(585))
		actorRef ! (Bus.Connect(585))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(585))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(585, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(585))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(585))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(585))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(585, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (586)" should "AUTO-GEN-TEST (586)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(586))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(586))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(586, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(586))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(586))
		expectMsg(Bus.Success)

	}

	"The System-under-test (587)" should "AUTO-GEN-TEST (587)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(587))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(587, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(587))
		actorRef ! (Bus.SubscribeCallback(587))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(587))
		actorRef ! (Bus.UnsubscribeCallback(587))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(587))
		actorRef ! (Bus.UnsubscribeCallback(587))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(587))
		actorRef ! (Bus.Publish(587, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(587))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(587))
		actorRef ! (Bus.Subscribe(587))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(587))
		actorRef ! (Bus.Publish(587, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (588)" should "AUTO-GEN-TEST (588)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(588))
		actorRef ! (Bus.Publish(588, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(588))
		actorRef ! (Bus.DestroyConnection(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(588))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(588))
		actorRef ! (Bus.CreateConnection(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(588))
		actorRef ! (Bus.DestroyConnection(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(588))
		actorRef ! (Bus.CreateConnection(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(588))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(588))
		actorRef ! (Bus.CreateConnection(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(588))
		actorRef ! (Bus.Subscribe(588))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(588))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(588))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(588))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(588))
		expectMsg(Bus.Error)

	}

	"The System-under-test (589)" should "AUTO-GEN-TEST (589)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(589))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(589))
		actorRef ! (Bus.UnsubscribeCallback(589))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(589))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(589))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(589))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(589, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(589))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(589))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(589))
		expectMsg(Bus.Success)

	}

	"The System-under-test (590)" should "AUTO-GEN-TEST (590)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(590))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(590))
		actorRef ! (Bus.UnsubscribeCallback(590))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(590))
		actorRef ! (Bus.Subscribe(590))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(590))
		actorRef ! (Bus.Unsubscribe(590))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(590))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(590))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(590))
		expectMsg(Bus.Error)

	}

	"The System-under-test (591)" should "AUTO-GEN-TEST (591)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(591))
		actorRef ! (Bus.Disconnect(591))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(591, "foo"))
		actorRef ! (Bus.Unsubscribe(591))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(591))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(591))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(591))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(591))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(591))
		actorRef ! (Bus.UnsubscribeCallback(591))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(591))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(591))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(591))
		actorRef ! (Bus.Disconnect(591))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(591))
		actorRef ! (Bus.Connect(591))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)

	}

	"The System-under-test (592)" should "AUTO-GEN-TEST (592)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(592))
		actorRef ! (Bus.Subscribe(592))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(592))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(592))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(592))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(592))
		expectMsg(Bus.Error)

	}

	"The System-under-test (593)" should "AUTO-GEN-TEST (593)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(593, "foo"))
		actorRef ! (Bus.CreateConnection(593))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(593, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(593, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(593))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(593))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(593, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (594)" should "AUTO-GEN-TEST (594)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(594))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(594, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(594))
		actorRef ! (Bus.DestroyConnection(594))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(594))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(594))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(594, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(594))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(594))
		expectMsg(Bus.Error)

	}

	"The System-under-test (595)" should "AUTO-GEN-TEST (595)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(595))
		actorRef ! (Bus.CreateConnection(595))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(595))
		actorRef ! (Bus.Unsubscribe(595))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(595))
		actorRef ! (Bus.UnsubscribeCallback(595))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(595))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(595, "foo"))
		actorRef ! (Bus.Publish(595, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(595))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(595))
		expectMsg(Bus.Error)

	}

	"The System-under-test (596)" should "AUTO-GEN-TEST (596)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(596))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(596))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(596))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(596))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(596))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(596))
		expectMsg(Bus.Success)

	}

	"The System-under-test (597)" should "AUTO-GEN-TEST (597)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(597))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(597))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(597))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(597))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(597))
		expectMsg(Bus.Error)

	}

	"The System-under-test (598)" should "AUTO-GEN-TEST (598)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(598))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(598))
		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(598))
		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(598, "foo"))
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(598))
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(598))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(598))
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(598))
		expectMsg(Bus.Error)

	}

	"The System-under-test (599)" should "AUTO-GEN-TEST (599)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(599))
		actorRef ! (Bus.Subscribe(599))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(599))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(599))
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(599))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(599))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(599))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(599))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(599))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(599, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (600)" should "AUTO-GEN-TEST (600)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(600))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(600))
		actorRef ! (Bus.SubscribeCallback(600))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(600))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(600))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(600))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(600))
		actorRef ! (Bus.Connect(600))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(600))
		actorRef ! (Bus.DestroyConnection(600))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(600))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(600, "foo"))
		actorRef ! (Bus.CreateConnection(600))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(600))
		actorRef ! (Bus.UnsubscribeCallback(600))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(600, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(600))
		actorRef ! (Bus.UnsubscribeCallback(600))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(600))
		actorRef ! (Bus.Unsubscribe(600))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(600))
		expectMsg(Bus.Error)

	}

	"The System-under-test (601)" should "AUTO-GEN-TEST (601)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(601))
		actorRef ! (Bus.DestroyConnection(601))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(601, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(601, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(601, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(601))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(601))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(601))
		expectMsg(Bus.Success)

	}

	"The System-under-test (602)" should "AUTO-GEN-TEST (602)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(602))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(602))
		actorRef ! (Bus.Publish(602, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(602))
		actorRef ! (Bus.CreateConnection(602))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(602))
		actorRef ! (Bus.SubscribeCallback(602))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(602))
		actorRef ! (Bus.Unsubscribe(602))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(602))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(602))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(602))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(602))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(602))
		expectMsg(Bus.Error)

	}

	"The System-under-test (603)" should "AUTO-GEN-TEST (603)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(603))
		actorRef ! (Bus.UnsubscribeCallback(603))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(603))
		actorRef ! (Bus.Subscribe(603))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(603))
		actorRef ! (Bus.Disconnect(603))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(603, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(603))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(603))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(603))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(603, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (604)" should "AUTO-GEN-TEST (604)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(604))
		actorRef ! (Bus.DestroyConnection(604))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(604))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(604))
		expectMsg(Bus.Error)

	}

	"The System-under-test (605)" should "AUTO-GEN-TEST (605)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(605))
		actorRef ! (Bus.CreateConnection(605))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(605, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(605))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(605))
		expectMsg(Bus.Error)

	}

	"The System-under-test (606)" should "AUTO-GEN-TEST (606)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(606))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(606))
		actorRef ! (Bus.SubscribeCallback(606))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(606))
		actorRef ! (Bus.Disconnect(606))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(606))
		actorRef ! (Bus.Publish(606, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(606))
		actorRef ! (Bus.Unsubscribe(606))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(606))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(606, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (607)" should "AUTO-GEN-TEST (607)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(607))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(607))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(607, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(607))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(607))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(607))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(607))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(607))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(607))
		expectMsg(Bus.Error)

	}

	"The System-under-test (608)" should "AUTO-GEN-TEST (608)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(608))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(608))
		actorRef ! (Bus.CreateConnection(608))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(608))
		actorRef ! (Bus.Disconnect(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(608))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(608))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(608))
		actorRef ! (Bus.DestroyConnection(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(608))
		actorRef ! (Bus.SubscribeCallback(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(608))
		actorRef ! (Bus.Publish(608, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(608))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(608))
		actorRef ! (Bus.Unsubscribe(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(608))
		actorRef ! (Bus.DestroyConnection(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(608))
		actorRef ! (Bus.SubscribeCallback(608))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (609)" should "AUTO-GEN-TEST (609)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(609))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(609))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(609))
		actorRef ! (Bus.Unsubscribe(609))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(609))
		actorRef ! (Bus.Publish(609, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(609))
		actorRef ! (Bus.UnsubscribeCallback(609))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(609))
		actorRef ! (Bus.Subscribe(609))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(609))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(609, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(609))
		actorRef ! (Bus.Subscribe(609))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(609, "foo"))
		actorRef ! (Bus.Disconnect(609))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(609))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(609))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(609))
		expectMsg(Bus.Error)

	}

	"The System-under-test (610)" should "AUTO-GEN-TEST (610)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(610))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(610, "foo"))
		actorRef ! (Bus.Publish(610, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(610))
		actorRef ! (Bus.Disconnect(610))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(610))
		actorRef ! (Bus.UnsubscribeCallback(610))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(610))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(610, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(610))
		actorRef ! (Bus.Publish(610, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(610))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(610))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(610))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(610))
		expectMsg(Bus.Error)

	}

	"The System-under-test (611)" should "AUTO-GEN-TEST (611)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(611))
		actorRef ! (Bus.Disconnect(611))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(611, "foo"))
		actorRef ! (Bus.Unsubscribe(611))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(611))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(611))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(611, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(611))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(611, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(611))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(611, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (612)" should "AUTO-GEN-TEST (612)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(612))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(612, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (613)" should "AUTO-GEN-TEST (613)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(613))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(613))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(613, "foo"))
		actorRef ! (Bus.Subscribe(613))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(613))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(613))
		expectMsg(Bus.Error)

	}

	"The System-under-test (614)" should "AUTO-GEN-TEST (614)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(614))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(614, "foo"))
		actorRef ! (Bus.Publish(614, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(614))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(614, "foo"))
		actorRef ! (Bus.Subscribe(614))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(614))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(614))
		expectMsg(Bus.Error)

	}

	"The System-under-test (615)" should "AUTO-GEN-TEST (615)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(615))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(615))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(615))
		expectMsg(Bus.Success)

	}

	"The System-under-test (616)" should "AUTO-GEN-TEST (616)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(616))
		actorRef ! (Bus.Unsubscribe(616))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(616))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(616))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(616))
		expectMsg(Bus.Error)

	}

	"The System-under-test (617)" should "AUTO-GEN-TEST (617)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(617))
		actorRef ! (Bus.CreateConnection(617))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(617))
		actorRef ! (Bus.SubscribeCallback(617))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(617))
		actorRef ! (Bus.Unsubscribe(617))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(617))
		actorRef ! (Bus.UnsubscribeCallback(617))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(617))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(617))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(617))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(617))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(617))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(617, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(617))
		expectMsg(Bus.Error)

	}

	"The System-under-test (618)" should "AUTO-GEN-TEST (618)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(618))
		actorRef ! (Bus.Subscribe(618))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(618))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(618, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(618, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(618, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(618, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(618))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(618))
		expectMsg(Bus.Error)

	}

	"The System-under-test (619)" should "AUTO-GEN-TEST (619)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(619))
		actorRef ! (Bus.UnsubscribeCallback(619))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(619))
		actorRef ! (Bus.SubscribeCallback(619))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(619))
		actorRef ! (Bus.Unsubscribe(619))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(619))
		actorRef ! (Bus.DestroyConnection(619))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(619))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(619))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(619))
		expectMsg(Bus.Success)

	}

	"The System-under-test (620)" should "AUTO-GEN-TEST (620)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(620))
		actorRef ! (Bus.Subscribe(620))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(620, "foo"))
		actorRef ! (Bus.Connect(620))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(620))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(620))
		actorRef ! (Bus.Subscribe(620))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(620))
		actorRef ! (Bus.Disconnect(620))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(620))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(620))
		actorRef ! (Bus.Unsubscribe(620))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(620, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(620))
		expectMsg(Bus.Error)

	}

	"The System-under-test (621)" should "AUTO-GEN-TEST (621)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(621))
		actorRef ! (Bus.SubscribeCallback(621))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(621))
		actorRef ! (Bus.UnsubscribeCallback(621))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(621))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(621))
		actorRef ! (Bus.Disconnect(621))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(621))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(621))
		actorRef ! (Bus.CreateConnection(621))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(621))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(621))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(621))
		actorRef ! (Bus.CreateConnection(621))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(621))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(621))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(621))
		actorRef ! (Bus.Unsubscribe(621))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(621))
		expectMsg(Bus.Error)

	}

	"The System-under-test (622)" should "AUTO-GEN-TEST (622)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(622))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(622))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(622))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(622))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(622))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(622))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(622))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(622))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(622))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(622))
		expectMsg(Bus.Success)

	}

	"The System-under-test (623)" should "AUTO-GEN-TEST (623)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(623))
		actorRef ! (Bus.UnsubscribeCallback(623))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(623))
		actorRef ! (Bus.Subscribe(623))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(623))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(623))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(623))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(623))
		actorRef ! (Bus.SubscribeCallback(623))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(623))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(623))
		actorRef ! (Bus.Subscribe(623))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(623))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(623))
		actorRef ! (Bus.CreateConnection(623))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(623))
		actorRef ! (Bus.Disconnect(623))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(623))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(623))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(623, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (624)" should "AUTO-GEN-TEST (624)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(624))
		actorRef ! (Bus.Subscribe(624))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(624, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(624))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(624, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(624))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(624))
		expectMsg(Bus.Error)

	}

	"The System-under-test (625)" should "AUTO-GEN-TEST (625)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(625))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(625))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(625, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (626)" should "AUTO-GEN-TEST (626)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(626))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(626))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(626))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(626))
		actorRef ! (Bus.Subscribe(626))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(626))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(626))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(626))
		expectMsg(Bus.Error)

	}

	"The System-under-test (627)" should "AUTO-GEN-TEST (627)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(627))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(627))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(627))
		expectMsg(Bus.Error)

	}

	"The System-under-test (628)" should "AUTO-GEN-TEST (628)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(628))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(628))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(628))
		actorRef ! (Bus.CreateConnection(628))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(628))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(628))
		actorRef ! (Bus.UnsubscribeCallback(628))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(628))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(628))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(628, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (629)" should "AUTO-GEN-TEST (629)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(629))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(629, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(629))
		actorRef ! (Bus.Disconnect(629))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(629, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(629))
		actorRef ! (Bus.SubscribeCallback(629))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(629, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(629))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(629))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(629))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(629, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (630)" should "AUTO-GEN-TEST (630)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(630))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(630))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(630))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(630))
		expectMsg(Bus.Error)

	}

	"The System-under-test (631)" should "AUTO-GEN-TEST (631)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(631))
		actorRef ! (Bus.Unsubscribe(631))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(631))
		actorRef ! (Bus.DestroyConnection(631))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (632)" should "AUTO-GEN-TEST (632)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(632))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(632))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(632))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(632, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (633)" should "AUTO-GEN-TEST (633)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(633))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(633, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(633))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(633, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(633, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(633))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(633))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(633))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(633))
		expectMsg(Bus.Error)

	}

	"The System-under-test (634)" should "AUTO-GEN-TEST (634)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(634))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(634))
		actorRef ! (Bus.CreateConnection(634))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(634))
		actorRef ! (Bus.UnsubscribeCallback(634))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(634))
		actorRef ! (Bus.DestroyConnection(634))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(634, "foo"))
		actorRef ! (Bus.Connect(634))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(634))
		actorRef ! (Bus.UnsubscribeCallback(634))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(634))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(634, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(634))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(634, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (635)" should "AUTO-GEN-TEST (635)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(635))
		actorRef ! (Bus.Publish(635, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(635))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(635))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(635))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(635))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(635))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(635))
		expectMsg(Bus.Success)

	}

	"The System-under-test (636)" should "AUTO-GEN-TEST (636)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(636))
		actorRef ! (Bus.Subscribe(636))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(636))
		actorRef ! (Bus.SubscribeCallback(636))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(636))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(636))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(636))
		actorRef ! (Bus.Unsubscribe(636))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(636))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(636))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(636, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(636))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(636))
		actorRef ! (Bus.DestroyConnection(636))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(636, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(636))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(636))
		actorRef ! (Bus.Publish(636, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(636))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(636))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(636))
		expectMsg(Bus.Success)

	}

	"The System-under-test (637)" should "AUTO-GEN-TEST (637)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(637))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(637, "foo"))
		actorRef ! (Bus.Publish(637, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(637))
		actorRef ! (Bus.Disconnect(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(637))
		actorRef ! (Bus.DestroyConnection(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(637))
		actorRef ! (Bus.SubscribeCallback(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(637))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(637))
		actorRef ! (Bus.DestroyConnection(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(637))
		actorRef ! (Bus.DestroyConnection(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(637))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(637))
		actorRef ! (Bus.Unsubscribe(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(637))
		actorRef ! (Bus.Connect(637))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (638)" should "AUTO-GEN-TEST (638)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(638))
		actorRef ! (Bus.DestroyConnection(638))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(638))
		actorRef ! (Bus.Connect(638))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(638))
		actorRef ! (Bus.Publish(638, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(638))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(638))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(638))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(638))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(638))
		expectMsg(Bus.Error)

	}

	"The System-under-test (639)" should "AUTO-GEN-TEST (639)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(639))
		actorRef ! (Bus.Connect(639))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(639))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(639))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(639))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(639))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(639))
		expectMsg(Bus.Error)

	}

	"The System-under-test (640)" should "AUTO-GEN-TEST (640)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(640))
		actorRef ! (Bus.DestroyConnection(640))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(640))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(640))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(640))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(640))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(640))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(640))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(640))
		expectMsg(Bus.Error)

	}

	"The System-under-test (641)" should "AUTO-GEN-TEST (641)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(641))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(641))
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(641))
		actorRef ! (Bus.CreateConnection(641))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(641))
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(641))
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(641))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(641))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(641))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(641))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(641))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(641))
		actorRef ! (Bus.DestroyConnection(641))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(641))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(641))
		expectMsg(Bus.Error)

	}

	"The System-under-test (642)" should "AUTO-GEN-TEST (642)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(642))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(642))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(642))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(642))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(642))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(642))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(642, "foo"))
		actorRef ! (Bus.Publish(642, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(642, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(642))
		actorRef ! (Bus.Connect(642))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(642))
		actorRef ! (Bus.UnsubscribeCallback(642))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(642))
		actorRef ! (Bus.Unsubscribe(642))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(642))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(642))
		actorRef ! (Bus.Subscribe(642))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(642, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(642))
		expectMsg(Bus.Error)

	}

	"The System-under-test (643)" should "AUTO-GEN-TEST (643)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(643))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(643))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(643))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(643))
		actorRef ! (Bus.Publish(643, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(643))
		actorRef ! (Bus.DestroyConnection(643))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(643))
		actorRef ! (Bus.Publish(643, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(643))
		actorRef ! (Bus.DestroyConnection(643))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(643))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(643))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(643))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(643))
		actorRef ! (Bus.Unsubscribe(643))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(643))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(643))
		actorRef ! (Bus.UnsubscribeCallback(643))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(643))
		expectMsg(Bus.Error)

	}

	"The System-under-test (644)" should "AUTO-GEN-TEST (644)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(644))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(644))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(644))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(644))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(644))
		actorRef ! (Bus.Connect(644))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(644))
		actorRef ! (Bus.Subscribe(644))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(644, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(644))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(644, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (645)" should "AUTO-GEN-TEST (645)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(645))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(645))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(645, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(645, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(645))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(645))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(645))
		expectMsg(Bus.Error)

	}

	"The System-under-test (646)" should "AUTO-GEN-TEST (646)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(646))
		actorRef ! (Bus.Publish(646, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(646))
		actorRef ! (Bus.SubscribeCallback(646))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(646))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(646))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(646))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(646))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(646))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(646))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(646))
		expectMsg(Bus.Error)

	}

	"The System-under-test (647)" should "AUTO-GEN-TEST (647)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(647))
		actorRef ! (Bus.Disconnect(647))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(647))
		actorRef ! (Bus.Disconnect(647))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(647))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(647))
		actorRef ! (Bus.Disconnect(647))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(647))
		actorRef ! (Bus.DestroyConnection(647))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(647))
		actorRef ! (Bus.CreateConnection(647))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(647))
		actorRef ! (Bus.Disconnect(647))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(647))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(647))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(647, "foo"))
		actorRef ! (Bus.Publish(647, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(647, "foo"))
		actorRef ! (Bus.Unsubscribe(647))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(647))
		expectMsg(Bus.Error)

	}

	"The System-under-test (648)" should "AUTO-GEN-TEST (648)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(648))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(648))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(648))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(648))
		expectMsg(Bus.Success)

	}

	"The System-under-test (649)" should "AUTO-GEN-TEST (649)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(649))
		actorRef ! (Bus.Unsubscribe(649))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(649, "foo"))
		actorRef ! (Bus.Unsubscribe(649))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(649))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(649))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(649, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(649))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(649))
		expectMsg(Bus.Error)

	}

	"The System-under-test (650)" should "AUTO-GEN-TEST (650)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(650))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(650))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(650, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(650))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(650))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(650))
		expectMsg(Bus.Success)

	}

	"The System-under-test (651)" should "AUTO-GEN-TEST (651)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(651))
		actorRef ! (Bus.Disconnect(651))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(651))
		actorRef ! (Bus.Connect(651))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(651))
		actorRef ! (Bus.Publish(651, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(651))
		actorRef ! (Bus.UnsubscribeCallback(651))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(651, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(651))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(651))
		actorRef ! (Bus.Disconnect(651))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(651))
		expectMsg(Bus.Error)

	}

	"The System-under-test (652)" should "AUTO-GEN-TEST (652)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(652))
		actorRef ! (Bus.Publish(652, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(652))
		actorRef ! (Bus.SubscribeCallback(652))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(652))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(652))
		actorRef ! (Bus.Disconnect(652))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(652))
		actorRef ! (Bus.Connect(652))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(652))
		actorRef ! (Bus.SubscribeCallback(652))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(652))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(652))
		actorRef ! (Bus.DestroyConnection(652))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(652))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(652))
		actorRef ! (Bus.Connect(652))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(652))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(652))
		expectMsg(Bus.Error)

	}

	"The System-under-test (653)" should "AUTO-GEN-TEST (653)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(653))
		actorRef ! (Bus.Subscribe(653))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(653, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(653, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(653))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(653))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(653))
		expectMsg(Bus.Error)

	}

	"The System-under-test (654)" should "AUTO-GEN-TEST (654)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(654))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(654))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(654))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(654))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(654, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(654))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(654))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(654))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(654, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (655)" should "AUTO-GEN-TEST (655)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(655))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(655, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(655))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(655, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(655, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(655))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(655))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(655))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(655))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(655, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(655))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(655))
		expectMsg(Bus.Error)

	}

	"The System-under-test (656)" should "AUTO-GEN-TEST (656)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(656))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(656))
		actorRef ! (Bus.Disconnect(656))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(656, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(656))
		actorRef ! (Bus.Connect(656))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(656))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(656))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(656))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(656))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(656, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(656))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(656))
		expectMsg(Bus.Error)

	}

	"The System-under-test (657)" should "AUTO-GEN-TEST (657)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(657))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(657))
		actorRef ! (Bus.UnsubscribeCallback(657))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(657))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(657))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(657))
		actorRef ! (Bus.UnsubscribeCallback(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(657))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(657, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(657))
		actorRef ! (Bus.DestroyConnection(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(657))
		actorRef ! (Bus.Unsubscribe(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(657))
		actorRef ! (Bus.Publish(657, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(657))
		actorRef ! (Bus.SubscribeCallback(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(657))
		actorRef ! (Bus.SubscribeCallback(657))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (658)" should "AUTO-GEN-TEST (658)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(658))
		actorRef ! (Bus.Connect(658))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(658))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(658))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(658))
		actorRef ! (Bus.CreateConnection(658))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(658))
		actorRef ! (Bus.UnsubscribeCallback(658))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(658))
		actorRef ! (Bus.CreateConnection(658))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(658))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(658))
		actorRef ! (Bus.UnsubscribeCallback(658))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(658))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(658))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(658, "foo"))
		actorRef ! (Bus.Unsubscribe(658))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(658))
		actorRef ! (Bus.SubscribeCallback(658))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(658))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(658))
		expectMsg(Bus.Success)

	}

	"The System-under-test (659)" should "AUTO-GEN-TEST (659)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(659))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(659))
		actorRef ! (Bus.Connect(659))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(659))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(659))
		actorRef ! (Bus.Subscribe(659))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(659))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(659))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(659))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(659))
		expectMsg(Bus.Success)

	}

	"The System-under-test (660)" should "AUTO-GEN-TEST (660)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(660))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(660))
		actorRef ! (Bus.Connect(660))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(660))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(660))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(660, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(660))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(660))
		expectMsg(Bus.Error)

	}

	"The System-under-test (661)" should "AUTO-GEN-TEST (661)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(661))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(661, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(661))
		actorRef ! (Bus.Subscribe(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(661))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(661))
		actorRef ! (Bus.Disconnect(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(661))
		actorRef ! (Bus.Disconnect(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(661))
		actorRef ! (Bus.Disconnect(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(661))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(661))
		actorRef ! (Bus.DestroyConnection(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(661))
		actorRef ! (Bus.DestroyConnection(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(661))
		actorRef ! (Bus.Disconnect(661))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(661))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(661))
		expectMsg(Bus.Error)

	}

	"The System-under-test (662)" should "AUTO-GEN-TEST (662)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(662))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(662))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(662))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(662))
		actorRef ! (Bus.Disconnect(662))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(662))
		actorRef ! (Bus.SubscribeCallback(662))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(662))
		actorRef ! (Bus.Publish(662, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(662))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(662))
		actorRef ! (Bus.Unsubscribe(662))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(662))
		actorRef ! (Bus.DestroyConnection(662))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(662, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(662))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(662))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(662))
		actorRef ! (Bus.UnsubscribeCallback(662))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(662))
		expectMsg(Bus.Error)

	}

	"The System-under-test (663)" should "AUTO-GEN-TEST (663)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(663))
		actorRef ! (Bus.Unsubscribe(663))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(663))
		actorRef ! (Bus.Subscribe(663))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(663, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(663))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(663))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(663))
		expectMsg(Bus.Success)

	}

	"The System-under-test (664)" should "AUTO-GEN-TEST (664)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(664))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(664))
		actorRef ! (Bus.DestroyConnection(664))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(664))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(664))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(664))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(664, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(664))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(664, "foo"))
		actorRef ! (Bus.Connect(664))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(664))
		actorRef ! (Bus.SubscribeCallback(664))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(664))
		actorRef ! (Bus.CreateConnection(664))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(664))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(664))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(664))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(664))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(664))
		expectMsg(Bus.Error)

	}

	"The System-under-test (665)" should "AUTO-GEN-TEST (665)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(665))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(665))
		actorRef ! (Bus.Connect(665))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(665))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(665))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(665, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(665))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(665))
		expectMsg(Bus.Error)

	}

	"The System-under-test (666)" should "AUTO-GEN-TEST (666)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(666))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(666))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(666))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(666))
		expectMsg(Bus.Success)

	}

	"The System-under-test (667)" should "AUTO-GEN-TEST (667)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(667))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(667))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(667))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(667))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(667))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(667))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(667, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (668)" should "AUTO-GEN-TEST (668)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(668))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(668))
		actorRef ! (Bus.Unsubscribe(668))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(668, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(668))
		actorRef ! (Bus.Unsubscribe(668))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(668))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(668, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(668))
		expectMsg(Bus.Error)

	}

	"The System-under-test (669)" should "AUTO-GEN-TEST (669)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(669, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(669))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(669))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(669, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(669))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(669))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(669))
		expectMsg(Bus.Error)

	}

	"The System-under-test (670)" should "AUTO-GEN-TEST (670)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(670))
		actorRef ! (Bus.UnsubscribeCallback(670))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(670))
		actorRef ! (Bus.SubscribeCallback(670))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(670))
		actorRef ! (Bus.DestroyConnection(670))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(670, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(670))
		actorRef ! (Bus.Connect(670))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(670))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(670))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(670))
		expectMsg(Bus.Error)

	}

	"The System-under-test (671)" should "AUTO-GEN-TEST (671)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(671))
		actorRef ! (Bus.DestroyConnection(671))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(671))
		actorRef ! (Bus.Subscribe(671))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(671))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(671))
		actorRef ! (Bus.DestroyConnection(671))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(671, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(671, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(671))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(671))
		expectMsg(Bus.Error)

	}

	"The System-under-test (672)" should "AUTO-GEN-TEST (672)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(672))
		actorRef ! (Bus.Disconnect(672))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(672))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(672))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(672))
		actorRef ! (Bus.UnsubscribeCallback(672))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(672))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(672))
		actorRef ! (Bus.Disconnect(672))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(672))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(672))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(672))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(672))
		expectMsg(Bus.Error)

	}

	"The System-under-test (673)" should "AUTO-GEN-TEST (673)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(673))
		actorRef ! (Bus.Connect(673))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(673))
		actorRef ! (Bus.Publish(673, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(673, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (674)" should "AUTO-GEN-TEST (674)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(674))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(674))
		actorRef ! (Bus.DestroyConnection(674))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(674))
		actorRef ! (Bus.Unsubscribe(674))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(674, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(674))
		actorRef ! (Bus.Subscribe(674))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(674))
		actorRef ! (Bus.Subscribe(674))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(674))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(674))
		expectMsg(Bus.Error)

	}

	"The System-under-test (675)" should "AUTO-GEN-TEST (675)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(675))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(675))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(675))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(675))
		expectMsg(Bus.Error)

	}

	"The System-under-test (676)" should "AUTO-GEN-TEST (676)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(676))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(676))
		actorRef ! (Bus.Publish(676, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(676))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(676))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(676))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(676))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(676))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(676))
		expectMsg(Bus.Success)

	}

	"The System-under-test (677)" should "AUTO-GEN-TEST (677)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(677))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(677))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(677))
		actorRef ! (Bus.Disconnect(677))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(677))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(677))
		actorRef ! (Bus.Disconnect(677))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(677))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(677))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(677))
		actorRef ! (Bus.SubscribeCallback(677))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(677))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(677))
		actorRef ! (Bus.UnsubscribeCallback(677))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(677))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(677))
		actorRef ! (Bus.Connect(677))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(677))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(677))
		actorRef ! (Bus.Unsubscribe(677))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(677))
		expectMsg(Bus.Error)

	}

	"The System-under-test (678)" should "AUTO-GEN-TEST (678)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(678))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(678))
		actorRef ! (Bus.Subscribe(678))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(678, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(678))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(678))
		actorRef ! (Bus.Disconnect(678))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(678))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(678))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(678))
		actorRef ! (Bus.Connect(678))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(678))
		actorRef ! (Bus.DestroyConnection(678))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(678))
		actorRef ! (Bus.CreateConnection(678))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(678))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(678))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(678))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(678))
		expectMsg(Bus.Error)

	}

	"The System-under-test (679)" should "AUTO-GEN-TEST (679)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(679))
		actorRef ! (Bus.DestroyConnection(679))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(679))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(679))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(679))
		actorRef ! (Bus.CreateConnection(679))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(679))
		actorRef ! (Bus.Publish(679, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(679))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(679))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(679, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (680)" should "AUTO-GEN-TEST (680)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(680))
		actorRef ! (Bus.Subscribe(680))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(680))
		actorRef ! (Bus.Disconnect(680))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(680))
		actorRef ! (Bus.Connect(680))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(680))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(680))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(680))
		expectMsg(Bus.Error)

	}

	"The System-under-test (681)" should "AUTO-GEN-TEST (681)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(681))
		actorRef ! (Bus.Disconnect(681))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(681))
		actorRef ! (Bus.UnsubscribeCallback(681))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(681))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(681))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(681))
		expectMsg(Bus.Success)

	}

	"The System-under-test (682)" should "AUTO-GEN-TEST (682)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(682))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(682))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(682))
		actorRef ! (Bus.DestroyConnection(682))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(682))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(682))
		actorRef ! (Bus.Disconnect(682))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(682))
		actorRef ! (Bus.Disconnect(682))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(682))
		expectMsg(Bus.Success)

	}

	"The System-under-test (683)" should "AUTO-GEN-TEST (683)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(683))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(683, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(683))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(683))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(683))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(683))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(683))
		expectMsg(Bus.Error)

	}

	"The System-under-test (684)" should "AUTO-GEN-TEST (684)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(684))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(684))
		actorRef ! (Bus.DestroyConnection(684))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(684))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(684))
		actorRef ! (Bus.CreateConnection(684))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(684))
		actorRef ! (Bus.Subscribe(684))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(684))
		actorRef ! (Bus.Unsubscribe(684))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(684))
		actorRef ! (Bus.SubscribeCallback(684))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(684))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(684))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(684))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(684))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(684))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(684))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(684))
		expectMsg(Bus.Error)

	}

	"The System-under-test (685)" should "AUTO-GEN-TEST (685)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(685))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(685, "foo"))
		actorRef ! (Bus.Publish(685, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(685))
		actorRef ! (Bus.CreateConnection(685))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(685))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(685))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(685))
		actorRef ! (Bus.Connect(685))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(685))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(685))
		actorRef ! (Bus.Connect(685))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(685))
		expectMsg(Bus.Error)

	}

	"The System-under-test (686)" should "AUTO-GEN-TEST (686)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(686))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(686))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(686))
		expectMsg(Bus.Success)

	}

	"The System-under-test (687)" should "AUTO-GEN-TEST (687)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(687))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(687))
		actorRef ! (Bus.DestroyConnection(687))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(687))
		actorRef ! (Bus.SubscribeCallback(687))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(687))
		actorRef ! (Bus.Disconnect(687))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(687, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(687))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(687))
		expectMsg(Bus.Error)

	}

	"The System-under-test (688)" should "AUTO-GEN-TEST (688)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(688))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(688, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(688))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(688))
		actorRef ! (Bus.DestroyConnection(688))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(688))
		actorRef ! (Bus.UnsubscribeCallback(688))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(688, "foo"))
		actorRef ! (Bus.Publish(688, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(688))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(688))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(688))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(688))
		expectMsg(Bus.Success)

	}

	"The System-under-test (689)" should "AUTO-GEN-TEST (689)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(689))
		actorRef ! (Bus.DestroyConnection(689))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(689))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(689))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(689))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(689))
		actorRef ! (Bus.SubscribeCallback(689))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(689))
		actorRef ! (Bus.Subscribe(689))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(689))
		actorRef ! (Bus.SubscribeCallback(689))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(689))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(689))
		actorRef ! (Bus.UnsubscribeCallback(689))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(689))
		actorRef ! (Bus.Disconnect(689))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(689))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(689))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(689, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (690)" should "AUTO-GEN-TEST (690)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(690))
		actorRef ! (Bus.Disconnect(690))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(690))
		actorRef ! (Bus.UnsubscribeCallback(690))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(690))
		actorRef ! (Bus.UnsubscribeCallback(690))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(690))
		actorRef ! (Bus.SubscribeCallback(690))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(690))
		actorRef ! (Bus.Disconnect(690))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(690))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(690))
		expectMsg(Bus.Error)

	}

	"The System-under-test (691)" should "AUTO-GEN-TEST (691)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(691))
		actorRef ! (Bus.Publish(691, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(691, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(691))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(691))
		actorRef ! (Bus.Disconnect(691))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(691))
		actorRef ! (Bus.SubscribeCallback(691))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(691))
		actorRef ! (Bus.Disconnect(691))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(691, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(691))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(691))
		expectMsg(Bus.Success)

	}

	"The System-under-test (692)" should "AUTO-GEN-TEST (692)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(692))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(692))
		actorRef ! (Bus.Unsubscribe(692))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(692, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (693)" should "AUTO-GEN-TEST (693)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(693))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(693, "foo"))
		actorRef ! (Bus.SubscribeCallback(693))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(693))
		actorRef ! (Bus.Disconnect(693))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(693))
		actorRef ! (Bus.UnsubscribeCallback(693))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(693))
		actorRef ! (Bus.Connect(693))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(693))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(693, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(693))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(693))
		expectMsg(Bus.Error)

	}

	"The System-under-test (694)" should "AUTO-GEN-TEST (694)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(694))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(694, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(694))
		expectMsg(Bus.Error)

	}

	"The System-under-test (695)" should "AUTO-GEN-TEST (695)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(695))
		actorRef ! (Bus.SubscribeCallback(695))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(695))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(695))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(695))
		actorRef ! (Bus.Publish(695, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(695))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(695))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(695))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(695))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(695))
		expectMsg(Bus.Success)

	}

	"The System-under-test (696)" should "AUTO-GEN-TEST (696)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(696))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(696))
		actorRef ! (Bus.CreateConnection(696))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(696))
		actorRef ! (Bus.Subscribe(696))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(696))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(696))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(696))
		expectMsg(Bus.Error)

	}

	"The System-under-test (697)" should "AUTO-GEN-TEST (697)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(697))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(697))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(697))
		actorRef ! (Bus.Unsubscribe(697))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(697))
		actorRef ! (Bus.Subscribe(697))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(697, "foo"))
		actorRef ! (Bus.Publish(697, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (698)" should "AUTO-GEN-TEST (698)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(698))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(698, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(698))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(698))
		expectMsg(Bus.Error)

	}

	"The System-under-test (699)" should "AUTO-GEN-TEST (699)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(699))
		actorRef ! (Bus.Disconnect(699))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(699))
		actorRef ! (Bus.DestroyConnection(699))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(699))
		actorRef ! (Bus.Disconnect(699))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(699, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(699, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(699))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(699))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(699, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(699))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(699))
		expectMsg(Bus.Error)

	}

	"The System-under-test (700)" should "AUTO-GEN-TEST (700)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(700))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(700))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(700, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(700))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(700))
		expectMsg(Bus.Error)

	}

	"The System-under-test (701)" should "AUTO-GEN-TEST (701)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(701))
		actorRef ! (Bus.Disconnect(701))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(701))
		actorRef ! (Bus.SubscribeCallback(701))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(701))
		actorRef ! (Bus.Unsubscribe(701))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(701))
		actorRef ! (Bus.Unsubscribe(701))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(701))
		actorRef ! (Bus.Publish(701, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(701))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(701))
		actorRef ! (Bus.Disconnect(701))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(701))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(701, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(701))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(701))
		actorRef ! (Bus.Publish(701, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(701))
		actorRef ! (Bus.SubscribeCallback(701))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (702)" should "AUTO-GEN-TEST (702)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(702))
		actorRef ! (Bus.UnsubscribeCallback(702))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(702, "foo"))
		actorRef ! (Bus.Subscribe(702))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(702))
		actorRef ! (Bus.Disconnect(702))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(702))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(702))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(702))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(702, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(702))
		expectMsg(Bus.Error)

	}

	"The System-under-test (703)" should "AUTO-GEN-TEST (703)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(703))
		actorRef ! (Bus.UnsubscribeCallback(703))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(703))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(703))
		actorRef ! (Bus.Unsubscribe(703))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(703, "foo"))
		actorRef ! (Bus.CreateConnection(703))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(703, "foo"))
		actorRef ! (Bus.Publish(703, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(703))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(703))
		actorRef ! (Bus.UnsubscribeCallback(703))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(703))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(703))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(703))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(703))
		actorRef ! (Bus.Disconnect(703))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(703))
		actorRef ! (Bus.Connect(703))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(703))
		expectMsg(Bus.Error)

	}

	"The System-under-test (704)" should "AUTO-GEN-TEST (704)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(704))
		actorRef ! (Bus.Unsubscribe(704))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(704))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(704, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(704))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(704))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(704))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(704))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(704))
		expectMsg(Bus.Error)

	}

	"The System-under-test (705)" should "AUTO-GEN-TEST (705)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(705))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(705))
		actorRef ! (Bus.Unsubscribe(705))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(705))
		actorRef ! (Bus.Publish(705, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(705))
		actorRef ! (Bus.Disconnect(705))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(705))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(705))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(705))
		actorRef ! (Bus.Disconnect(705))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(705))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(705))
		actorRef ! (Bus.Subscribe(705))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (706)" should "AUTO-GEN-TEST (706)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(706))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(706, "foo"))
		actorRef ! (Bus.Publish(706, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(706))
		actorRef ! (Bus.Publish(706, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(706))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(706))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(706))
		expectMsg(Bus.Error)

	}

	"The System-under-test (707)" should "AUTO-GEN-TEST (707)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(707))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(707, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(707, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(707))
		actorRef ! (Bus.Subscribe(707))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(707))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(707))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(707))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(707, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(707))
		expectMsg(Bus.Error)

	}

	"The System-under-test (708)" should "AUTO-GEN-TEST (708)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(708))
		actorRef ! (Bus.UnsubscribeCallback(708))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(708))
		actorRef ! (Bus.Publish(708, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(708))
		actorRef ! (Bus.UnsubscribeCallback(708))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(708))
		actorRef ! (Bus.Disconnect(708))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(708))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(708))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(708))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(708))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(708, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(708))
		expectMsg(Bus.Error)

	}

	"The System-under-test (709)" should "AUTO-GEN-TEST (709)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(709))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(709))
		actorRef ! (Bus.Connect(709))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(709))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(709))
		actorRef ! (Bus.UnsubscribeCallback(709))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(709))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(709))
		actorRef ! (Bus.SubscribeCallback(709))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(709))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(709))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(709))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(709))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(709))
		expectMsg(Bus.Error)

	}

	"The System-under-test (710)" should "AUTO-GEN-TEST (710)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(710))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(710))
		actorRef ! (Bus.Publish(710, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(710))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(710))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(710))
		actorRef ! (Bus.Publish(710, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(710))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(710))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(710, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(710))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(710))
		expectMsg(Bus.Error)

	}

	"The System-under-test (711)" should "AUTO-GEN-TEST (711)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(711))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(711))
		actorRef ! (Bus.Connect(711))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(711, "foo"))
		actorRef ! (Bus.DestroyConnection(711))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(711))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(711))
		expectMsg(Bus.Error)

	}

	"The System-under-test (712)" should "AUTO-GEN-TEST (712)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(712))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(712))
		actorRef ! (Bus.CreateConnection(712))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(712))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(712))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(712))
		actorRef ! (Bus.Disconnect(712))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(712))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(712))
		actorRef ! (Bus.Connect(712))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(712))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(712))
		actorRef ! (Bus.Unsubscribe(712))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(712))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(712))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(712))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(712))
		actorRef ! (Bus.DestroyConnection(712))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(712))
		expectMsg(Bus.Error)

	}

	"The System-under-test (713)" should "AUTO-GEN-TEST (713)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(713))
		actorRef ! (Bus.DestroyConnection(713))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(713))
		actorRef ! (Bus.Subscribe(713))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(713))
		actorRef ! (Bus.DestroyConnection(713))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(713))
		actorRef ! (Bus.SubscribeCallback(713))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(713, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(713))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(713))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(713))
		expectMsg(Bus.Error)

	}

	"The System-under-test (714)" should "AUTO-GEN-TEST (714)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(714, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(714))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(714))
		actorRef ! (Bus.Unsubscribe(714))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(714, "foo"))
		actorRef ! (Bus.SubscribeCallback(714))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(714))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(714))
		expectMsg(Bus.Success)

	}

	"The System-under-test (715)" should "AUTO-GEN-TEST (715)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(715))
		actorRef ! (Bus.CreateConnection(715))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(715))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(715))
		actorRef ! (Bus.DestroyConnection(715))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(715))
		actorRef ! (Bus.Connect(715))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(715))
		actorRef ! (Bus.Connect(715))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(715))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(715))
		actorRef ! (Bus.Publish(715, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(715))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(715, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (716)" should "AUTO-GEN-TEST (716)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(716))
		actorRef ! (Bus.Disconnect(716))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(716))
		actorRef ! (Bus.DestroyConnection(716))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(716))
		actorRef ! (Bus.Disconnect(716))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(716))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(716))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(716))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(716, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(716))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(716))
		expectMsg(Bus.Error)

	}

	"The System-under-test (717)" should "AUTO-GEN-TEST (717)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(717))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(717))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(717))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(717))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(717))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(717))
		expectMsg(Bus.Error)

	}

	"The System-under-test (718)" should "AUTO-GEN-TEST (718)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(718))
		actorRef ! (Bus.SubscribeCallback(718))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(718))
		actorRef ! (Bus.Unsubscribe(718))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(718))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(718))
		actorRef ! (Bus.Subscribe(718))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(718))
		actorRef ! (Bus.Publish(718, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(718))
		actorRef ! (Bus.SubscribeCallback(718))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(718))
		actorRef ! (Bus.Unsubscribe(718))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(718))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(718, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(718))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(718))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(718, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(718))
		expectMsg(Bus.Error)

	}

	"The System-under-test (719)" should "AUTO-GEN-TEST (719)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(719))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(719, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (720)" should "AUTO-GEN-TEST (720)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(720))
		actorRef ! (Bus.Disconnect(720))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(720))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(720))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(720, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(720))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(720))
		expectMsg(Bus.Error)

	}

	"The System-under-test (721)" should "AUTO-GEN-TEST (721)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(721))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(721, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(721))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(721))
		actorRef ! (Bus.Disconnect(721))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(721))
		actorRef ! (Bus.Publish(721, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(721))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(721, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (722)" should "AUTO-GEN-TEST (722)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(722))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(722, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(722, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(722))
		actorRef ! (Bus.CreateConnection(722))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(722))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(722))
		actorRef ! (Bus.SubscribeCallback(722))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(722))
		actorRef ! (Bus.CreateConnection(722))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(722))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(722))
		actorRef ! (Bus.SubscribeCallback(722))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(722))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(722))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(722))
		expectMsg(Bus.Success)

	}

	"The System-under-test (723)" should "AUTO-GEN-TEST (723)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(723))
		actorRef ! (Bus.Connect(723))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(723))
		actorRef ! (Bus.Disconnect(723))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(723))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(723))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(723))
		expectMsg(Bus.Error)

	}

	"The System-under-test (724)" should "AUTO-GEN-TEST (724)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(724))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(724))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(724, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(724))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(724))
		actorRef ! (Bus.Subscribe(724))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(724))
		actorRef ! (Bus.Publish(724, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(724))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(724))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(724))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(724, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(724))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(724))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(724, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (725)" should "AUTO-GEN-TEST (725)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(725))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(725))
		actorRef ! (Bus.Subscribe(725))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(725))
		actorRef ! (Bus.CreateConnection(725))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(725))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(725))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(725))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(725, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (726)" should "AUTO-GEN-TEST (726)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(726))
		actorRef ! (Bus.Publish(726, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(726))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(726, "foo"))
		actorRef ! (Bus.Connect(726))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(726))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(726))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(726))
		actorRef ! (Bus.UnsubscribeCallback(726))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(726))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(726))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(726, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(726, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(726))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(726))
		expectMsg(Bus.Success)

	}

	"The System-under-test (727)" should "AUTO-GEN-TEST (727)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(727))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(727))
		actorRef ! (Bus.UnsubscribeCallback(727))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (728)" should "AUTO-GEN-TEST (728)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(728))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(728))
		actorRef ! (Bus.Unsubscribe(728))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(728))
		actorRef ! (Bus.Connect(728))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(728))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(728, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(728, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(728, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(728))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(728))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(728))
		expectMsg(Bus.Error)

	}

	"The System-under-test (729)" should "AUTO-GEN-TEST (729)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(729))
		actorRef ! (Bus.SubscribeCallback(729))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(729, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(729, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(729))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(729))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(729))
		expectMsg(Bus.Error)

	}

	"The System-under-test (730)" should "AUTO-GEN-TEST (730)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(730))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(730))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(730, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (731)" should "AUTO-GEN-TEST (731)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(731))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(731))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(731))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(731))
		actorRef ! (Bus.UnsubscribeCallback(731))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(731, "foo"))
		actorRef ! (Bus.DestroyConnection(731))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (732)" should "AUTO-GEN-TEST (732)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(732))
		actorRef ! (Bus.Disconnect(732))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(732))
		expectMsg(Bus.Error)

	}

	"The System-under-test (733)" should "AUTO-GEN-TEST (733)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(733))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(733))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(733, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(733, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(733))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(733, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (734)" should "AUTO-GEN-TEST (734)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(734))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(734, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(734))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(734))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(734))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(734))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(734))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(734))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(734))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(734))
		expectMsg(Bus.Error)

	}

	"The System-under-test (735)" should "AUTO-GEN-TEST (735)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(735))
		actorRef ! (Bus.Subscribe(735))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(735, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(735))
		actorRef ! (Bus.Publish(735, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(735))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(735))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(735))
		expectMsg(Bus.Success)

	}

	"The System-under-test (736)" should "AUTO-GEN-TEST (736)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(736))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(736))
		actorRef ! (Bus.Unsubscribe(736))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(736))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(736))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(736))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(736))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(736))
		actorRef ! (Bus.Publish(736, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(736))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(736))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(736))
		actorRef ! (Bus.Connect(736))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(736))
		actorRef ! (Bus.Publish(736, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(736))
		expectMsg(Bus.Error)

	}

	"The System-under-test (737)" should "AUTO-GEN-TEST (737)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(737))
		actorRef ! (Bus.UnsubscribeCallback(737))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(737))
		actorRef ! (Bus.UnsubscribeCallback(737))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(737, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(737))
		actorRef ! (Bus.SubscribeCallback(737))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(737))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(737))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(737))
		actorRef ! (Bus.UnsubscribeCallback(737))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(737))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(737))
		actorRef ! (Bus.SubscribeCallback(737))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(737))
		actorRef ! (Bus.DestroyConnection(737))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(737))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(737))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(737))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(737, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (738)" should "AUTO-GEN-TEST (738)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(738))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(738))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(738))
		actorRef ! (Bus.CreateConnection(738))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(738, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(738))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(738))
		actorRef ! (Bus.Publish(738, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(738))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(738))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(738))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(738))
		expectMsg(Bus.Error)

	}

	"The System-under-test (739)" should "AUTO-GEN-TEST (739)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(739))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(739))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(739))
		actorRef ! (Bus.Disconnect(739))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(739, "foo"))
		actorRef ! (Bus.SubscribeCallback(739))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(739))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(739))
		actorRef ! (Bus.CreateConnection(739))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(739))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(739, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(739))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(739))
		expectMsg(Bus.Error)

	}

	"The System-under-test (740)" should "AUTO-GEN-TEST (740)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(740))
		actorRef ! (Bus.UnsubscribeCallback(740))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(740, "foo"))
		actorRef ! (Bus.Subscribe(740))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(740))
		actorRef ! (Bus.CreateConnection(740))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(740))
		actorRef ! (Bus.Publish(740, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(740))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(740))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(740, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (741)" should "AUTO-GEN-TEST (741)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(741))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(741))
		actorRef ! (Bus.DestroyConnection(741))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(741, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(741, "foo"))
		actorRef ! (Bus.Subscribe(741))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(741))
		actorRef ! (Bus.DestroyConnection(741))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(741))
		actorRef ! (Bus.DestroyConnection(741))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(741))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(741))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(741))
		actorRef ! (Bus.Connect(741))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(741))
		actorRef ! (Bus.SubscribeCallback(741))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(741))
		actorRef ! (Bus.DestroyConnection(741))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(741))
		actorRef ! (Bus.DestroyConnection(741))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(741))
		expectMsg(Bus.Error)

	}

	"The System-under-test (742)" should "AUTO-GEN-TEST (742)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(742))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(742, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(742))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(742))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(742, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(742, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(742))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(742))
		expectMsg(Bus.Error)

	}

	"The System-under-test (743)" should "AUTO-GEN-TEST (743)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(743))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(743))
		actorRef ! (Bus.DestroyConnection(743))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(743))
		actorRef ! (Bus.DestroyConnection(743))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(743))
		actorRef ! (Bus.Disconnect(743))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(743))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(743))
		actorRef ! (Bus.Disconnect(743))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (744)" should "AUTO-GEN-TEST (744)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(744))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(744))
		actorRef ! (Bus.DestroyConnection(744))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(744))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(744))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(744))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(744))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(744, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(744))
		actorRef ! (Bus.Connect(744))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(744))
		actorRef ! (Bus.Disconnect(744))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(744))
		actorRef ! (Bus.SubscribeCallback(744))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(744))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(744))
		expectMsg(Bus.Error)

	}

	"The System-under-test (745)" should "AUTO-GEN-TEST (745)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(745))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(745))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(745))
		actorRef ! (Bus.UnsubscribeCallback(745))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(745))
		actorRef ! (Bus.Subscribe(745))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(745))
		actorRef ! (Bus.CreateConnection(745))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(745))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(745))
		actorRef ! (Bus.Unsubscribe(745))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(745))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(745))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(745))
		expectMsg(Bus.Error)

	}

	"The System-under-test (746)" should "AUTO-GEN-TEST (746)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(746))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(746))
		actorRef ! (Bus.DestroyConnection(746))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(746, "foo"))
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(746))
		actorRef ! (Bus.CreateConnection(746))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(746, "foo"))
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(746))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(746))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(746))
		expectMsg(Bus.Error)

	}

	"The System-under-test (747)" should "AUTO-GEN-TEST (747)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(747))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(747))
		actorRef ! (Bus.Publish(747, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(747))
		actorRef ! (Bus.SubscribeCallback(747))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(747))
		actorRef ! (Bus.CreateConnection(747))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(747, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(747))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(747))
		actorRef ! (Bus.UnsubscribeCallback(747))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(747))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(747, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (748)" should "AUTO-GEN-TEST (748)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(748))
		actorRef ! (Bus.Connect(748))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(748))
		actorRef ! (Bus.Publish(748, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(748))
		actorRef ! (Bus.CreateConnection(748))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(748))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(748))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(748, "foo"))
		actorRef ! (Bus.SubscribeCallback(748))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(748))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(748, "foo"))
		actorRef ! (Bus.CreateConnection(748))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(748))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(748))
		actorRef ! (Bus.UnsubscribeCallback(748))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(748))
		actorRef ! (Bus.UnsubscribeCallback(748))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(748))
		expectMsg(Bus.Error)

	}

	"The System-under-test (749)" should "AUTO-GEN-TEST (749)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(749))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(749))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(749))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(749, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(749))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(749))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(749))
		expectMsg(Bus.Error)

	}

	"The System-under-test (750)" should "AUTO-GEN-TEST (750)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(750))
		actorRef ! (Bus.CreateConnection(750))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(750))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(750, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(750, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (751)" should "AUTO-GEN-TEST (751)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(751))
		actorRef ! (Bus.DestroyConnection(751))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(751))
		actorRef ! (Bus.Disconnect(751))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(751))
		actorRef ! (Bus.DestroyConnection(751))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(751))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(751))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(751))
		actorRef ! (Bus.SubscribeCallback(751))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(751))
		actorRef ! (Bus.CreateConnection(751))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(751))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(751, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(751))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(751))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(751))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(751))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(751))
		expectMsg(Bus.Error)

	}

	"The System-under-test (752)" should "AUTO-GEN-TEST (752)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(752))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(752))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(752))
		actorRef ! (Bus.DestroyConnection(752))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(752))
		actorRef ! (Bus.Connect(752))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(752, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(752))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(752))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(752))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(752))
		expectMsg(Bus.Error)

	}

	"The System-under-test (753)" should "AUTO-GEN-TEST (753)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(753))
		actorRef ! (Bus.Subscribe(753))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(753))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(753))
		actorRef ! (Bus.DestroyConnection(753))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(753))
		actorRef ! (Bus.Disconnect(753))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(753))
		actorRef ! (Bus.Disconnect(753))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(753, "foo"))
		actorRef ! (Bus.DestroyConnection(753))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(753))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(753))
		actorRef ! (Bus.Publish(753, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (754)" should "AUTO-GEN-TEST (754)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(754))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(754))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(754))
		actorRef ! (Bus.Subscribe(754))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(754, "foo"))
		actorRef ! (Bus.Subscribe(754))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(754))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(754))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(754, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(754))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(754))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(754))
		expectMsg(Bus.Error)

	}

	"The System-under-test (755)" should "AUTO-GEN-TEST (755)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(755))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(755, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(755))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(755))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(755))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(755))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(755))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(755))
		expectMsg(Bus.Success)

	}

	"The System-under-test (756)" should "AUTO-GEN-TEST (756)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(756))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(756))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(756))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(756))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(756))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(756))
		expectMsg(Bus.Success)

	}

	"The System-under-test (757)" should "AUTO-GEN-TEST (757)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(757))
		actorRef ! (Bus.DestroyConnection(757))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(757))
		actorRef ! (Bus.Unsubscribe(757))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(757))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(757, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(757, "foo"))
		actorRef ! (Bus.Unsubscribe(757))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(757))
		actorRef ! (Bus.Unsubscribe(757))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(757))
		actorRef ! (Bus.Publish(757, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(757))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(757))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(757))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(757))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(757))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(757))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(757))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(757, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (758)" should "AUTO-GEN-TEST (758)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(758))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(758))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(758))
		expectMsg(Bus.Error)

	}

	"The System-under-test (759)" should "AUTO-GEN-TEST (759)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(759))
		actorRef ! (Bus.Unsubscribe(759))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(759))
		actorRef ! (Bus.Connect(759))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(759))
		actorRef ! (Bus.Disconnect(759))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(759))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(759))
		actorRef ! (Bus.Subscribe(759))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(759))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(759))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(759))
		actorRef ! (Bus.DestroyConnection(759))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(759))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(759))
		actorRef ! (Bus.UnsubscribeCallback(759))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(759))
		actorRef ! (Bus.DestroyConnection(759))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(759))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(759))
		expectMsg(Bus.Error)

	}

	"The System-under-test (760)" should "AUTO-GEN-TEST (760)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(760))
		actorRef ! (Bus.Publish(760, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(760, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(760))
		actorRef ! (Bus.Subscribe(760))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(760))
		actorRef ! (Bus.DestroyConnection(760))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(760))
		actorRef ! (Bus.Unsubscribe(760))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(760))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(760))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(760))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(760))
		expectMsg(Bus.Error)

	}

	"The System-under-test (761)" should "AUTO-GEN-TEST (761)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(761))
		actorRef ! (Bus.SubscribeCallback(761))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(761))
		actorRef ! (Bus.Disconnect(761))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(761))
		actorRef ! (Bus.Connect(761))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(761))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(761))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(761))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(761))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(761))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(761, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (762)" should "AUTO-GEN-TEST (762)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(762))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(762))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(762))
		actorRef ! (Bus.Connect(762))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(762))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(762))
		actorRef ! (Bus.Connect(762))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(762))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(762, "foo"))
		actorRef ! (Bus.SubscribeCallback(762))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(762, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (763)" should "AUTO-GEN-TEST (763)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(763))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(763))
		actorRef ! (Bus.SubscribeCallback(763))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(763))
		actorRef ! (Bus.UnsubscribeCallback(763))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(763))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(763, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(763))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(763))
		expectMsg(Bus.Error)

	}

	"The System-under-test (764)" should "AUTO-GEN-TEST (764)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(764))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(764, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(764, "foo"))
		actorRef ! (Bus.CreateConnection(764))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(764, "foo"))
		actorRef ! (Bus.Unsubscribe(764))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(764))
		actorRef ! (Bus.SubscribeCallback(764))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(764))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(764))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(764))
		actorRef ! (Bus.DestroyConnection(764))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(764))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(764))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(764, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(764))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(764))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(764, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(764))
		expectMsg(Bus.Error)

	}

	"The System-under-test (765)" should "AUTO-GEN-TEST (765)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(765))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(765))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(765))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(765))
		expectMsg(Bus.Error)

	}

	"The System-under-test (766)" should "AUTO-GEN-TEST (766)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(766))
		expectMsg(Bus.Success)

	}

	"The System-under-test (767)" should "AUTO-GEN-TEST (767)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(767))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(767))
		actorRef ! (Bus.Subscribe(767))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(767))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(767))
		actorRef ! (Bus.Publish(767, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(767))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(767))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(767))
		expectMsg(Bus.Error)

	}

	"The System-under-test (768)" should "AUTO-GEN-TEST (768)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(768))
		actorRef ! (Bus.Disconnect(768))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(768))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(768))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(768))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(768))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(768))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(768))
		expectMsg(Bus.Error)

	}

	"The System-under-test (769)" should "AUTO-GEN-TEST (769)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(769))
		actorRef ! (Bus.Disconnect(769))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(769))
		actorRef ! (Bus.SubscribeCallback(769))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(769, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(769, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(769, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(769))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(769, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(769))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(769))
		expectMsg(Bus.Error)

	}

	"The System-under-test (770)" should "AUTO-GEN-TEST (770)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(770))
		actorRef ! (Bus.Publish(770, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(770))
		actorRef ! (Bus.Disconnect(770))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(770, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(770, "foo"))
		actorRef ! (Bus.CreateConnection(770))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(770))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(770))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(770))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(770))
		expectMsg(Bus.Success)

	}

	"The System-under-test (771)" should "AUTO-GEN-TEST (771)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(771))
		actorRef ! (Bus.Connect(771))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(771))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(771))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(771))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(771))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(771))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(771))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(771))
		expectMsg(Bus.Error)

	}

	"The System-under-test (772)" should "AUTO-GEN-TEST (772)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(772, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(772))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(772))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(772, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(772))
		expectMsg(Bus.Error)

	}

	"The System-under-test (773)" should "AUTO-GEN-TEST (773)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(773))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(773))
		actorRef ! (Bus.Subscribe(773))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(773, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(773))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(773))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(773, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(773, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(773))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(773))
		expectMsg(Bus.Error)

	}

	"The System-under-test (774)" should "AUTO-GEN-TEST (774)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(774))
		actorRef ! (Bus.UnsubscribeCallback(774))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(774))
		actorRef ! (Bus.CreateConnection(774))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(774, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(774))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(774))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(774))
		expectMsg(Bus.Error)

	}

	"The System-under-test (775)" should "AUTO-GEN-TEST (775)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(775))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(775))
		actorRef ! (Bus.Subscribe(775))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(775, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(775))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(775))
		actorRef ! (Bus.Unsubscribe(775))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(775))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(775, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(775))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(775))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(775, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(775))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(775))
		expectMsg(Bus.Success)

	}

	"The System-under-test (776)" should "AUTO-GEN-TEST (776)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(776))
		actorRef ! (Bus.Unsubscribe(776))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(776))
		actorRef ! (Bus.Connect(776))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(776))
		actorRef ! (Bus.UnsubscribeCallback(776))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(776))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(776))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(776))
		actorRef ! (Bus.Connect(776))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(776))
		actorRef ! (Bus.SubscribeCallback(776))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(776, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(776))
		actorRef ! (Bus.Disconnect(776))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(776))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(776, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(776))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(776))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(776))
		expectMsg(Bus.Error)

	}

	"The System-under-test (777)" should "AUTO-GEN-TEST (777)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(777))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(777))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(777))
		expectMsg(Bus.Error)

	}

	"The System-under-test (778)" should "AUTO-GEN-TEST (778)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(778))
		actorRef ! (Bus.SubscribeCallback(778))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(778, "foo"))
		actorRef ! (Bus.Publish(778, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(778))
		actorRef ! (Bus.CreateConnection(778))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(778, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(778))
		actorRef ! (Bus.Unsubscribe(778))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(778))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(778))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(778))
		actorRef ! (Bus.Connect(778))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(778))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(778))
		actorRef ! (Bus.Unsubscribe(778))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(778))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(778))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(778))
		expectMsg(Bus.Error)

	}

	"The System-under-test (779)" should "AUTO-GEN-TEST (779)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(779))
		actorRef ! (Bus.CreateConnection(779))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(779))
		actorRef ! (Bus.Disconnect(779))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(779))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(779))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(779))
		expectMsg(Bus.Error)

	}

	"The System-under-test (780)" should "AUTO-GEN-TEST (780)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(780))
		actorRef ! (Bus.Subscribe(780))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(780))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(780, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (781)" should "AUTO-GEN-TEST (781)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(781))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(781))
		actorRef ! (Bus.Publish(781, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(781, "foo"))
		actorRef ! (Bus.Connect(781))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(781))
		actorRef ! (Bus.Subscribe(781))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(781, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(781))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(781))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(781))
		expectMsg(Bus.Error)

	}

	"The System-under-test (782)" should "AUTO-GEN-TEST (782)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(782))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(782))
		actorRef ! (Bus.CreateConnection(782))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(782))
		actorRef ! (Bus.CreateConnection(782))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(782))
		actorRef ! (Bus.DestroyConnection(782))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(782))
		actorRef ! (Bus.Subscribe(782))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(782))
		actorRef ! (Bus.SubscribeCallback(782))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(782))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(782))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(782, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(782))
		expectMsg(Bus.Error)

	}

	"The System-under-test (783)" should "AUTO-GEN-TEST (783)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(783))
		actorRef ! (Bus.DestroyConnection(783))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(783))
		actorRef ! (Bus.Publish(783, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(783))
		actorRef ! (Bus.UnsubscribeCallback(783))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(783))
		actorRef ! (Bus.Connect(783))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(783))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(783, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(783))
		expectMsg(Bus.Error)

	}

	"The System-under-test (784)" should "AUTO-GEN-TEST (784)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(784))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(784, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(784))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(784))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(784))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(784))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(784))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(784, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (785)" should "AUTO-GEN-TEST (785)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(785))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(785))
		actorRef ! (Bus.UnsubscribeCallback(785))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(785))
		actorRef ! (Bus.Subscribe(785))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(785))
		actorRef ! (Bus.Disconnect(785))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(785))
		actorRef ! (Bus.UnsubscribeCallback(785))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(785))
		actorRef ! (Bus.DestroyConnection(785))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(785))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(785))
		actorRef ! (Bus.Disconnect(785))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(785))
		expectMsg(Bus.Error)

	}

	"The System-under-test (786)" should "AUTO-GEN-TEST (786)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(786))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(786))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(786))
		actorRef ! (Bus.Subscribe(786))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(786))
		actorRef ! (Bus.CreateConnection(786))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(786))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(786))
		actorRef ! (Bus.Unsubscribe(786))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(786))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(786))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(786))
		expectMsg(Bus.Success)

	}

	"The System-under-test (787)" should "AUTO-GEN-TEST (787)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(787))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(787, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(787))
		actorRef ! (Bus.UnsubscribeCallback(787))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(787))
		actorRef ! (Bus.Subscribe(787))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(787))
		actorRef ! (Bus.SubscribeCallback(787))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(787, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(787))
		actorRef ! (Bus.UnsubscribeCallback(787))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(787))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(787))
		actorRef ! (Bus.SubscribeCallback(787))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(787))
		expectMsg(Bus.Error)

	}

	"The System-under-test (788)" should "AUTO-GEN-TEST (788)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(788))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(788))
		actorRef ! (Bus.SubscribeCallback(788))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(788))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(788))
		actorRef ! (Bus.UnsubscribeCallback(788))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(788))
		actorRef ! (Bus.Connect(788))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(788))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(788))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(788))
		actorRef ! (Bus.SubscribeCallback(788))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(788))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(788))
		actorRef ! (Bus.Connect(788))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(788))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(788))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(788))
		expectMsg(Bus.Error)

	}

	"The System-under-test (789)" should "AUTO-GEN-TEST (789)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(789))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(789))
		actorRef ! (Bus.CreateConnection(789))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(789))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(789))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(789))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(789))
		expectMsg(Bus.Error)

	}

	"The System-under-test (790)" should "AUTO-GEN-TEST (790)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(790))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(790))
		actorRef ! (Bus.CreateConnection(790))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(790))
		actorRef ! (Bus.CreateConnection(790))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(790))
		actorRef ! (Bus.Publish(790, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(790))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(790))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(790, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (791)" should "AUTO-GEN-TEST (791)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(791))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(791))
		actorRef ! (Bus.CreateConnection(791))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(791))
		actorRef ! (Bus.Disconnect(791))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(791))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(791))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(791))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(791))
		actorRef ! (Bus.Disconnect(791))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(791))
		actorRef ! (Bus.Disconnect(791))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(791))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(791, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(791, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (792)" should "AUTO-GEN-TEST (792)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(792))
		actorRef ! (Bus.Subscribe(792))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(792))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(792))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(792))
		actorRef ! (Bus.SubscribeCallback(792))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(792))
		actorRef ! (Bus.CreateConnection(792))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(792))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(792))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(792))
		actorRef ! (Bus.Subscribe(792))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(792))
		actorRef ! (Bus.SubscribeCallback(792))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(792))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(792))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(792))
		expectMsg(Bus.Success)

	}

	"The System-under-test (793)" should "AUTO-GEN-TEST (793)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(793))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(793))
		actorRef ! (Bus.Publish(793, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(793))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(793))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(793))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(793))
		expectMsg(Bus.Error)

	}

	"The System-under-test (794)" should "AUTO-GEN-TEST (794)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(794))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(794))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(794, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(794))
		expectMsg(Bus.Error)

	}

	"The System-under-test (795)" should "AUTO-GEN-TEST (795)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(795))
		actorRef ! (Bus.DestroyConnection(795))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(795))
		actorRef ! (Bus.Disconnect(795))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(795, "foo"))
		actorRef ! (Bus.Subscribe(795))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(795, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(795))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(795))
		expectMsg(Bus.Error)

	}

	"The System-under-test (796)" should "AUTO-GEN-TEST (796)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(796))
		actorRef ! (Bus.Unsubscribe(796))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(796))
		actorRef ! (Bus.DestroyConnection(796))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(796))
		actorRef ! (Bus.DestroyConnection(796))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(796))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(796, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(796))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(796))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(796))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(796))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(796))
		expectMsg(Bus.Error)

	}

	"The System-under-test (797)" should "AUTO-GEN-TEST (797)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(797))
		actorRef ! (Bus.Publish(797, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (798)" should "AUTO-GEN-TEST (798)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(798))
		actorRef ! (Bus.CreateConnection(798))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(798))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(798))
		actorRef ! (Bus.Connect(798))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(798))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(798))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(798))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(798))
		actorRef ! (Bus.Unsubscribe(798))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(798))
		actorRef ! (Bus.UnsubscribeCallback(798))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(798))
		expectMsg(Bus.Error)

	}

	"The System-under-test (799)" should "AUTO-GEN-TEST (799)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(799))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(799))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(799))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(799))
		actorRef ! (Bus.Connect(799))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(799))
		actorRef ! (Bus.Disconnect(799))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(799))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(799))
		actorRef ! (Bus.UnsubscribeCallback(799))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(799))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(799))
		actorRef ! (Bus.UnsubscribeCallback(799))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(799))
		actorRef ! (Bus.DestroyConnection(799))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(799))
		actorRef ! (Bus.Publish(799, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(799))
		actorRef ! (Bus.UnsubscribeCallback(799))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(799))
		expectMsg(Bus.Error)

	}

	"The System-under-test (800)" should "AUTO-GEN-TEST (800)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(800))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(800, "foo"))
		actorRef ! (Bus.DestroyConnection(800))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(800))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(800))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(800, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(800))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(800))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(800))
		expectMsg(Bus.Error)

	}

	"The System-under-test (801)" should "AUTO-GEN-TEST (801)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(801))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(801))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(801))
		actorRef ! (Bus.CreateConnection(801))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(801))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(801))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(801))
		actorRef ! (Bus.DestroyConnection(801))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(801))
		actorRef ! (Bus.CreateConnection(801))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(801))
		actorRef ! (Bus.Unsubscribe(801))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(801))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(801))
		actorRef ! (Bus.UnsubscribeCallback(801))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(801))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(801, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(801))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(801))
		expectMsg(Bus.Error)

	}

	"The System-under-test (802)" should "AUTO-GEN-TEST (802)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(802))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(802))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(802))
		actorRef ! (Bus.SubscribeCallback(802))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(802, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(802, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(802))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(802))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(802))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(802))
		expectMsg(Bus.Error)

	}

	"The System-under-test (803)" should "AUTO-GEN-TEST (803)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(803))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(803))
		actorRef ! (Bus.Unsubscribe(803))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(803))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(803))
		actorRef ! (Bus.Connect(803))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(803))
		actorRef ! (Bus.SubscribeCallback(803))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(803))
		actorRef ! (Bus.SubscribeCallback(803))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(803))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(803))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(803))
		expectMsg(Bus.Error)

	}

	"The System-under-test (804)" should "AUTO-GEN-TEST (804)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(804))
		actorRef ! (Bus.CreateConnection(804))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(804))
		actorRef ! (Bus.Subscribe(804))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(804))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(804))
		actorRef ! (Bus.Subscribe(804))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(804, "foo"))
		actorRef ! (Bus.Subscribe(804))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(804))
		actorRef ! (Bus.Disconnect(804))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(804))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(804))
		expectMsg(Bus.Error)

	}

	"The System-under-test (805)" should "AUTO-GEN-TEST (805)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(805))
		expectMsg(Bus.Success)

	}

	"The System-under-test (806)" should "AUTO-GEN-TEST (806)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(806))
		actorRef ! (Bus.SubscribeCallback(806))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(806))
		actorRef ! (Bus.CreateConnection(806))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(806, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(806, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(806, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(806))
		expectMsg(Bus.Error)

	}

	"The System-under-test (807)" should "AUTO-GEN-TEST (807)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(807))
		actorRef ! (Bus.SubscribeCallback(807))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(807))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(807))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(807))
		expectMsg(Bus.Error)

	}

	"The System-under-test (808)" should "AUTO-GEN-TEST (808)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(808))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(808, "foo"))
		actorRef ! (Bus.DestroyConnection(808))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(808, "foo"))
		actorRef ! (Bus.Connect(808))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(808))
		actorRef ! (Bus.Publish(808, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(808, "foo"))
		actorRef ! (Bus.SubscribeCallback(808))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(808))
		actorRef ! (Bus.SubscribeCallback(808))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(808))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(808))
		expectMsg(Bus.Error)

	}

	"The System-under-test (809)" should "AUTO-GEN-TEST (809)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(809))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(809))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(809))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(809))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(809))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(809))
		expectMsg(Bus.Success)

	}

	"The System-under-test (810)" should "AUTO-GEN-TEST (810)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(810))
		actorRef ! (Bus.Connect(810))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(810))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(810))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(810))
		actorRef ! (Bus.DestroyConnection(810))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(810))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(810))
		expectMsg(Bus.Success)

	}

	"The System-under-test (811)" should "AUTO-GEN-TEST (811)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(811))
		actorRef ! (Bus.Unsubscribe(811))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(811))
		actorRef ! (Bus.CreateConnection(811))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(811))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(811))
		actorRef ! (Bus.Publish(811, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(811))
		actorRef ! (Bus.CreateConnection(811))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(811))
		actorRef ! (Bus.Disconnect(811))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(811))
		actorRef ! (Bus.UnsubscribeCallback(811))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(811))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(811))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(811))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(811))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(811))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(811))
		expectMsg(Bus.Error)

	}

	"The System-under-test (812)" should "AUTO-GEN-TEST (812)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(812))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(812))
		actorRef ! (Bus.CreateConnection(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(812))
		actorRef ! (Bus.Disconnect(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(812, "foo"))
		actorRef ! (Bus.Disconnect(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(812))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(812))
		actorRef ! (Bus.Disconnect(812))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(812))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(812))
		actorRef ! (Bus.SubscribeCallback(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(812, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(812, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(812))
		actorRef ! (Bus.DestroyConnection(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(812))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(812))
		actorRef ! (Bus.SubscribeCallback(812))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (813)" should "AUTO-GEN-TEST (813)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(813))
		actorRef ! (Bus.SubscribeCallback(813))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(813))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(813))
		actorRef ! (Bus.Subscribe(813))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(813))
		actorRef ! (Bus.Disconnect(813))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(813))
		actorRef ! (Bus.SubscribeCallback(813))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(813))
		actorRef ! (Bus.Subscribe(813))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(813))
		actorRef ! (Bus.Unsubscribe(813))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(813))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(813, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (814)" should "AUTO-GEN-TEST (814)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(814))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(814, "foo"))
		actorRef ! (Bus.SubscribeCallback(814))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(814))
		actorRef ! (Bus.DestroyConnection(814))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(814))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(814))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(814))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(814))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(814))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(814))
		expectMsg(Bus.Success)

	}

	"The System-under-test (815)" should "AUTO-GEN-TEST (815)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(815))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(815, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(815))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(815))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(815))
		actorRef ! (Bus.SubscribeCallback(815))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(815, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(815))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(815))
		actorRef ! (Bus.Disconnect(815))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(815))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(815))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(815))
		expectMsg(Bus.Error)

	}

	"The System-under-test (816)" should "AUTO-GEN-TEST (816)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(816))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(816, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(816))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(816))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(816))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(816))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(816, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(816, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (817)" should "AUTO-GEN-TEST (817)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(817))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(817))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(817))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(817))
		expectMsg(Bus.Error)

	}

	"The System-under-test (818)" should "AUTO-GEN-TEST (818)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(818))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(818))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(818))
		expectMsg(Bus.Error)

	}

	"The System-under-test (819)" should "AUTO-GEN-TEST (819)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(819))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(819))
		expectMsg(Bus.Error)

	}

	"The System-under-test (820)" should "AUTO-GEN-TEST (820)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(820))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(820))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(820, "foo"))
		actorRef ! (Bus.Disconnect(820))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(820))
		expectMsg(Bus.Error)

	}

	"The System-under-test (821)" should "AUTO-GEN-TEST (821)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(821))
		actorRef ! (Bus.UnsubscribeCallback(821))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(821))
		actorRef ! (Bus.Connect(821))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(821, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(821, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(821))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(821))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(821))
		expectMsg(Bus.Error)

	}

	"The System-under-test (822)" should "AUTO-GEN-TEST (822)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(822))
		actorRef ! (Bus.Disconnect(822))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(822))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(822, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(822))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(822))
		expectMsg(Bus.Success)

	}

	"The System-under-test (823)" should "AUTO-GEN-TEST (823)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(823))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(823))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(823))
		actorRef ! (Bus.DestroyConnection(823))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(823, "foo"))
		actorRef ! (Bus.DestroyConnection(823))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(823))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(823))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(823))
		actorRef ! (Bus.Publish(823, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(823))
		actorRef ! (Bus.Subscribe(823))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(823))
		actorRef ! (Bus.Unsubscribe(823))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(823))
		actorRef ! (Bus.UnsubscribeCallback(823))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(823))
		actorRef ! (Bus.CreateConnection(823))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(823))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(823))
		actorRef ! (Bus.Disconnect(823))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (824)" should "AUTO-GEN-TEST (824)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(824))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(824))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(824))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(824))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(824))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(824))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(824))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(824))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(824, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (825)" should "AUTO-GEN-TEST (825)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(825))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(825))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(825))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(825))
		expectMsg(Bus.Error)

	}

	"The System-under-test (826)" should "AUTO-GEN-TEST (826)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(826))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(826))
		actorRef ! (Bus.DestroyConnection(826))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(826))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(826))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(826))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(826, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(826))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(826))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(826))
		expectMsg(Bus.Error)

	}

	"The System-under-test (827)" should "AUTO-GEN-TEST (827)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(827))
		actorRef ! (Bus.SubscribeCallback(827))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(827))
		actorRef ! (Bus.CreateConnection(827))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(827))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(827))
		actorRef ! (Bus.Unsubscribe(827))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(827))
		actorRef ! (Bus.Connect(827))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(827))
		actorRef ! (Bus.SubscribeCallback(827))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(827))
		actorRef ! (Bus.DestroyConnection(827))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(827))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(827))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(827, "foo"))
		actorRef ! (Bus.Disconnect(827))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(827))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(827))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(827))
		expectMsg(Bus.Error)

	}

	"The System-under-test (828)" should "AUTO-GEN-TEST (828)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(828))
		actorRef ! (Bus.Unsubscribe(828))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(828))
		actorRef ! (Bus.SubscribeCallback(828))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(828))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(828))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(828))
		actorRef ! (Bus.Subscribe(828))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(828, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(828))
		actorRef ! (Bus.Publish(828, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(828))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(828))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(828))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(828))
		expectMsg(Bus.Error)

	}

	"The System-under-test (829)" should "AUTO-GEN-TEST (829)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(829))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(829))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(829))
		expectMsg(Bus.Error)

	}

	"The System-under-test (830)" should "AUTO-GEN-TEST (830)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(830))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(830, "foo"))
		actorRef ! (Bus.DestroyConnection(830))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(830))
		actorRef ! (Bus.Publish(830, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(830))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(830))
		expectMsg(Bus.Success)

	}

	"The System-under-test (831)" should "AUTO-GEN-TEST (831)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(831))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(831, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(831))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(831))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(831))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(831))
		expectMsg(Bus.Error)

	}

	"The System-under-test (832)" should "AUTO-GEN-TEST (832)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(832))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(832))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(832))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(832))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(832))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(832))
		expectMsg(Bus.Error)

	}

	"The System-under-test (833)" should "AUTO-GEN-TEST (833)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(833))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(833, "foo"))
		actorRef ! (Bus.Unsubscribe(833))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(833, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(833))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(833))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(833))
		expectMsg(Bus.Error)

	}

	"The System-under-test (834)" should "AUTO-GEN-TEST (834)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(834))
		actorRef ! (Bus.SubscribeCallback(834))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(834))
		actorRef ! (Bus.Publish(834, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(834))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(834))
		actorRef ! (Bus.SubscribeCallback(834))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(834))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(834))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(834))
		actorRef ! (Bus.Unsubscribe(834))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(834, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(834))
		expectMsg(Bus.Error)

	}

	"The System-under-test (835)" should "AUTO-GEN-TEST (835)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(835))
		actorRef ! (Bus.UnsubscribeCallback(835))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(835))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(835))
		actorRef ! (Bus.Connect(835))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(835))
		actorRef ! (Bus.Disconnect(835))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(835))
		actorRef ! (Bus.UnsubscribeCallback(835))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(835))
		actorRef ! (Bus.Connect(835))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(835, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(835))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(835))
		actorRef ! (Bus.Connect(835))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (836)" should "AUTO-GEN-TEST (836)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(836))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(836))
		actorRef ! (Bus.Disconnect(836))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(836))
		actorRef ! (Bus.Disconnect(836))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(836))
		actorRef ! (Bus.Unsubscribe(836))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(836))
		actorRef ! (Bus.SubscribeCallback(836))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(836, "foo"))
		actorRef ! (Bus.DestroyConnection(836))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(836))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(836))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(836))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(836))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(836))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(836))
		expectMsg(Bus.Error)

	}

	"The System-under-test (837)" should "AUTO-GEN-TEST (837)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(837))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(837, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(837))
		actorRef ! (Bus.UnsubscribeCallback(837))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(837))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(837))
		actorRef ! (Bus.Subscribe(837))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(837))
		actorRef ! (Bus.Disconnect(837))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(837))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(837))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(837, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (838)" should "AUTO-GEN-TEST (838)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(838))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(838))
		actorRef ! (Bus.CreateConnection(838))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (839)" should "AUTO-GEN-TEST (839)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(839))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(839))
		actorRef ! (Bus.Subscribe(839))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(839))
		actorRef ! (Bus.Unsubscribe(839))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(839))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(839))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(839))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(839))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(839))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(839))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(839))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(839, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (840)" should "AUTO-GEN-TEST (840)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(840))
		actorRef ! (Bus.UnsubscribeCallback(840))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(840))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(840))
		actorRef ! (Bus.Subscribe(840))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(840))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(840, "foo"))
		actorRef ! (Bus.Publish(840, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (841)" should "AUTO-GEN-TEST (841)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(841))
		actorRef ! (Bus.Disconnect(841))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(841))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(841))
		actorRef ! (Bus.SubscribeCallback(841))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(841))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(841))
		actorRef ! (Bus.Publish(841, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(841))
		actorRef ! (Bus.UnsubscribeCallback(841))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(841))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(841))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(841))
		expectMsg(Bus.Error)

	}

	"The System-under-test (842)" should "AUTO-GEN-TEST (842)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(842))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(842))
		actorRef ! (Bus.UnsubscribeCallback(842))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(842))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(842, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(842, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(842, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(842))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(842))
		expectMsg(Bus.Error)

	}

	"The System-under-test (843)" should "AUTO-GEN-TEST (843)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(843))
		actorRef ! (Bus.Disconnect(843))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(843))
		actorRef ! (Bus.SubscribeCallback(843))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(843))
		actorRef ! (Bus.UnsubscribeCallback(843))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(843))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(843))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(843, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(843, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (844)" should "AUTO-GEN-TEST (844)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(844))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(844, "foo"))
		actorRef ! (Bus.Unsubscribe(844))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(844))
		actorRef ! (Bus.UnsubscribeCallback(844))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(844))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(844))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(844, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(844))
		expectMsg(Bus.Error)

	}

	"The System-under-test (845)" should "AUTO-GEN-TEST (845)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(845))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(845, "foo"))
		actorRef ! (Bus.Unsubscribe(845))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(845, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(845, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(845))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(845))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(845))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(845))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(845))
		expectMsg(Bus.Error)

	}

	"The System-under-test (846)" should "AUTO-GEN-TEST (846)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(846))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(846, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(846, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(846))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(846))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(846))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(846))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(846))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(846))
		expectMsg(Bus.Error)

	}

	"The System-under-test (847)" should "AUTO-GEN-TEST (847)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(847))
		actorRef ! (Bus.Publish(847, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(847))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(847))
		actorRef ! (Bus.CreateConnection(847))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(847))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(847))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(847))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(847))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(847))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(847))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(847))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(847))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(847))
		expectMsg(Bus.Error)

	}

	"The System-under-test (848)" should "AUTO-GEN-TEST (848)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(848))
		actorRef ! (Bus.DestroyConnection(848))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(848, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(848))
		actorRef ! (Bus.SubscribeCallback(848))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(848, "foo"))
		actorRef ! (Bus.SubscribeCallback(848))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(848))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(848))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(848))
		actorRef ! (Bus.UnsubscribeCallback(848))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(848))
		actorRef ! (Bus.Disconnect(848))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(848))
		actorRef ! (Bus.DestroyConnection(848))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(848))
		actorRef ! (Bus.Subscribe(848))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(848))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(848))
		actorRef ! (Bus.Connect(848))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (849)" should "AUTO-GEN-TEST (849)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(849))
		actorRef ! (Bus.Disconnect(849))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(849, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(849))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(849))
		actorRef ! (Bus.Unsubscribe(849))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(849))
		actorRef ! (Bus.Disconnect(849))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(849))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(849, "foo"))
		actorRef ! (Bus.Connect(849))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(849, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (850)" should "AUTO-GEN-TEST (850)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(850))
		actorRef ! (Bus.UnsubscribeCallback(850))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(850))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(850))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(850))
		actorRef ! (Bus.Unsubscribe(850))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(850, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(850))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(850))
		actorRef ! (Bus.SubscribeCallback(850))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(850, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(850))
		actorRef ! (Bus.Disconnect(850))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(850))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(850))
		actorRef ! (Bus.DestroyConnection(850))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(850, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(850))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(850))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(850))
		expectMsg(Bus.Error)

	}

	"The System-under-test (851)" should "AUTO-GEN-TEST (851)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(851))
		actorRef ! (Bus.CreateConnection(851))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(851))
		actorRef ! (Bus.Subscribe(851))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(851))
		actorRef ! (Bus.CreateConnection(851))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(851))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(851))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(851))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(851))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(851))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(851))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(851))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(851, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (852)" should "AUTO-GEN-TEST (852)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(852))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(852))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(852))
		actorRef ! (Bus.SubscribeCallback(852))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(852))
		actorRef ! (Bus.CreateConnection(852))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(852))
		actorRef ! (Bus.Unsubscribe(852))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(852))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(852))
		actorRef ! (Bus.Connect(852))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(852, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(852))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(852, "foo"))
		actorRef ! (Bus.Subscribe(852))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(852))
		actorRef ! (Bus.Publish(852, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(852))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(852))
		expectMsg(Bus.Error)

	}

	"The System-under-test (853)" should "AUTO-GEN-TEST (853)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(853))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(853))
		actorRef ! (Bus.Disconnect(853))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(853))
		actorRef ! (Bus.CreateConnection(853))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(853))
		actorRef ! (Bus.UnsubscribeCallback(853))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(853))
		actorRef ! (Bus.UnsubscribeCallback(853))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(853))
		actorRef ! (Bus.CreateConnection(853))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(853))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(853))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(853))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(853))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(853))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(853))
		expectMsg(Bus.Error)

	}

	"The System-under-test (854)" should "AUTO-GEN-TEST (854)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(854))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(854, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(854, "foo"))
		actorRef ! (Bus.Publish(854, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(854, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(854, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(854))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(854))
		expectMsg(Bus.Success)

	}

	"The System-under-test (855)" should "AUTO-GEN-TEST (855)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(855))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(855))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(855))
		expectMsg(Bus.Error)

	}

	"The System-under-test (856)" should "AUTO-GEN-TEST (856)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(856))
		actorRef ! (Bus.Publish(856, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(856, "foo"))
		actorRef ! (Bus.Publish(856, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(856))
		actorRef ! (Bus.UnsubscribeCallback(856))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(856))
		actorRef ! (Bus.Disconnect(856))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(856, "foo"))
		actorRef ! (Bus.Connect(856))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(856))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(856))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(856))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(856))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(856))
		actorRef ! (Bus.UnsubscribeCallback(856))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (857)" should "AUTO-GEN-TEST (857)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(857))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(857))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(857))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(857))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(857))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(857))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(857))
		expectMsg(Bus.Error)

	}

	"The System-under-test (858)" should "AUTO-GEN-TEST (858)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(858))
		actorRef ! (Bus.Unsubscribe(858))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(858))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(858))
		actorRef ! (Bus.Disconnect(858))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(858))
		actorRef ! (Bus.UnsubscribeCallback(858))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(858))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(858))
		expectMsg(Bus.Success)

	}

	"The System-under-test (859)" should "AUTO-GEN-TEST (859)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(859))
		actorRef ! (Bus.UnsubscribeCallback(859))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(859))
		actorRef ! (Bus.CreateConnection(859))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(859))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(859))
		actorRef ! (Bus.Unsubscribe(859))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(859))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(859))
		actorRef ! (Bus.Publish(859, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(859))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(859))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(859))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(859))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(859))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(859))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(859))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(859, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (860)" should "AUTO-GEN-TEST (860)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(860))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(860))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(860))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(860))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(860))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(860))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(860))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(860, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(860))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(860, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (861)" should "AUTO-GEN-TEST (861)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(861))
		actorRef ! (Bus.DestroyConnection(861))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(861))
		actorRef ! (Bus.DestroyConnection(861))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(861, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(861, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(861, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(861))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(861, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(861))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(861))
		expectMsg(Bus.Error)

	}

	"The System-under-test (862)" should "AUTO-GEN-TEST (862)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(862))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(862))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(862))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(862))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(862))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(862))
		expectMsg(Bus.Error)

	}

	"The System-under-test (863)" should "AUTO-GEN-TEST (863)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(863, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(863, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(863, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(863))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(863, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(863))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(863))
		expectMsg(Bus.Error)

	}

	"The System-under-test (864)" should "AUTO-GEN-TEST (864)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(864))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(864))
		actorRef ! (Bus.Publish(864, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(864))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(864))
		actorRef ! (Bus.Subscribe(864))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(864, "foo"))
		actorRef ! (Bus.SubscribeCallback(864))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(864))
		actorRef ! (Bus.Connect(864))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(864, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(864, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(864))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(864))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(864))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(864))
		expectMsg(Bus.Success)

	}

	"The System-under-test (865)" should "AUTO-GEN-TEST (865)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(865))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(865))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(865))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(865))
		actorRef ! (Bus.SubscribeCallback(865))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(865, "foo"))
		actorRef ! (Bus.Disconnect(865))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(865))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(865, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (866)" should "AUTO-GEN-TEST (866)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(866))
		actorRef ! (Bus.SubscribeCallback(866))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(866))
		actorRef ! (Bus.SubscribeCallback(866))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(866, "foo"))
		actorRef ! (Bus.CreateConnection(866))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(866, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (867)" should "AUTO-GEN-TEST (867)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(867))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(867))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(867))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(867))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(867))
		expectMsg(Bus.Error)

	}

	"The System-under-test (868)" should "AUTO-GEN-TEST (868)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(868))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(868))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(868))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(868))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(868))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(868))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(868))
		expectMsg(Bus.Error)

	}

	"The System-under-test (869)" should "AUTO-GEN-TEST (869)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(869))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(869, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(869, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(869))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(869))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(869, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (870)" should "AUTO-GEN-TEST (870)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(870))
		actorRef ! (Bus.Subscribe(870))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(870))
		actorRef ! (Bus.Publish(870, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(870))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(870))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(870, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (871)" should "AUTO-GEN-TEST (871)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(871))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(871))
		actorRef ! (Bus.Connect(871))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(871))
		actorRef ! (Bus.DestroyConnection(871))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(871))
		expectMsg(Bus.Error)

	}

	"The System-under-test (872)" should "AUTO-GEN-TEST (872)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(872))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(872))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(872))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(872))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(872))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(872))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(872, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(872))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(872))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(872))
		expectMsg(Bus.Success)

	}

	"The System-under-test (873)" should "AUTO-GEN-TEST (873)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(873))
		actorRef ! (Bus.Disconnect(873))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(873))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(873, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(873))
		actorRef ! (Bus.SubscribeCallback(873))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(873))
		actorRef ! (Bus.UnsubscribeCallback(873))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(873, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(873))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(873))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(873, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(873))
		expectMsg(Bus.Error)

	}

	"The System-under-test (874)" should "AUTO-GEN-TEST (874)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(874))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(874))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(874))
		actorRef ! (Bus.Disconnect(874))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(874, "foo"))
		actorRef ! (Bus.Subscribe(874))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(874))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(874, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(874))
		actorRef ! (Bus.Unsubscribe(874))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(874))
		actorRef ! (Bus.SubscribeCallback(874))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(874))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(874))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(874))
		expectMsg(Bus.Success)

	}

	"The System-under-test (875)" should "AUTO-GEN-TEST (875)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(875))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(875))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(875))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(875))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(875))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(875))
		expectMsg(Bus.Error)

	}

	"The System-under-test (876)" should "AUTO-GEN-TEST (876)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(876))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(876))
		actorRef ! (Bus.Unsubscribe(876))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(876))
		actorRef ! (Bus.CreateConnection(876))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(876))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(876))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(876))
		actorRef ! (Bus.Subscribe(876))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(876))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(876))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(876))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(876))
		expectMsg(Bus.Error)

	}

	"The System-under-test (877)" should "AUTO-GEN-TEST (877)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(877))
		actorRef ! (Bus.UnsubscribeCallback(877))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(877))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(877))
		actorRef ! (Bus.Disconnect(877))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(877))
		actorRef ! (Bus.UnsubscribeCallback(877))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(877))
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(877, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(877))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(877, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(877, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(877))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(877))
		expectMsg(Bus.Success)

	}

	"The System-under-test (878)" should "AUTO-GEN-TEST (878)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(878))
		actorRef ! (Bus.CreateConnection(878))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(878, "foo"))
		actorRef ! (Bus.Disconnect(878))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(878))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(878))
		actorRef ! (Bus.Connect(878))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(878))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(878))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(878))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(878))
		actorRef ! (Bus.DestroyConnection(878))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(878))
		actorRef ! (Bus.Publish(878, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(878))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(878))
		actorRef ! (Bus.Disconnect(878))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(878))
		actorRef ! (Bus.Connect(878))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(878))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(878))
		expectMsg(Bus.Success)

	}

	"The System-under-test (879)" should "AUTO-GEN-TEST (879)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(879))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(879))
		actorRef ! (Bus.Connect(879))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(879))
		actorRef ! (Bus.Disconnect(879))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(879))
		actorRef ! (Bus.SubscribeCallback(879))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(879))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(879))
		actorRef ! (Bus.SubscribeCallback(879))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(879))
		actorRef ! (Bus.Unsubscribe(879))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(879))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(879))
		actorRef ! (Bus.SubscribeCallback(879))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(879, "foo"))
		actorRef ! (Bus.DestroyConnection(879))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(879))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(879))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(879))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(879))
		expectMsg(Bus.Success)

	}

	"The System-under-test (880)" should "AUTO-GEN-TEST (880)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(880))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(880))
		actorRef ! (Bus.Unsubscribe(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(880))
		actorRef ! (Bus.Subscribe(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(880))
		actorRef ! (Bus.Unsubscribe(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(880))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(880))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(880))
		actorRef ! (Bus.Unsubscribe(880))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(880, "foo"))
		actorRef ! (Bus.DestroyConnection(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(880))
		actorRef ! (Bus.Unsubscribe(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(880))
		actorRef ! (Bus.Publish(880, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(880))
		actorRef ! (Bus.Disconnect(880))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(880))
		expectMsg(Bus.Error)

	}

	"The System-under-test (881)" should "AUTO-GEN-TEST (881)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(881))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(881))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(881))
		actorRef ! (Bus.DestroyConnection(881))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(881))
		actorRef ! (Bus.Subscribe(881))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(881))
		actorRef ! (Bus.Subscribe(881))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(881))
		actorRef ! (Bus.DestroyConnection(881))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(881))
		actorRef ! (Bus.CreateConnection(881))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(881))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(881))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(881))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(881))
		actorRef ! (Bus.DestroyConnection(881))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(881))
		actorRef ! (Bus.Subscribe(881))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (882)" should "AUTO-GEN-TEST (882)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(882))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(882, "foo"))
		actorRef ! (Bus.SubscribeCallback(882))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(882, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(882))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(882))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(882))
		expectMsg(Bus.Success)

	}

	"The System-under-test (883)" should "AUTO-GEN-TEST (883)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(883))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(883))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(883))
		actorRef ! (Bus.Publish(883, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(883))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(883))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(883))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(883, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (884)" should "AUTO-GEN-TEST (884)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(884))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(884))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(884))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(884, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(884))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(884))
		expectMsg(Bus.Success)

	}

	"The System-under-test (885)" should "AUTO-GEN-TEST (885)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(885))
		actorRef ! (Bus.SubscribeCallback(885))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(885))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(885))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(885, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(885))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(885))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(885))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(885))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(885, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (886)" should "AUTO-GEN-TEST (886)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(886))
		actorRef ! (Bus.Connect(886))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(886, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(886))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(886))
		expectMsg(Bus.Success)

	}

	"The System-under-test (887)" should "AUTO-GEN-TEST (887)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(887))
		actorRef ! (Bus.DestroyConnection(887))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(887))
		actorRef ! (Bus.DestroyConnection(887))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(887))
		actorRef ! (Bus.CreateConnection(887))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(887))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(887))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(887))
		expectMsg(Bus.Success)

	}

	"The System-under-test (888)" should "AUTO-GEN-TEST (888)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(888))
		actorRef ! (Bus.Unsubscribe(888))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(888))
		actorRef ! (Bus.Unsubscribe(888))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(888))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(888))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(888))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(888, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (889)" should "AUTO-GEN-TEST (889)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(889))
		actorRef ! (Bus.CreateConnection(889))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(889, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(889))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(889))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(889))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(889))
		actorRef ! (Bus.Disconnect(889))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(889))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(889))
		actorRef ! (Bus.Unsubscribe(889))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(889))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(889))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(889))
		actorRef ! (Bus.DestroyConnection(889))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(889))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(889, "foo"))
		actorRef ! (Bus.DestroyConnection(889))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(889))
		actorRef ! (Bus.Unsubscribe(889))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (890)" should "AUTO-GEN-TEST (890)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(890))
		actorRef ! (Bus.Disconnect(890))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(890))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(890))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(890))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(890))
		expectMsg(Bus.Error)

	}

	"The System-under-test (891)" should "AUTO-GEN-TEST (891)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(891))
		actorRef ! (Bus.Disconnect(891))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(891))
		actorRef ! (Bus.CreateConnection(891))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(891))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(891))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(891))
		expectMsg(Bus.Error)

	}

	"The System-under-test (892)" should "AUTO-GEN-TEST (892)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(892))
		actorRef ! (Bus.Publish(892, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(892))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(892))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(892))
		actorRef ! (Bus.CreateConnection(892))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(892, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (893)" should "AUTO-GEN-TEST (893)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(893))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(893))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(893))
		actorRef ! (Bus.UnsubscribeCallback(893))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(893))
		actorRef ! (Bus.Publish(893, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(893))
		actorRef ! (Bus.SubscribeCallback(893))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(893))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(893))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(893))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(893, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(893))
		expectMsg(Bus.Error)

	}

	"The System-under-test (894)" should "AUTO-GEN-TEST (894)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(894))
		actorRef ! (Bus.UnsubscribeCallback(894))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(894))
		actorRef ! (Bus.SubscribeCallback(894))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(894))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(894, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(894))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(894))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(894))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(894))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(894))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(894))
		expectMsg(Bus.Error)

	}

	"The System-under-test (895)" should "AUTO-GEN-TEST (895)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(895))
		actorRef ! (Bus.Unsubscribe(895))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(895))
		actorRef ! (Bus.SubscribeCallback(895))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(895))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(895))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(895))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(895))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(895))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(895, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (896)" should "AUTO-GEN-TEST (896)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(896))
		actorRef ! (Bus.Publish(896, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(896))
		actorRef ! (Bus.Disconnect(896))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(896))
		actorRef ! (Bus.UnsubscribeCallback(896))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(896))
		actorRef ! (Bus.Subscribe(896))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(896))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(896))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(896, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(896))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(896))
		expectMsg(Bus.Error)

	}

	"The System-under-test (897)" should "AUTO-GEN-TEST (897)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(897))
		actorRef ! (Bus.Connect(897))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(897))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(897))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(897))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(897))
		expectMsg(Bus.Error)

	}

	"The System-under-test (898)" should "AUTO-GEN-TEST (898)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(898))
		actorRef ! (Bus.Publish(898, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(898))
		actorRef ! (Bus.Connect(898))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(898))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(898))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(898))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(898))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(898))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(898))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(898))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(898))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(898))
		expectMsg(Bus.Error)

	}

	"The System-under-test (899)" should "AUTO-GEN-TEST (899)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(899))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(899))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(899))
		actorRef ! (Bus.UnsubscribeCallback(899))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(899, "foo"))
		actorRef ! (Bus.SubscribeCallback(899))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(899, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (900)" should "AUTO-GEN-TEST (900)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(900))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(900))
		actorRef ! (Bus.Unsubscribe(900))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(900))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(900))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(900))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(900))
		actorRef ! (Bus.Publish(900, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(900))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(900))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(900, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (901)" should "AUTO-GEN-TEST (901)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(901))
		actorRef ! (Bus.Connect(901))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(901))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(901))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(901))
		expectMsg(Bus.Error)

	}

	"The System-under-test (902)" should "AUTO-GEN-TEST (902)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(902))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(902, "foo"))
		actorRef ! (Bus.SubscribeCallback(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(902))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(902))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(902))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(902))
		actorRef ! (Bus.UnsubscribeCallback(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(902))
		actorRef ! (Bus.UnsubscribeCallback(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(902))
		actorRef ! (Bus.Connect(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(902))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(902))
		actorRef ! (Bus.Unsubscribe(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(902))
		actorRef ! (Bus.CreateConnection(902))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(902))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(902))
		actorRef ! (Bus.Unsubscribe(902))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(902))
		expectMsg(Bus.Error)

	}

	"The System-under-test (903)" should "AUTO-GEN-TEST (903)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(903))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(903))
		actorRef ! (Bus.Disconnect(903))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(903))
		actorRef ! (Bus.Publish(903, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(903, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(903))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(903))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(903, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(903, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(903))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(903))
		expectMsg(Bus.Error)

	}

	"The System-under-test (904)" should "AUTO-GEN-TEST (904)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(904))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(904))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(904))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(904, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(904))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(904))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(904))
		expectMsg(Bus.Error)

	}

	"The System-under-test (905)" should "AUTO-GEN-TEST (905)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(905))
		actorRef ! (Bus.Disconnect(905))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(905))
		actorRef ! (Bus.Unsubscribe(905))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(905))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(905))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(905))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(905))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(905))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(905))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(905))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(905))
		expectMsg(Bus.Error)

	}

	"The System-under-test (906)" should "AUTO-GEN-TEST (906)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(906))
		actorRef ! (Bus.Unsubscribe(906))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(906))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(906))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(906, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (907)" should "AUTO-GEN-TEST (907)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(907))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(907, "foo"))
		actorRef ! (Bus.Disconnect(907))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(907))
		actorRef ! (Bus.Connect(907))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(907))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(907))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(907))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(907, "foo"))
		actorRef ! (Bus.CreateConnection(907))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(907))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(907, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(907, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(907))
		expectMsg(Bus.Error)

	}

	"The System-under-test (908)" should "AUTO-GEN-TEST (908)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(908))
		actorRef ! (Bus.CreateConnection(908))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(908, "foo"))
		actorRef ! (Bus.Unsubscribe(908))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(908))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(908))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(908))
		actorRef ! (Bus.DestroyConnection(908))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(908))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(908))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(908, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(908, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(908))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(908))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(908))
		expectMsg(Bus.Success)

	}

	"The System-under-test (909)" should "AUTO-GEN-TEST (909)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(909, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(909))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(909))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(909))
		expectMsg(Bus.Error)

	}

	"The System-under-test (910)" should "AUTO-GEN-TEST (910)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(910))
		actorRef ! (Bus.CreateConnection(910))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(910))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(910))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(910))
		expectMsg(Bus.Error)

	}

	"The System-under-test (911)" should "AUTO-GEN-TEST (911)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(911))
		actorRef ! (Bus.Subscribe(911))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(911))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(911))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(911))
		expectMsg(Bus.Success)

	}

	"The System-under-test (912)" should "AUTO-GEN-TEST (912)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(912))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(912))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(912))
		actorRef ! (Bus.CreateConnection(912))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(912))
		actorRef ! (Bus.SubscribeCallback(912))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(912))
		actorRef ! (Bus.CreateConnection(912))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(912))
		actorRef ! (Bus.Connect(912))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(912, "foo"))
		actorRef ! (Bus.Publish(912, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (913)" should "AUTO-GEN-TEST (913)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(913))
		actorRef ! (Bus.SubscribeCallback(913))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(913))
		actorRef ! (Bus.SubscribeCallback(913))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(913, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(913))
		actorRef ! (Bus.Subscribe(913))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(913, "foo"))
		actorRef ! (Bus.Publish(913, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(913))
		actorRef ! (Bus.SubscribeCallback(913))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(913))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(913))
		actorRef ! (Bus.Subscribe(913))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (914)" should "AUTO-GEN-TEST (914)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(914))
		actorRef ! (Bus.CreateConnection(914))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(914, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(914))
		expectMsg(Bus.Error)

	}

	"The System-under-test (915)" should "AUTO-GEN-TEST (915)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(915))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(915))
		actorRef ! (Bus.Connect(915))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(915))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(915))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(915))
		expectMsg(Bus.Success)

	}

	"The System-under-test (916)" should "AUTO-GEN-TEST (916)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(916))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(916))
		actorRef ! (Bus.Subscribe(916))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(916, "foo"))
		actorRef ! (Bus.CreateConnection(916))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(916, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(916, "foo"))
		actorRef ! (Bus.SubscribeCallback(916))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(916, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(916))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(916))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(916))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(916, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (917)" should "AUTO-GEN-TEST (917)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(917))
		actorRef ! (Bus.CreateConnection(917))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(917))
		actorRef ! (Bus.Connect(917))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(917))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(917, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (918)" should "AUTO-GEN-TEST (918)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(918))
		actorRef ! (Bus.CreateConnection(918))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(918))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(918))
		actorRef ! (Bus.CreateConnection(918))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(918))
		expectMsg(Bus.Success)

	}

	"The System-under-test (919)" should "AUTO-GEN-TEST (919)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(919))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(919, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(919))
		actorRef ! (Bus.Unsubscribe(919))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(919))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(919))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(919, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(919))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(919))
		expectMsg(Bus.Error)

	}

	"The System-under-test (920)" should "AUTO-GEN-TEST (920)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(920))
		actorRef ! (Bus.CreateConnection(920))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(920, "foo"))
		actorRef ! (Bus.CreateConnection(920))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(920))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(920))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(920))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(920))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(920, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(920))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(920))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(920))
		expectMsg(Bus.Error)

	}

	"The System-under-test (921)" should "AUTO-GEN-TEST (921)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(921))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(921))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(921))
		expectMsg(Bus.Error)

	}

	"The System-under-test (922)" should "AUTO-GEN-TEST (922)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(922))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(922))
		actorRef ! (Bus.Connect(922))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(922))
		actorRef ! (Bus.CreateConnection(922))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(922))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(922, "foo"))
		actorRef ! (Bus.Disconnect(922))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(922))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(922))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(922))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(922))
		expectMsg(Bus.Error)

	}

	"The System-under-test (923)" should "AUTO-GEN-TEST (923)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(923))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(923))
		actorRef ! (Bus.CreateConnection(923))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(923))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(923))
		actorRef ! (Bus.Unsubscribe(923))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(923))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(923))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(923))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(923))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(923, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (924)" should "AUTO-GEN-TEST (924)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(924))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(924))
		actorRef ! (Bus.Disconnect(924))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(924))
		actorRef ! (Bus.Publish(924, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(924))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(924))
		actorRef ! (Bus.Disconnect(924))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(924))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(924))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(924))
		actorRef ! (Bus.Connect(924))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(924))
		actorRef ! (Bus.Subscribe(924))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(924))
		actorRef ! (Bus.Subscribe(924))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(924))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(924))
		actorRef ! (Bus.Publish(924, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (925)" should "AUTO-GEN-TEST (925)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(925))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(925))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(925))
		actorRef ! (Bus.DestroyConnection(925))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(925))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(925))
		actorRef ! (Bus.Subscribe(925))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(925, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(925))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(925))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(925, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(925, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (926)" should "AUTO-GEN-TEST (926)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(926))
		actorRef ! (Bus.UnsubscribeCallback(926))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(926))
		actorRef ! (Bus.SubscribeCallback(926))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(926))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(926))
		actorRef ! (Bus.CreateConnection(926))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(926))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(926, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(926))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(926, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (927)" should "AUTO-GEN-TEST (927)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(927))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(927))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(927))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(927))
		expectMsg(Bus.Error)

	}

	"The System-under-test (928)" should "AUTO-GEN-TEST (928)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(928))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(928))
		actorRef ! (Bus.Publish(928, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(928))
		actorRef ! (Bus.Connect(928))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(928))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(928))
		actorRef ! (Bus.Unsubscribe(928))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(928))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(928))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(928))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(928))
		actorRef ! (Bus.SubscribeCallback(928))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(928))
		actorRef ! (Bus.UnsubscribeCallback(928))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(928))
		actorRef ! (Bus.SubscribeCallback(928))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(928))
		actorRef ! (Bus.Unsubscribe(928))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (929)" should "AUTO-GEN-TEST (929)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(929))
		actorRef ! (Bus.UnsubscribeCallback(929))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(929, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(929))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(929))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(929))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(929))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(929))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(929))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(929))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(929))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(929, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (930)" should "AUTO-GEN-TEST (930)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(930))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(930))
		actorRef ! (Bus.Connect(930))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (931)" should "AUTO-GEN-TEST (931)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(931))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(931))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(931, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(931))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(931))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(931))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(931))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(931))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(931, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (932)" should "AUTO-GEN-TEST (932)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(932))
		actorRef ! (Bus.Subscribe(932))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(932, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(932))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(932))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(932))
		expectMsg(Bus.Error)

	}

	"The System-under-test (933)" should "AUTO-GEN-TEST (933)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(933))
		actorRef ! (Bus.CreateConnection(933))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(933))
		actorRef ! (Bus.UnsubscribeCallback(933))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(933, "foo"))
		actorRef ! (Bus.DestroyConnection(933))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(933))
		actorRef ! (Bus.Subscribe(933))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(933, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(933))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(933, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(933))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(933))
		expectMsg(Bus.Error)

	}

	"The System-under-test (934)" should "AUTO-GEN-TEST (934)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(934))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(934, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(934))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(934, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(934, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(934))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(934, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (935)" should "AUTO-GEN-TEST (935)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(935))
		actorRef ! (Bus.SubscribeCallback(935))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(935))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(935))
		actorRef ! (Bus.Disconnect(935))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(935))
		actorRef ! (Bus.Unsubscribe(935))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(935))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(935))
		actorRef ! (Bus.Publish(935, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(935))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(935))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(935))
		actorRef ! (Bus.UnsubscribeCallback(935))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(935))
		actorRef ! (Bus.CreateConnection(935))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(935))
		actorRef ! (Bus.Connect(935))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(935, "foo"))
		actorRef ! (Bus.DestroyConnection(935))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)

	}

	"The System-under-test (936)" should "AUTO-GEN-TEST (936)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(936))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(936))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(936))
		actorRef ! (Bus.UnsubscribeCallback(936))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(936))
		actorRef ! (Bus.CreateConnection(936))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(936))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(936))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(936))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(936))
		expectMsg(Bus.Error)

	}

	"The System-under-test (937)" should "AUTO-GEN-TEST (937)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(937))
		actorRef ! (Bus.CreateConnection(937))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(937))
		actorRef ! (Bus.Disconnect(937))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(937))
		actorRef ! (Bus.Connect(937))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(937))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(937, "foo"))
		actorRef ! (Bus.Disconnect(937))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(937))
		actorRef ! (Bus.Connect(937))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(937))
		actorRef ! (Bus.UnsubscribeCallback(937))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(937))
		expectMsg(Bus.Error)

	}

	"The System-under-test (938)" should "AUTO-GEN-TEST (938)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(938))
		actorRef ! (Bus.DestroyConnection(938))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(938))
		actorRef ! (Bus.Publish(938, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(938))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(938))
		expectMsg(Bus.Error)

	}

	"The System-under-test (939)" should "AUTO-GEN-TEST (939)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(939))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(939))
		actorRef ! (Bus.SubscribeCallback(939))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(939))
		actorRef ! (Bus.UnsubscribeCallback(939))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(939, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(939))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(939))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(939))
		expectMsg(Bus.Error)

	}

	"The System-under-test (940)" should "AUTO-GEN-TEST (940)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(940))
		actorRef ! (Bus.Connect(940))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(940))
		actorRef ! (Bus.Connect(940))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(940))
		actorRef ! (Bus.Disconnect(940))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(940))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(940))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(940))
		actorRef ! (Bus.Unsubscribe(940))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(940))
		actorRef ! (Bus.Publish(940, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(940))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(940))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(940, "foo"))
		actorRef ! (Bus.DestroyConnection(940))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(940))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(940))
		actorRef ! (Bus.CreateConnection(940))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(940))
		actorRef ! (Bus.Connect(940))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(940))
		expectMsg(Bus.Success)

	}

	"The System-under-test (941)" should "AUTO-GEN-TEST (941)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(941))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(941))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(941))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(941))
		actorRef ! (Bus.Connect(941))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(941))
		actorRef ! (Bus.Disconnect(941))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(941))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(941))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(941))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(941))
		expectMsg(Bus.Error)

	}

	"The System-under-test (942)" should "AUTO-GEN-TEST (942)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(942))
		actorRef ! (Bus.CreateConnection(942))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(942))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(942))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(942))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(942))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(942))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(942))
		expectMsg(Bus.Success)

	}

	"The System-under-test (943)" should "AUTO-GEN-TEST (943)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(943))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(943))
		actorRef ! (Bus.DestroyConnection(943))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(943))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(943, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(943))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(943, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(943))
		actorRef ! (Bus.DestroyConnection(943))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(943, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(943))
		actorRef ! (Bus.Publish(943, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(943))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(943))
		actorRef ! (Bus.UnsubscribeCallback(943))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)

	}

	"The System-under-test (944)" should "AUTO-GEN-TEST (944)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(944))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(944, "foo"))
		actorRef ! (Bus.Subscribe(944))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(944))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(944))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(944))
		expectMsg(Bus.Error)

	}

	"The System-under-test (945)" should "AUTO-GEN-TEST (945)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(945))
		actorRef ! (Bus.SubscribeCallback(945))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(945))
		actorRef ! (Bus.Publish(945, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(945))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(945))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(945))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(945))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(945))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(945, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(945))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(945))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(945))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(945))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(945))
		expectMsg(Bus.Error)

	}

	"The System-under-test (946)" should "AUTO-GEN-TEST (946)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(946))
		actorRef ! (Bus.Publish(946, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(946))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(946))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(946))
		actorRef ! (Bus.UnsubscribeCallback(946))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(946))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(946))
		actorRef ! (Bus.CreateConnection(946))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(946))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(946))
		expectMsg(Bus.Error)

	}

	"The System-under-test (947)" should "AUTO-GEN-TEST (947)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(947))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(947))
		actorRef ! (Bus.SubscribeCallback(947))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(947))
		actorRef ! (Bus.Subscribe(947))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(947))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(947))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(947))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(947))
		expectMsg(Bus.Error)

	}

	"The System-under-test (948)" should "AUTO-GEN-TEST (948)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(948))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(948))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(948))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(948, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (949)" should "AUTO-GEN-TEST (949)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(949))
		actorRef ! (Bus.DestroyConnection(949))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(949))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(949))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(949))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(949))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(949))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(949))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(949, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (950)" should "AUTO-GEN-TEST (950)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(950))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(950))
		actorRef ! (Bus.Unsubscribe(950))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(950))
		actorRef ! (Bus.CreateConnection(950))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(950))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(950))
		expectMsg(Bus.Error)

	}

	"The System-under-test (951)" should "AUTO-GEN-TEST (951)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(951))
		actorRef ! (Bus.Disconnect(951))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(951))
		actorRef ! (Bus.Unsubscribe(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(951))
		actorRef ! (Bus.Connect(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(951))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(951))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(951))
		actorRef ! (Bus.Subscribe(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(951))
		actorRef ! (Bus.Unsubscribe(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(951))
		actorRef ! (Bus.DestroyConnection(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(951))
		actorRef ! (Bus.CreateConnection(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(951))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(951))
		actorRef ! (Bus.Disconnect(951))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(951))
		expectMsg(Bus.Error)

	}

	"The System-under-test (952)" should "AUTO-GEN-TEST (952)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(952))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(952))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(952))
		actorRef ! (Bus.DestroyConnection(952))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(952))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(952))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(952))
		expectMsg(Bus.Success)

	}

	"The System-under-test (953)" should "AUTO-GEN-TEST (953)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(953))
		actorRef ! (Bus.Publish(953, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(953))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(953))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(953))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(953))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(953))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(953))
		expectMsg(Bus.Error)

	}

	"The System-under-test (954)" should "AUTO-GEN-TEST (954)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(954))
		actorRef ! (Bus.Connect(954))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(954))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(954))
		actorRef ! (Bus.SubscribeCallback(954))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(954, "foo"))
		actorRef ! (Bus.Publish(954, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(954))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(954))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(954))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(954))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(954))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(954))
		expectMsg(Bus.Error)

	}

	"The System-under-test (955)" should "AUTO-GEN-TEST (955)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(955))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(955))
		actorRef ! (Bus.UnsubscribeCallback(955))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(955))
		actorRef ! (Bus.Publish(955, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(955))
		actorRef ! (Bus.CreateConnection(955))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(955))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(955))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(955))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(955))
		expectMsg(Bus.Success)

	}

	"The System-under-test (956)" should "AUTO-GEN-TEST (956)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(956))
		actorRef ! (Bus.SubscribeCallback(956))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(956))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(956))
		actorRef ! (Bus.UnsubscribeCallback(956))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(956))
		actorRef ! (Bus.Unsubscribe(956))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(956))
		actorRef ! (Bus.UnsubscribeCallback(956))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(956))
		actorRef ! (Bus.Connect(956))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(956))
		actorRef ! (Bus.CreateConnection(956))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(956))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(956))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(956))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(956, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(956))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(956))
		expectMsg(Bus.Error)

	}

	"The System-under-test (957)" should "AUTO-GEN-TEST (957)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(957))
		actorRef ! (Bus.Subscribe(957))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(957))
		actorRef ! (Bus.UnsubscribeCallback(957))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(957, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(957))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(957))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(957, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(957))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(957))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(957))
		expectMsg(Bus.Error)

	}

	"The System-under-test (958)" should "AUTO-GEN-TEST (958)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(958))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(958))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(958))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(958))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(958, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(958))
		actorRef ! (Bus.SubscribeCallback(958))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(958))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(958, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(958))
		expectMsg(Bus.Error)

	}

	"The System-under-test (959)" should "AUTO-GEN-TEST (959)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(959))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(959))
		expectMsg(Bus.Error)

	}

	"The System-under-test (960)" should "AUTO-GEN-TEST (960)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(960))
		actorRef ! (Bus.Unsubscribe(960))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(960))
		actorRef ! (Bus.Unsubscribe(960))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(960, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(960))
		expectMsg(Bus.Error)

	}

	"The System-under-test (961)" should "AUTO-GEN-TEST (961)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(961))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(961))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(961))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(961))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(961))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(961))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(961))
		expectMsg(Bus.Success)

	}

	"The System-under-test (962)" should "AUTO-GEN-TEST (962)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(962))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(962))
		actorRef ! (Bus.SubscribeCallback(962))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(962))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(962))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(962))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(962))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(962))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(962, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (963)" should "AUTO-GEN-TEST (963)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(963))
		actorRef ! (Bus.SubscribeCallback(963))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(963))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(963))
		expectMsg(Bus.Error)

	}

	"The System-under-test (964)" should "AUTO-GEN-TEST (964)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(964))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(964, "foo"))
		actorRef ! (Bus.SubscribeCallback(964))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(964))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(964, "foo"))
		actorRef ! (Bus.Unsubscribe(964))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(964))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(964))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(964))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(964))
		expectMsg(Bus.Error)

	}

	"The System-under-test (965)" should "AUTO-GEN-TEST (965)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(965))
		expectMsg(Bus.Success)

	}

	"The System-under-test (966)" should "AUTO-GEN-TEST (966)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(966))
		actorRef ! (Bus.Connect(966))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(966))
		actorRef ! (Bus.Disconnect(966))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(966))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(966, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(966))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(966))
		expectMsg(Bus.Error)

	}

	"The System-under-test (967)" should "AUTO-GEN-TEST (967)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(967))
		actorRef ! (Bus.Disconnect(967))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(967))
		actorRef ! (Bus.Subscribe(967))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(967))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(967))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(967))
		expectMsg(Bus.Success)

	}

	"The System-under-test (968)" should "AUTO-GEN-TEST (968)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(968))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(968))
		actorRef ! (Bus.Disconnect(968))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(968, "foo"))
		actorRef ! (Bus.UnsubscribeCallback(968))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(968))
		actorRef ! (Bus.UnsubscribeCallback(968))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(968))
		actorRef ! (Bus.Publish(968, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(968))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(968))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(968))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(968))
		expectMsg(Bus.Error)

	}

	"The System-under-test (969)" should "AUTO-GEN-TEST (969)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(969))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(969))
		actorRef ! (Bus.UnsubscribeCallback(969))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(969))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(969))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(969))
		expectMsg(Bus.Error)

	}

	"The System-under-test (970)" should "AUTO-GEN-TEST (970)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(970))
		actorRef ! (Bus.DestroyConnection(970))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(970))
		actorRef ! (Bus.Disconnect(970))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(970))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(970))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(970))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(970))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(970))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(970))
		expectMsg(Bus.Error)

	}

	"The System-under-test (971)" should "AUTO-GEN-TEST (971)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(971))
		actorRef ! (Bus.SubscribeCallback(971))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(971))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(971))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(971))
		actorRef ! (Bus.Connect(971))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(971))
		actorRef ! (Bus.Connect(971))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(971))
		actorRef ! (Bus.DestroyConnection(971))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(971))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(971))
		actorRef ! (Bus.DestroyConnection(971))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(971))
		actorRef ! (Bus.Publish(971, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(971))
		actorRef ! (Bus.Subscribe(971))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(971))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(971, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(971))
		expectMsg(Bus.Error)

	}

	"The System-under-test (972)" should "AUTO-GEN-TEST (972)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(972))
		actorRef ! (Bus.UnsubscribeCallback(972))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(972))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(972))
		actorRef ! (Bus.SubscribeCallback(972))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (973)" should "AUTO-GEN-TEST (973)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(973))
		actorRef ! (Bus.Subscribe(973))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(973))
		actorRef ! (Bus.Subscribe(973))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(973, "foo"))
		actorRef ! (Bus.Subscribe(973))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(973))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(973))
		actorRef ! (Bus.Disconnect(973))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(973))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(973))
		actorRef ! (Bus.SubscribeCallback(973))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(973))
		actorRef ! (Bus.UnsubscribeCallback(973))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(973))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(973))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(973))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(973))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(973))
		expectMsg(Bus.Success)

	}

	"The System-under-test (974)" should "AUTO-GEN-TEST (974)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(974))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(974))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(974))
		expectMsg(Bus.Error)

	}

	"The System-under-test (975)" should "AUTO-GEN-TEST (975)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(975, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(975, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(975))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(975))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(975, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (976)" should "AUTO-GEN-TEST (976)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(976))
		actorRef ! (Bus.DestroyConnection(976))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(976))
		actorRef ! (Bus.UnsubscribeCallback(976))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(976))
		actorRef ! (Bus.Disconnect(976))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(976))
		actorRef ! (Bus.Unsubscribe(976))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(976))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(976))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(976))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(976))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(976))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(976))
		expectMsg(Bus.Error)

	}

	"The System-under-test (977)" should "AUTO-GEN-TEST (977)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(977))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(977))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(977))
		expectMsg(Bus.Error)

	}

	"The System-under-test (978)" should "AUTO-GEN-TEST (978)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(978))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(978))
		actorRef ! (Bus.CreateConnection(978))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(978))
		actorRef ! (Bus.Unsubscribe(978))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(978))
		actorRef ! (Bus.Unsubscribe(978))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(978))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(978))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(978, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (979)" should "AUTO-GEN-TEST (979)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(979))
		actorRef ! (Bus.Disconnect(979))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(979))
		actorRef ! (Bus.CreateConnection(979))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(979))
		actorRef ! (Bus.DestroyConnection(979))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(979))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(979))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(979))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(979))
		expectMsg(Bus.Error)

	}

	"The System-under-test (980)" should "AUTO-GEN-TEST (980)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(980))
		actorRef ! (Bus.CreateConnection(980))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(980))
		actorRef ! (Bus.Connect(980))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(980))
		actorRef ! (Bus.Disconnect(980))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(980))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(980))
		actorRef ! (Bus.CreateConnection(980))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(980))
		actorRef ! (Bus.CreateConnection(980))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(980))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(980))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(980))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(980))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(980))
		actorRef ! (Bus.CreateConnection(980))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(980))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(980))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(980))
		expectMsg(Bus.Error)

	}

	"The System-under-test (981)" should "AUTO-GEN-TEST (981)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(981))
		actorRef ! (Bus.Subscribe(981))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(981))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(981))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(981))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(981))
		expectMsg(Bus.Error)

	}

	"The System-under-test (982)" should "AUTO-GEN-TEST (982)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(982))
		actorRef ! (Bus.Subscribe(982))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(982))
		actorRef ! (Bus.Unsubscribe(982))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(982))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(982))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(982))
		actorRef ! (Bus.DestroyConnection(982))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(982))
		actorRef ! (Bus.Connect(982))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(982, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(982))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(982))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(982))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(982, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (983)" should "AUTO-GEN-TEST (983)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(983))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(983))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(983))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(983))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(983))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(983))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(983))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(983))
		expectMsg(Bus.Success)

	}

	"The System-under-test (984)" should "AUTO-GEN-TEST (984)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(984))
		actorRef ! (Bus.Disconnect(984))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(984))
		actorRef ! (Bus.Connect(984))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(984))
		actorRef ! (Bus.Connect(984))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(984))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(984))
		actorRef ! (Bus.Publish(984, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(984, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (985)" should "AUTO-GEN-TEST (985)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(985))
		actorRef ! (Bus.SubscribeCallback(985))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(985))
		actorRef ! (Bus.Connect(985))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(985))
		actorRef ! (Bus.SubscribeCallback(985))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(985))
		actorRef ! (Bus.Subscribe(985))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(985))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(985))
		actorRef ! (Bus.DestroyConnection(985))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(985))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(985))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(985))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(985))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(985))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(985))
		expectMsg(Bus.Success)

	}

	"The System-under-test (986)" should "AUTO-GEN-TEST (986)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(986))
		actorRef ! (Bus.Unsubscribe(986))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(986))
		actorRef ! (Bus.Connect(986))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(986))
		actorRef ! (Bus.CreateConnection(986))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(986))
		actorRef ! (Bus.Disconnect(986))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(986, "foo"))
		actorRef ! (Bus.Unsubscribe(986))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(986))
		actorRef ! (Bus.Publish(986, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(986))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(986, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (987)" should "AUTO-GEN-TEST (987)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(987))
		actorRef ! (Bus.UnsubscribeCallback(987))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(987))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(987))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(987))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(987))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(987))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(987))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(987))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(987))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(987))
		expectMsg(Bus.Error)

	}

	"The System-under-test (988)" should "AUTO-GEN-TEST (988)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(988))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(988))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(988))
		actorRef ! (Bus.Subscribe(988))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(988))
		expectMsg(Bus.Success)

	}

	"The System-under-test (989)" should "AUTO-GEN-TEST (989)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(989))
		actorRef ! (Bus.Publish(989, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(989))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Disconnect(989))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(989))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(989))
		actorRef ! (Bus.CreateConnection(989))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(989))
		actorRef ! (Bus.Subscribe(989))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(989, "foo"))
		actorRef ! (Bus.SubscribeCallback(989))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(989))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(989))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(989, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(989, "foo"))
		actorRef ! (Bus.Subscribe(989))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(989, "foo"))
		actorRef ! (Bus.CreateConnection(989))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(989))
		actorRef ! (Bus.Publish(989, "foo"))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(989))
		expectMsg(Bus.Success)

	}

	"The System-under-test (990)" should "AUTO-GEN-TEST (990)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(990))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(990, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(990, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(990))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(990))
		expectMsg(Bus.Error)

	}

	"The System-under-test (991)" should "AUTO-GEN-TEST (991)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(991))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(991))
		actorRef ! (Bus.CreateConnection(991))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(991))
		actorRef ! (Bus.Subscribe(991))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(991))
		actorRef ! (Bus.DestroyConnection(991))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(991))
		actorRef ! (Bus.Subscribe(991))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(991))
		actorRef ! (Bus.Unsubscribe(991))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(991))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(991))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(991))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(991))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(991))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(991, "foo"))
		expectMsg(Bus.Error)

	}

	"The System-under-test (992)" should "AUTO-GEN-TEST (992)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(992))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(992))
		actorRef ! (Bus.Connect(992))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(992))
		actorRef ! (Bus.Publish(992, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(992))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(992))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(992))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(992))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(992))
		expectMsg(Bus.Success)

	}

	"The System-under-test (993)" should "AUTO-GEN-TEST (993)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(993))
		actorRef ! (Bus.DestroyConnection(993))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(993))
		actorRef ! (Bus.Subscribe(993))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(993))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(993))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(993))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(993))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(993))
		expectMsg(Bus.Error)

	}

	"The System-under-test (994)" should "AUTO-GEN-TEST (994)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(994))
		actorRef ! (Bus.Connect(994))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(994))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(994))
		actorRef ! (Bus.Unsubscribe(994))
		expectMsg(Bus.Error)
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(994))
		expectMsg(Bus.Success)
		actorRef ! (Bus.UnsubscribeCallback(994))
		actorRef ! (Bus.Connect(994))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)

	}

	"The System-under-test (995)" should "AUTO-GEN-TEST (995)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(995))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(995))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(995))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Unsubscribe(995))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(995))
		actorRef ! (Bus.Publish(995, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(995))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(995))
		actorRef ! (Bus.Connect(995))
		expectMsg(Bus.Success)
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(995, "foo"))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(995, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (996)" should "AUTO-GEN-TEST (996)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(996))
		actorRef ! (Bus.CreateConnection(996))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Subscribe(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(996, "foo"))
		actorRef ! (Bus.Subscribe(996))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.DestroyConnection(996))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Subscribe(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(996))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(996, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(996))
		actorRef ! (Bus.Publish(996, "foo"))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(996))
		actorRef ! (Bus.DestroyConnection(996))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(996))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(996, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(996))
		expectMsg(Bus.Error)

	}

	"The System-under-test (997)" should "AUTO-GEN-TEST (997)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(997))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(997))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(997, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(997))
		expectMsg(Bus.Success)
		actorRef ! (Bus.DestroyConnection(997))
		expectMsg(Bus.Success)

	}

	"The System-under-test (998)" should "AUTO-GEN-TEST (998)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(998))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Connect(998))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(998, "foo"))
		actorRef ! (Bus.Connect(998))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(998))
		expectMsg(Bus.Success)
		actorRef ! (Bus.CreateConnection(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(998, "foo"))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Unsubscribe(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.CreateConnection(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(998))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Connect(998))
		expectMsg(Bus.Success)
		actorRef ! (Bus.SubscribeCallback(998))
		expectMsg(Bus.Success)
		actorRef ! (Bus.Publish(998, "foo"))
		expectMsg(Bus.Success)

	}

	"The System-under-test (999)" should "AUTO-GEN-TEST (999)" in {
		val actorRef = TestActorRef[Bus]

		actorRef ! (Bus.CreateConnection(999))
		actorRef ! (Bus.Subscribe(999))
		expectMsg(Bus.Success)
		expectMsg(Bus.Error)
		actorRef ! (Bus.Disconnect(999))
		expectMsg(Bus.Error)
		actorRef ! (Bus.Publish(999, "foo"))
		actorRef ! (Bus.Disconnect(999))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(999))
		actorRef ! (Bus.Disconnect(999))
		expectMsg(Bus.Error)
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(999))
		expectMsg(Bus.Error)
		actorRef ! (Bus.SubscribeCallback(999))
		expectMsg(Bus.Error)
		actorRef ! (Bus.UnsubscribeCallback(999))
		expectMsg(Bus.Error)

	}

}

